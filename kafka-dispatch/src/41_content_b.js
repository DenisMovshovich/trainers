/* ------------------------------------------------ 3 */
{
n:3, id:"prod", title:"Продюсер", sub:"Ключи и запись",
lede:"Как продюсер выбирает партицию, зачем нужен ключ, что делают батчи и почему ключ — это решение об архитектуре, а не о формате.",
theory:`
<h2>Из чего состоит запись</h2>
<div class="tw"><table>
<tr><th>Поле</th><th>Назначение</th></tr>
<tr><td><b>ключ</b></td><td>определяет партицию; может быть пустым</td></tr>
<tr><td><b>значение</b></td><td>полезная нагрузка</td></tr>
<tr><td><b>заголовки</b></td><td>метаданные: идентификатор трассировки, версия схемы, тип события</td></tr>
<tr><td><b>отметка времени</b></td><td>время события или время записи</td></tr>
</table></div>
<p>Ключ — <b>не</b> идентификатор записи и <b>не</b> средство поиска. По ключу нельзя запросить запись: Kafka не индекс. Ключ решает ровно одну задачу — куда положить.</p>

<h2>Как выбирается партиция</h2>
<figure class="fig">
<svg viewBox="0 0 620 168" role="img" aria-label="Выбор партиции продюсером">
 <g font-family="var(--fm)" font-size="10">
  <rect x="0" y="16" width="128" height="30" rx="3" fill="var(--surf3)" stroke="var(--line)"/>
  <text x="64" y="35" text-anchor="middle" font-size="9.5" fill="var(--ink)">указана партиция?</text>
  <path d="M132 31h30" stroke="var(--ok)" stroke-width="1.2"/><path d="M162 31l-7-4v8z" fill="var(--ok)"/>
  <text x="168" y="34" font-size="9" fill="var(--ok)">да → берём её, ключ не смотрим</text>
  <path d="M64 50v18" stroke="var(--line2)" stroke-width="1.2"/><path d="M64 68l-4-7h8z" fill="var(--line2)"/>
  <rect x="0" y="72" width="128" height="30" rx="3" fill="var(--surf3)" stroke="var(--line)"/>
  <text x="64" y="91" text-anchor="middle" font-size="9.5" fill="var(--ink)">есть ключ?</text>
  <path d="M132 87h30" stroke="var(--acc)" stroke-width="1.2"/><path d="M162 87l-7-4v8z" fill="var(--acc)"/>
  <text x="168" y="83" font-size="9" fill="var(--acc)">да → murmur2(key) &amp; 0x7fffffff % N</text>
  <text x="168" y="95" font-size="8.5" fill="var(--ink3)">один ключ всегда даёт одну и ту же партицию</text>
  <path d="M64 106v18" stroke="var(--line2)" stroke-width="1.2"/><path d="M64 124l-4-7h8z" fill="var(--line2)"/>
  <rect x="0" y="128" width="128" height="30" rx="3" fill="var(--key-s)" stroke="var(--key)"/>
  <text x="64" y="147" text-anchor="middle" font-size="9.5" fill="var(--ink)">ключа нет</text>
  <path d="M132 143h30" stroke="var(--key)" stroke-width="1.2"/><path d="M162 143l-7-4v8z" fill="var(--key)"/>
  <text x="168" y="140" font-size="9" fill="var(--key)">липкое распределение: пачка в одну партицию,</text>
  <text x="168" y="152" font-size="9" fill="var(--key)">затем следующая — в другую</text>
 </g>
</svg>
<figcaption>Хеш берётся от байтов ключа, а не от его смысла: <code>"42"</code> и <code>42</code> в разных сериализациях дадут разные партиции.</figcaption>
</figure>

<h2>Ключ выбирают по единице порядка</h2>
<p>Вопрос при выборе ключа один: <b>для какой сущности события обязаны идти строго по порядку?</b></p>
<div class="tw"><table>
<tr><th>Задача</th><th>Ключ</th><th>Почему</th></tr>
<tr><td>события заказа</td><td><code>order_id</code></td><td>«создан» обязан прийти раньше «оплачен»</td></tr>
<tr><td>изменения профиля</td><td><code>user_id</code></td><td>последнее изменение должно оказаться последним</td></tr>
<tr><td>метрики хоста</td><td><code>host</code> или пусто</td><td>порядок между хостами не важен</td></tr>
<tr><td>журнал доступа</td><td>пусто</td><td>порядок не нужен вовсе, важна равномерность</td></tr>
</table></div>
<div class="note trap"><b class="hd">Перекос: одна партиция горячее остальных</b><p>Ключ <code>country</code> в сервисе, где 80% трафика из одной страны, отправит 80% записей в одну партицию. Один потребитель захлебнётся, остальные будут простаивать, а добавление партиций не поможет: ключ всё равно один.</p><p>Лечится выбором более дробного ключа (<code>user_id</code> вместо <code>country</code>) либо составным ключом с искусственным разбросом: <code>country + "-" + (user_id % 10)</code> — если порядок нужен внутри пользователя, а не внутри страны.</p></div>

<h2>Батчи и задержка</h2>
<p>Продюсер не отправляет каждую запись отдельно: он копит их в буфере и шлёт пачкой на брокер-лидер. Два параметра управляют компромиссом:</p>
<div class="tw"><table>
<tr><th>Параметр</th><th>Смысл</th></tr>
<tr><td><code>batch.size</code></td><td>максимальный размер пачки в байтах</td></tr>
<tr><td><code>linger.ms</code></td><td>сколько ждать наполнения пачки перед отправкой; по умолчанию 0</td></tr>
<tr><td><code>compression.type</code></td><td>сжатие пачки целиком: <code>lz4</code>, <code>zstd</code>, <code>snappy</code></td></tr>
<tr><td><code>max.in.flight.requests.per.connection</code></td><td>сколько неподтверждённых запросов может быть в полёте</td></tr>
</table></div>
<p><code>linger.ms=0</code> означает «отправлять сразу» — минимальная задержка, худшая пропускная способность. Значение в 5–20 мс обычно даёт кратный рост пропускной способности почти без ущерба для отклика, потому что сжимается пачка, а не каждая запись.</p>
<div class="note warn"><b class="hd">Отправка асинхронна</b><p><code>send()</code> кладёт запись в буфер и возвращает будущее значение. Если не проверять результат — ни в обратном вызове, ни ожиданием, — ошибки записи пройдут незамеченными, и данные потеряются молча. Это одна из самых частых ошибок при работе с Kafka.</p></div>
`,
quiz:[
 {q:"Что определяет ключ записи?",
  opts:["Уникальность записи","Партицию, в которую она попадёт","Срок хранения","Право доступа"],
  a:1, why:"Ключ не идентификатор и не средство поиска: по нему нельзя запросить запись. Он отвечает только на вопрос «куда положить»."},
 {q:"Записи с ключом <code>user-7</code> уходят в партицию 1. Куда пойдёт следующая с тем же ключом?",
  opts:["В любую свободную","Снова в партицию 1 — пока число партиций не изменилось","В следующую по кругу","В наименее загруженную"],
  a:1, why:"Партиция считается как murmur2(key) % N. При неизменном N результат детерминирован — на этом и держится гарантия порядка для сущности."},
 {q:"В сервисе 80% трафика из одной страны, ключ — <code>country</code>. Что будет?",
  opts:["Kafka выровняет нагрузку","Одна партиция станет горячей, остальные будут простаивать","Записи начнут теряться","Продюсер переключится на распределение по кругу"],
  a:1, why:"Перекос по ключу не лечится добавлением партиций. Нужен более дробный ключ или искусственный разброс внутри ключа."},
 {q:"Что делает <code>linger.ms=10</code>?",
  opts:["Отбрасывает записи старше 10 мс","Ждёт до 10 мс, накапливая пачку, ради пропускной способности","Повторяет отправку через 10 мс","Ограничивает время жизни соединения"],
  a:1, why:"Небольшое ожидание позволяет собрать и сжать пачку целиком. Пропускная способность растёт кратно, задержка — на единицы миллисекунд."},
 {q:"Что произойдёт, если не проверять результат асинхронного <code>send()</code>?",
  opts:["Ничего, клиент повторит сам","Ошибки записи пройдут незамеченными, и данные потеряются молча","Продюсер остановится","Записи уйдут дважды"],
  a:1, why:"send() лишь кладёт запись в буфер. Без обратного вызова или ожидания результата отказ брокера ничем себя не проявит."},
 {q:"Куда попадёт запись, если явно указана партиция и задан ключ?",
  opts:["В партицию по ключу","В явно указанную партицию — ключ на выбор не влияет","В обе","Будет ошибка"],
  a:1, why:"Явное указание имеет приоритет. Ключ при этом сохраняется в записи и продолжает работать при уплотнении."}
],
labs:[
 {id:"3a", title:"Ключ решает",
  brief:"<ul><li>Создайте топик <code>clicks</code> с 4 партициями и репликацией 1</li><li>Запишите 4 события без ключа и посмотрите <code>dump</code> — они разошлись</li><li>Запишите события двух пользователей: <code>--records u1:a,u2:a,u1:b,u2:b,u1:c</code></li><li>В <code>dump</code> убедитесь: все <code>u1</code> в одной партиции и идут подряд, все <code>u2</code> — в своей</li><li>Запишите запись с ключом <code>u1</code>, но явно указав <code>--partition 3</code>: она уйдёт туда, куда сказано</li></ul>",
  hint:"produce --topic clicks --key u1 --value forced --partition 3",
  checks:[
   {label:"Топик <code>clicks</code> создан с 4 партициями", test:st=>{const t=topic(st,"clicks"); return !!t&&t.partitions.length===4;}},
   {label:"Записи без ключа разошлись минимум по трём партициям", test:st=>{const t=topic(st,"clicks"); return !!t&&t.partitions.filter(p=>p.log.some(r=>r.key===null)).length>=3;}},
   {label:"Все записи <code>u1</code> в одной партиции", test:st=>{const t=topic(st,"clicks"); if(!t) return false;
     const ps=t.partitions.filter(p=>p.log.some(r=>r.key==="u1"&&r.value!=="forced")); return ps.length===1&&ps[0].log.filter(r=>r.key==="u1").length>=3;}},
   {label:"Записи <code>u2</code> легли в свою партицию", test:st=>{const t=topic(st,"clicks"); if(!t) return false;
     const ps=t.partitions.filter(p=>p.log.some(r=>r.key==="u2")); return ps.length===1&&ps[0].log.filter(r=>r.key==="u2").length>=2;}},
   {label:"Явная партиция перекрыла ключ", test:st=>{const t=topic(st,"clicks"); return !!t&&t.partitions[3].log.some(r=>r.key==="u1");}}
  ]},
 {id:"3b", title:"Перекос по ключу",
  brief:"<p>Смоделируйте горячую партицию и вылечите её.</p><ul><li>Создайте топик <code>hot</code> с 4 партициями, репликация 1</li><li>Запишите 8 событий с одним и тем же ключом <code>ru</code> — весь трафик уйдёт в одну партицию</li><li>Посмотрите <code>dump --topic hot</code>: три партиции пусты</li><li>Теперь запишите 8 событий с составными ключами <code>ru-0</code>, <code>ru-1</code>, <code>ru-2</code>, <code>ru-3</code> — по два на каждый</li><li>Убедитесь, что задействовано больше одной партиции</li></ul>",
  hint:"produce --topic hot --records ru-0:a,ru-1:a,ru-2:a,ru-3:a,ru-0:b,ru-1:b,ru-2:b,ru-3:b",
  checks:[
   {label:"Топик <code>hot</code> создан с 4 партициями", test:st=>{const t=topic(st,"hot"); return !!t&&t.partitions.length===4;}},
   {label:"Восемь записей с ключом <code>ru</code> попали в одну партицию", test:st=>{const t=topic(st,"hot"); if(!t) return false;
     const ps=t.partitions.filter(p=>p.log.some(r=>r.key==="ru")); return ps.length===1&&ps[0].log.filter(r=>r.key==="ru").length>=8;}},
   {label:"Использованы составные ключи с разбросом", test:st=>allRecs(st,"hot").filter(r=>/^ru-\d$/.test(String(r.key))).length>=8},
   {label:"Составные ключи задействовали больше одной партиции", test:st=>{const t=topic(st,"hot"); if(!t) return false;
     return t.partitions.filter(p=>p.log.some(r=>/^ru-\d$/.test(String(r.key)))).length>=2;}}
  ]}
]
},

/* ------------------------------------------------ 4 */
{
n:4, id:"acks", title:"Гарантии записи", sub:"acks, ISR, min.insync.replicas",
lede:"Три значения одного параметра определяют, потеряются ли данные при падении брокера. Здесь это видно вживую.",
theory:`
<p>Продюсер пишет только <b>лидеру</b> партиции. Остальные реплики (фолловеры) сами вычитывают у лидера. Параметр <code>acks</code> отвечает на вопрос: в какой момент лидер считает запись успешной и отвечает продюсеру.</p>

<div class="tw"><table>
<tr><th>acks</th><th>Когда приходит подтверждение</th><th>Что теряется</th></tr>
<tr><td><code>0</code></td><td>сразу после отправки в сокет</td><td>всё, что не дошло; отказы не видны вовсе</td></tr>
<tr><td><code>1</code></td><td>после записи у лидера</td><td>записи, не успевшие размножиться, если лидер упал</td></tr>
<tr><td><code>all</code></td><td>после записи во все реплики ISR</td><td>ничего, пока жива хоть одна реплика из ISR</td></tr>
</table></div>

<h2>ISR — множество догнавших реплик</h2>
<p><b>ISR</b> (in-sync replicas) — те реплики, которые не отстали от лидера дольше <code>replica.lag.time.max.ms</code>. Лидер всегда в ISR. Отставшая реплика выбывает из ISR и возвращается, когда догонит.</p>
<figure class="fig">
<svg viewBox="0 0 620 176" role="img" aria-label="acks=1 против acks=all">
 <g font-family="var(--fm)" font-size="10">
  <text x="0" y="12" font-size="10" font-weight="700" fill="var(--warn)">acks=1</text>
  <rect x="0" y="20" width="150" height="24" rx="2" fill="var(--acc-s)" stroke="var(--acc)"/>
  <text x="8" y="36" font-size="9" fill="var(--ink)">лидер   LEO=5</text>
  <rect x="0" y="48" width="150" height="24" rx="2" fill="var(--surf3)" stroke="var(--line)"/>
  <text x="8" y="64" font-size="9" fill="var(--ink2)">реплика LEO=2</text>
  <text x="158" y="30" font-size="9" fill="var(--ink3)">продюсер уже получил «ок» на записи 2, 3, 4</text>
  <text x="158" y="42" font-size="9" fill="var(--ink3)">высокая отметка при этом всё ещё 2</text>
  <text x="158" y="60" font-size="9" fill="var(--trap)">лидер падает → записи 2, 3, 4 исчезают навсегда</text>

  <line x1="0" y1="88" x2="620" y2="88" stroke="var(--line)"/>

  <text x="0" y="108" font-size="10" font-weight="700" fill="var(--ok)">acks=all</text>
  <rect x="0" y="116" width="150" height="24" rx="2" fill="var(--acc-s)" stroke="var(--acc)"/>
  <text x="8" y="132" font-size="9" fill="var(--ink)">лидер   LEO=5</text>
  <rect x="0" y="144" width="150" height="24" rx="2" fill="var(--ok-s)" stroke="var(--ok)"/>
  <text x="8" y="160" font-size="9" fill="var(--ink)">реплика LEO=5</text>
  <text x="158" y="126" font-size="9" fill="var(--ink3)">«ок» приходит только после записи во все реплики ISR</text>
  <text x="158" y="138" font-size="9" fill="var(--ink3)">высокая отметка = 5, данные видны потребителям</text>
  <text x="158" y="158" font-size="9" fill="var(--ok)">лидер падает → реплика становится лидером, ничего не потеряно</text>
 </g>
</svg>
<figcaption>Цена <code>acks=all</code> — задержка на один круг репликации. Цена <code>acks=1</code> — окно, в котором подтверждённые данные существуют в одном экземпляре.</figcaption>
</figure>

<h2>min.insync.replicas</h2>
<div class="note trap"><b class="hd">acks=all без min.insync.replicas ничего не гарантирует</b><p>Если реплики отстали и выбыли, ISR может усохнуть до одной — самого лидера. Тогда <code>acks=all</code> означает «записано у одного лидера», то есть ровно то же, что <code>acks=1</code>, но с чувством выполненного долга.</p><p><code>min.insync.replicas=2</code> заставляет брокер <b>отказать</b> в записи, когда в ISR меньше двух реплик. Продюсер получит <code>NOT_ENOUGH_REPLICAS</code> и будет повторять. Это сознательный выбор: лучше временно не принимать данные, чем принять и потерять.</p></div>
<p>Рабочее сочетание для важных данных: фактор репликации <b>3</b>, <code>min.insync.replicas=2</code>, <code>acks=all</code>. Оно переживает падение одного брокера без потерь и без остановки записи.</p>
<div class="note warn"><b class="hd">Почему не min.insync.replicas=3 при rf=3</b><p>Тогда любое падение любого брокера немедленно останавливает запись во все партиции, где он был репликой. Запас в одну реплику — то, ради чего вообще держат три копии.</p></div>

<h2>В этом тренажёре</h2>
<div class="note"><b class="hd">Шаг репликации сделан явным</b><p>В настоящей Kafka фолловеры тянут данные у лидера непрерывно, и окно расхождения измеряется миллисекундами. Здесь фолловеры догоняют только при <code>acks=all</code> либо по команде <code>replicate</code> — чтобы окно потери данных можно было увидеть и потрогать. Всё остальное поведение совпадает с настоящим.</p></div>
`,
quiz:[
 {q:"При каком значении <code>acks</code> продюсер не узнает об ошибке записи?",
  opts:["<code>acks=all</code>","<code>acks=0</code>","<code>acks=1</code>","При любом узнает"],
  a:1, why:"acks=0 — отправка без подтверждения: клиент не ждёт ответа и не может отличить успех от потери. Годится только для данных, которые не жалко."},
 {q:"Что гарантирует <code>acks=all</code> при <code>min.insync.replicas=1</code> и rf=3?",
  opts:["Данные записаны во все три реплики","Практически ничего: ISR мог усохнуть до одного лидера","Запись отклоняется при сбое","Данные записаны на диск"],
  a:1, why:"acks=all означает «во все реплики ISR», а не «во все реплики». Если в ISR остался один лидер, гарантия вырождается в acks=1."},
 {q:"Что произойдёт при <code>acks=all</code>, <code>min.insync.replicas=2</code> и одной реплике в ISR?",
  opts:["Запись пройдёт","Брокер откажет с <code>NOT_ENOUGH_REPLICAS</code>","Запись уйдёт в другую партицию","Kafka добавит реплику"],
  a:1, why:"Это сознательный отказ: лучше временно не принимать данные, чем принять их в единственном экземпляре и потерять при следующем сбое."},
 {q:"Почему при rf=3 не ставят <code>min.insync.replicas=3</code>?",
  opts:["Так медленнее","Тогда падение любого брокера немедленно останавливает запись","Kafka это запрещает","Это ничего не меняет"],
  a:1, why:"Запас в одну реплику — весь смысл третьей копии. Иначе кластер перестаёт переживать даже плановый перезапуск узла."},
 {q:"Что такое ISR?",
  opts:["Все реплики партиции","Реплики, не отставшие от лидера дольше допустимого","Реплики на других стойках","Реплики только для чтения"],
  a:1, why:"Отставшая реплика выбывает из ISR и возвращается, догнав лидера. От состава ISR зависят и высокая отметка, и работа acks=all."},
 {q:"Кому продюсер отправляет запись?",
  opts:["Любому брокеру, тот разошлёт","Только брокеру-лидеру этой партиции","Всем репликам сразу","Координатору группы"],
  a:1, why:"Вся запись и чтение идут через лидера партиции. Фолловеры сами вычитывают у него — это модель pull, а не push."}
],
labs:[
 {id:"4a", title:"Окно потери данных",
  brief:"<p>Увидьте своими глазами, что теряет <code>acks=1</code>.</p><ul><li>Создайте топик <code>pay</code> с 1 партицией и репликацией 3</li><li>Запишите две записи с <code>--acks all</code> и посмотрите <code>dump --topic pay</code>: LEO и HW совпадают</li><li>Запишите три записи с <code>--acks 1</code> и снова загляните в <code>dump</code>: LEO ушёл вперёд, HW остался позади</li><li>Прочитайте топик с начала — записи выше высокой отметки не видны</li><li>Выполните <code>replicate</code> и прочитайте снова: теперь видны все</li></ul>",
  hint:"HW — это минимум LEO по репликам ISR. Пока фолловеры не догнали, потребителю показывать нечего.",
  checks:[
   {label:"Топик <code>pay</code> создан с репликацией 3", test:st=>{const t=topic(st,"pay"); return !!t&&t.rf===3&&t.partitions.length===1;}},
   {label:"Записано с <code>--acks all</code>", test:st=>okRan(st,/produce[\s\S]*--acks\s+all/)},
   {label:"Записано с <code>--acks 1</code>", test:st=>okRan(st,/produce[\s\S]*--acks\s+1\b/)},
   {label:"Возникло расхождение LEO и высокой отметки", test:st=>st.log.some(e=>!e.err&&/^\s*dump/.test(e.cmd)&&/LEO=(\d+)\s+HW=(\d+)/.test(e.lines.join("\n"))&&(()=>{const m=/LEO=(\d+)\s+HW=(\d+)/.exec(e.lines.join("\n")); return +m[1]>+m[2];})())},
   {label:"После <code>replicate</code> высокая отметка догнала LEO", test:st=>okRan(st,/^\s*replicate/)&&(()=>{const t=topic(st,"pay"); return !!t&&highWatermark(t.partitions[0])===leo(t.partitions[0])&&leo(t.partitions[0])>=5;})()}
  ]},
 {id:"4b", title:"min.insync.replicas в бою",
  brief:"<ul><li>Задайте топику <code>pay</code> настройку <code>min.insync.replicas=3</code></li><li>Остановите один из брокеров: <code>broker stop 2</code></li><li>Попробуйте записать с <code>--acks all</code> — получите отказ <code>NOT_ENOUGH_REPLICAS</code></li><li>Запишите то же самое с <code>--acks 1</code> — пройдёт, но гарантий уже нет</li><li>Поднимите брокер обратно и убедитесь через <code>kafka-topics --describe</code>, что ISR снова полон</li><li>Смените настройку на разумную <code>min.insync.replicas=2</code></li></ul>",
  hint:"kafka-configs --alter --topic pay --add-config min.insync.replicas=3",
  checks:[
   {label:"Настроен <code>min.insync.replicas</code>", test:st=>okRan(st,/kafka-configs[\s\S]*min\.insync\.replicas/)},
   {label:"Брокер остановлен", test:st=>okRan(st,/broker\s+stop\s+\d/)},
   {label:"Запись с <code>acks=all</code> отклонена", test:st=>errRan(st,/min\.insync\.replicas/)},
   {label:"Запись с <code>acks=1</code> при усохшем ISR прошла", test:st=>st.log.some(e=>!e.err&&/produce[\s\S]*--acks\s+1\b/.test(e.cmd)&&/отправлено записей/.test(e.lines.join("\n")))},
   {label:"Брокер поднят и ISR восстановлен", test:st=>okRan(st,/broker\s+start\s+\d/)&&st.c.brokers.every(b=>b.alive)},
   {label:"Итоговая настройка — <code>min.insync.replicas=2</code>", test:st=>{const t=topic(st,"pay"); return !!t&&t.config["min.insync.replicas"]===2;}}
  ]}
]
},

/* ------------------------------------------------ 5 */
{
n:5, id:"repl", title:"Репликация и отказы", sub:"Лидеры, выборы, доступность",
lede:"Что делает кластер, когда брокер падает: как выбирается новый лидер, почему иногда лидера не выбирают вовсе и чем за это платят.",
theory:`
<p>Каждая партиция существует в <code>replication.factor</code> экземплярах на разных брокерах. Один из них — <b>лидер</b>: через него идут все чтения и записи. Остальные — <b>фолловеры</b>: они только вычитывают у лидера и ждут своего часа.</p>
<div class="note"><b class="hd">Фолловеры не обслуживают клиентов</b><p>Это отличает Kafka от многих хранилищ: реплика не разгружает чтение (за исключением специального режима чтения с ближайшей реплики, который включают ради экономии межзонального трафика). Реплики нужны для сохранности и быстрого замещения, а не для масштабирования чтения. Масштабирование чтения в Kafka даёт увеличение числа партиций.</p></div>

<h2>Что происходит при падении брокера</h2>
<figure class="fig">
<svg viewBox="0 0 620 186" role="img" aria-label="Выбор нового лидера при падении брокера">
 <g font-family="var(--fm)" font-size="10">
  ${[["1. брокер-лидер перестаёт отвечать","контроллер замечает пропажу сессии в KRaft"],
     ["2. реплика выбывает из ISR","её LEO больше не догоняет лидера"],
     ["3. новый лидер выбирается из ISR","берётся первая живая реплика списка; данные не теряются"],
     ["4. клиенты получают новые метаданные","продюсеры и консьюмеры переподключаются к новому лидеру"]].map((r,i)=>{
    const y = 12 + i*40;
    return '<rect x="0" y="'+y+'" width="26" height="26" rx="3" fill="var(--acc)"/>'+
      '<text x="13" y="'+(y+18)+'" text-anchor="middle" font-size="11" font-weight="700" fill="#fff">'+(i+1)+'</text>'+
      '<text x="36" y="'+(y+12)+'" font-size="10" fill="var(--ink)">'+r[0].replace(/^\\d+\\. /,"")+'</text>'+
      '<text x="36" y="'+(y+24)+'" font-size="9" fill="var(--ink3)">'+r[1]+'</text>';
  }).join("")}
  <text x="0" y="180" font-size="9" fill="var(--ink3)">Всё это занимает секунды. Клиентские библиотеки переживают смену лидера прозрачно, повторяя запрос.</text>
 </g>
</svg>
<figcaption>Пока в ISR есть хоть одна живая реплика, смена лидера проходит без потери подтверждённых данных.</figcaption>
</figure>

<h2>Когда лидера выбрать не из кого</h2>
<p>Если все реплики из ISR мертвы, а живая реплика есть — но отставшая, — у кластера выбор из двух зол:</p>
<div class="tw"><table>
<tr><th><code>unclean.leader.election.enable</code></th><th>Что происходит</th><th>Цена</th></tr>
<tr><td><code>false</code> (по умолчанию)</td><td>партиция остаётся без лидера, запись и чтение недоступны</td><td>простой до возвращения реплики из ISR</td></tr>
<tr><td><code>true</code></td><td>лидером становится отставшая реплика</td><td>подтверждённые записи, которых у неё нет, исчезают навсегда</td></tr>
</table></div>
<div class="note trap"><b class="hd">Это выбор между доступностью и сохранностью, и он всегда осознанный</b><p>Умолчание <code>false</code> появилось не случайно: молчаливая потеря подтверждённых данных обычно хуже простоя. Включают <code>true</code> там, где поток важнее отдельных записей — например, в телеметрии.</p></div>

<h2>Размещение реплик</h2>
<p>Три копии на трёх брокерах в одной стойке или одной зоне доступности защищают от отказа диска, но не от отказа стойки. Параметр <code>broker.rack</code> заставляет Kafka раскладывать реплики по разным стойкам и зонам.</p>
<div class="note warn"><b class="hd">Предпочитаемый лидер и перекос</b><p>Первая реплика в списке назначения считается <b>предпочитаемой</b>. После восстановления упавшего брокера лидерство к нему само не возвращается — его возвращает периодическая балансировка (<code>auto.leader.rebalance.enable</code>) или ручной запуск. Без этого один брокер может остаться лидером почти всех партиций и стать узким местом, хотя формально кластер здоров.</p></div>
`,
quiz:[
 {q:"Кто обслуживает чтение партиции в обычной конфигурации Kafka?",
  opts:["Любая реплика","Только лидер","Ближайший брокер","Контроллер"],
  a:1, why:"Реплики нужны для сохранности и замещения, а не для масштабирования чтения. Чтение масштабируют числом партиций."},
 {q:"Все реплики из ISR мертвы, живая отставшая реплика есть, <code>unclean.leader.election.enable=false</code>. Что будет?",
  opts:["Лидером станет отставшая реплика","Партиция останется без лидера — запись и чтение недоступны","Kafka создаст новую реплику","Данные перейдут в другую партицию"],
  a:1, why:"Умолчание выбирает сохранность: лучше простой, чем молчаливая потеря подтверждённых записей."},
 {q:"Чем платят за <code>unclean.leader.election.enable=true</code>?",
  opts:["Задержкой записи","Потерей подтверждённых записей, которых нет у новой реплики-лидера","Двойной доставкой","Ростом объёма диска"],
  a:1, why:"Отставшая реплика становится источником истины, и всё, что было записано после её отставания, исчезает."},
 {q:"Зачем нужен <code>broker.rack</code>?",
  opts:["Для приоритета лидерства","Чтобы реплики раскладывались по разным стойкам и зонам","Для ограничения трафика","Для маршрутизации клиентов"],
  a:1, why:"Три копии в одной стойке защищают от отказа диска, но не от отказа стойки целиком."},
 {q:"Брокер восстановился после сбоя. Вернётся ли к нему лидерство само?",
  opts:["Да, немедленно","Нет: нужна балансировка предпочитаемых лидеров","Да, после следующей записи","Лидерство не меняется никогда"],
  a:1, why:"Иначе один брокер может остаться лидером почти всех партиций и стать узким местом при формально здоровом кластере."},
 {q:"Что делает реплика, выбывшая из ISR, после того как догонит лидера?",
  opts:["Остаётся вне ISR до перезапуска","Возвращается в ISR автоматически","Становится лидером","Удаляется"],
  a:1, why:"Членство в ISR определяется одним условием — отставанием не дольше replica.lag.time.max.ms — и пересчитывается постоянно."}
],
labs:[
 {id:"5a", title:"Падение и выборы",
  brief:"<ul><li>Создайте топик <code>audit</code> с 2 партициями и репликацией 3</li><li>Запишите несколько событий с <code>--acks all</code></li><li>Посмотрите <code>broker list</code> — кто лидер для каких партиций</li><li>Остановите брокера, который является лидером хотя бы одной партиции</li><li>Проверьте <code>kafka-topics --describe --topic audit</code>: лидер сменился, ISR усох до двух</li><li>Убедитесь, что запись и чтение продолжают работать</li><li>Поднимите брокер и проверьте, что ISR снова полон</li></ul>",
  hint:"broker list показывает, для каких партиций каждый брокер лидер.",
  checks:[
   {label:"Топик <code>audit</code> создан с репликацией 3", test:st=>{const t=topic(st,"audit"); return !!t&&t.rf===3;}},
   {label:"Просмотрен <code>broker list</code>", test:st=>okRan(st,/^\s*broker(\s+list)?\s*$/)},
   {label:"Брокер остановлен и лидер сменился", test:st=>outHas(st,/партиций, потерявших лидера: [1-9]/)},
   {label:"Запись после сбоя прошла", test:st=>{const idx=st.log.findIndex(e=>!e.err&&/broker\s+stop/.test(e.cmd));
     return idx>=0&&st.log.slice(idx).some(e=>!e.err&&/^\s*produce/.test(e.cmd));}},
   {label:"Брокер поднят, ISR полон", test:st=>{const t=topic(st,"audit"); return !!t&&st.c.brokers.every(b=>b.alive)&&t.partitions.every(p=>isrOf(p).length===3);}}
  ]},
 {id:"5b", title:"Доступность против сохранности",
  brief:"<p>Доведите партицию до состояния без лидера и посмотрите на оба выхода.</p><ul><li>Создайте топик <code>tel</code> с 1 партицией и репликацией 2</li><li>Запишите две записи с <code>--acks all</code></li><li>Остановите <b>оба</b> брокера, где лежат реплики (посмотрите их в <code>--describe</code>)</li><li>Убедитесь, что партиция осталась без лидера, а запись отклоняется</li><li>Включите <code>unclean on</code>, поднимите один из брокеров и посмотрите, что произойдёт</li><li>Верните <code>unclean off</code></li></ul>",
  hint:"kafka-topics --describe --topic tel покажет Replicas — номера брокеров, где лежат реплики.",
  checks:[
   {label:"Топик <code>tel</code> создан с репликацией 2", test:st=>{const t=topic(st,"tel"); return !!t&&t.rf===2&&t.partitions.length===1;}},
   {label:"Оба брокера с репликами остановлены", test:st=>st.log.filter(e=>!e.err&&/broker\s+stop/.test(e.cmd)).length>=2},
   {label:"Партиция осталась без лидера", test:st=>outHas(st,/без лидера/)||st.log.some(e=>!e.err&&/--describe/.test(e.cmd)&&/Leader: none/.test(e.lines.join("\n")))},
   {label:"Запись без лидера отклонена", test:st=>errRan(st,/нет живого лидера/)},
   {label:"Режим <code>unclean</code> включался", test:st=>okRan(st,/^\s*unclean\s+on/)},
   {label:"Режим <code>unclean</code> возвращён в выключенное состояние", test:st=>okRan(st,/^\s*unclean\s+off/)&&st.c.unclean===false}
  ]}
]
},
