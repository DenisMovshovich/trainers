/* ============================================================
   Разделы 13–16
   ============================================================ */
SECTIONS.push(

/* ----------------------------------------------------- 13 */
{
n:13, id:"csharp", title:"C# для автотестов", sub:"Что спрашивают на входе",
lede:"Языковой минимум, без которого не проходят техническую часть: ссылки и значения, строки, коллекции, async и то, как это ломается именно в тестах.",
body:`
<p>Automation QA не обязан знать язык на уровне разработчика бэкенда, но обязан отвечать за код, который пишет. Ниже — темы, которые спрашивают почти всегда, с уклоном в то, как они проявляются в тестовом коде.</p>

<h2>Значения и ссылки</h2>
<div class="tw"><table>
<tr><th></th><th>Типы значений</th><th>Ссылочные типы</th></tr>
<tr><td>Что это</td><td><code>int</code>, <code>bool</code>, <code>double</code>, <code>struct</code>, <code>enum</code>, <code>DateTime</code></td><td><code>class</code>, <code>string</code>, массивы, делегаты, <code>record</code> (по умолчанию)</td></tr>
<tr><td>При присваивании</td><td>копируется значение</td><td>копируется ссылка — оба имени указывают на один объект</td></tr>
<tr><td>Где живёт</td><td>обычно стек либо внутри объекта</td><td>управляемая куча</td></tr>
<tr><td>Значение по умолчанию</td><td>ноль соответствующего вида</td><td><code>null</code></td></tr>
</table></div>
<div class="note trap"><b class="hd">Как это ломает тест</b><p>Передали объект с тестовыми данными в метод-помощник, тот поменял поле — изменился и оригинал, потому что передавалась ссылка. Следующий тест в фикстуре получает уже испорченные данные и падает «без причины». Лечится либо неизменяемыми моделями (<code>record</code> с <code>init</code>-свойствами), либо созданием свежего объекта на каждый тест.</p></div>

<h2>Строки</h2>
<p><code>string</code> — ссылочный тип, но <b>неизменяемый</b>: любая операция создаёт новую строку. Отсюда два вопроса-спутника.</p>
<div class="tw"><table>
<tr><th>Вопрос</th><th>Ответ</th></tr>
<tr><td>Чем <code>==</code> отличается от <code>Equals</code> для строк?</td><td>Для <code>string</code> оператор <code>==</code> перегружен и сравнивает содержимое — то же, что <code>Equals</code>. Для произвольного ссылочного типа <code>==</code> по умолчанию сравнивает ссылки.</td></tr>
<tr><td>Когда нужен <code>StringBuilder</code>?</td><td>Когда строка собирается в цикле. Склейка через <code>+</code> в цикле на тысячу итераций создаёт тысячу временных строк.</td></tr>
<tr><td>Чем плох <code>string.Empty</code> против <code>""</code>?</td><td>Ничем, это одно и то же. А вот <code>null</code> и <code>""</code> — разные вещи: <code>string.IsNullOrEmpty</code> покрывает оба, <code>IsNullOrWhiteSpace</code> — ещё и строку из пробелов.</td></tr>
</table></div>

<h2>Коллекции</h2>
<div class="tw"><table>
<tr><th>Тип</th><th>Когда брать</th><th>Поиск</th></tr>
<tr><td><code>List&lt;T&gt;</code></td><td>упорядоченный набор, доступ по индексу</td><td>линейный, O(n)</td></tr>
<tr><td><code>Dictionary&lt;K,V&gt;</code></td><td>поиск по ключу — тестовые данные по имени случая</td><td>O(1) в среднем</td></tr>
<tr><td><code>HashSet&lt;T&gt;</code></td><td>уникальность, быстрая проверка вхождения</td><td>O(1) в среднем</td></tr>
<tr><td><code>IEnumerable&lt;T&gt;</code></td><td>тип параметра и возврата: не привязывает к реализации</td><td>—</td></tr>
</table></div>
<div class="note"><b class="hd">Отложенное выполнение LINQ</b><p><code>Where</code> и <code>Select</code> ничего не вычисляют в момент вызова — только описывают запрос. Вычисление происходит при переборе или при <code>ToList()</code>, <code>Count()</code>, <code>First()</code>. Поэтому запрос, построенный по коллекции, а затем перебранный после её изменения, выдаст новый результат. В тестах это ловят так: собрали запрос к списку локаторов, страница обновилась — и перебор пошёл по устаревшим элементам.</p></div>

<h2>async и await</h2>
<p>Весь Playwright асинхронен, поэтому тестовые методы возвращают <code>Task</code>. Три вещи, которые спрашивают:</p>
<div class="tw"><table>
<tr><th>Вопрос</th><th>Ответ</th></tr>
<tr><td>Что делает <code>await</code>?</td><td>Освобождает поток до завершения операции и продолжает выполнение с того же места. Это не создание потока.</td></tr>
<tr><td>Чем плох <code>.Result</code> или <code>.Wait()</code>?</td><td>Блокирует поток и в ряде контекстов приводит к взаимной блокировке. В тестах это ещё и съедает воркеров NUnit.</td></tr>
<tr><td>Что такое <code>async void</code>?</td><td>Метод, исключение из которого нельзя поймать вызывающей стороной. В тестах — гарантированно проглоченная ошибка. Допустим только для обработчиков событий.</td></tr>
</table></div>
<div class="note trap"><b class="hd">Забытый await</b><p><code>Page.ClickAsync(...)</code> без <code>await</code> компилируется — и тест идёт дальше, не дождавшись нажатия. Дефект плавающий: иногда успевает, иногда нет. Компилятор предупреждает (CS4014), но предупреждение легко не заметить. Проверьте, включено ли у вас <code>&lt;TreatWarningsAsErrors&gt;</code> — на собеседовании это сильный ответ на вопрос «как вы предотвращаете такие ошибки».</p></div>

<h2>Ещё три темы, которые всплывают</h2>
<div class="tw"><table>
<tr><th>Тема</th><th>Короткий ответ</th></tr>
<tr><td><code>IDisposable</code> и <code>using</code></td><td>Освобождение неуправляемых ресурсов детерминированно, не дожидаясь сборщика мусора. В тестах — соединения, файлы, свой драйвер.</td></tr>
<tr><td>Исключения</td><td>Ловить конкретный тип, а не <code>catch (Exception)</code>. Пустой <code>catch</code> в тесте превращает падение в зелёный прогон — это худшее, что можно сделать.</td></tr>
<tr><td>Nullable-ссылки</td><td><code>string?</code> против <code>string</code>. Включается <code>&lt;Nullable&gt;enable&lt;/Nullable&gt;</code> и убирает целый класс ошибок ещё до запуска.</td></tr>
</table></div>
`,
quiz:[
 {q:"Метод-помощник изменил поле у переданного объекта с тестовыми данными. Что увидит вызывающий код?",
  opts:["Оригинал не изменится — объект копируется","Изменение видно и в оригинале: передавалась ссылка","Будет ошибка компиляции","Зависит от модификатора доступа"],
  a:1, why:"Для ссылочных типов копируется ссылка, а не объект. Отсюда «протухшие» данные между тестами; лечится неизменяемыми моделями либо созданием свежего объекта на каждый тест."},
 {q:"Чем опасен вызов <code>SomeAsync().Result</code> в тесте?",
  opts:["Ничем, это синоним await","Блокирует поток, может привести к взаимной блокировке и занимает воркер NUnit","Не компилируется","Теряет возвращаемое значение"],
  a:1, why:"<code>await</code> освобождает поток на время ожидания, <code>.Result</code> — держит его занятым. При параллельном прогоне это прямо уменьшает пропускную способность."},
 {q:"Что произойдёт, если забыть <code>await</code> перед <code>Page.ClickAsync(...)</code>?",
  opts:["Ошибка компиляции","Код пойдёт дальше, не дождавшись нажатия — плавающее падение","Нажатие выполнится дважды","Playwright подставит await сам"],
  a:1, why:"Компилятор выдаёт лишь предупреждение CS4014. Включённый <code>TreatWarningsAsErrors</code> превращает его в ошибку сборки и снимает проблему целиком."},
 {q:"Когда выполняется запрос <code>list.Where(x =&gt; x.Ok)</code>?",
  opts:["Сразу при вызове Where","При переборе или вызове ToList/Count/First","После сборки мусора","В отдельном потоке"],
  a:1, why:"Отложенное выполнение. Если исходная коллекция изменилась между построением запроса и перебором, результат будет уже другим."},
 {q:"Чем <code>readonly</code> отличается от <code>const</code>?",
  opts:["Ничем","<code>const</code> вычисляется при компиляции и годится только для литералов; <code>readonly</code> — в конструкторе","<code>readonly</code> работает только со строками","<code>const</code> можно менять в наследнике"],
  a:1, why:"Поэтому <code>readonly</code> подходит для значений, зависящих от конфигурации, — например, базового адреса, приходящего из окружения."},
 {q:"Что не так с <code>catch (Exception) { }</code> внутри теста?",
  opts:["Ничего, тест станет стабильнее","Падение превращается в зелёный прогон — тест перестаёт что-либо проверять","Не компилируется без throw","Замедляет выполнение"],
  a:1, why:"Тест, который не может упасть, бесполезен. Ловить нужно конкретный ожидаемый тип и только там, где это часть проверки."}
],
iv:[
 {q:"Насколько хорошо вы знаете C#? Что писали сами?",
  probe:"Калибруют уровень и проверяют, не преувеличиваете ли вы. Дальше вопросы будут подстроены под ваш ответ.",
  a:"Отвечать стоит конкретно и без завышения. Например: «Пишу тестовый код на C# — страницы, фикстуры, помощники, работу с API через HttpClient. Уверенно владею коллекциями, LINQ, async/await, ООП. Не писал продакшен-сервисы и не работал с многопоточностью глубже параллельного запуска тестов». Такая формулировка задаёт честную рамку, и дальше вас спросят по ней, а не наугад. Полезно добавить, что именно вы делали руками: «переписал базовый класс страниц», «вынес конфигурацию из кода в окружение», «добавил генерацию тестовых данных».",
  more:["Что в вашем коде вам самому не нравится?","Читаете ли вы код продукта?"]},
 {q:"Напишите метод, который возвращает уникальный e-mail для теста.",
  probe:"Простая задача на клавиатуре. Смотрят не на синтаксис, а на то, подумаете ли вы о повторных прогонах и параллельности.",
  a:"<code>private static string UniqueEmail(string prefix = \"qa\") =&gt; $\"{prefix}+{Guid.NewGuid():N}@example.com\";</code> Дальше стоит проговорить решения: <code>Guid</code>, а не счётчик и не отметка времени — счётчик сломается при параллельном прогоне, отметка времени может совпасть у двух тестов, стартовавших в одну миллисекунду. Плюс-адресация (<code>qa+…@</code>) сохраняет доставляемость на реальный ящик, если письмо нужно потом прочитать. Домен <code>example.com</code> зарезервирован стандартом и никому не принадлежит — письма никуда не уйдут по ошибке. И оговорить: если система нормализует плюс-адреса, схема не сработает и нужен отдельный домен.",
  more:["А если нужно потом прочитать это письмо?","Что если система запрещает плюс в адресе?"]},
 {q:"В чём разница между Task и Thread?",
  probe:"Классический вопрос на понимание асинхронности. Ответ «Task — это обёртка над Thread» считается неверным.",
  a:"<code>Thread</code> — это поток операционной системы: дорогой ресурс, который вы создаёте и которым управляете сами. <code>Task</code> — это <em>обещание результата</em>, единица работы; она может выполниться в потоке из пула, а может не занять поток вовсе. Ключевой случай — операции ввода-вывода: пока запрос идёт по сети, никакой поток не ждёт, ожидание обслуживается механизмом завершения на уровне ОС. Именно поэтому <code>await</code> на сетевом вызове масштабируется, а тысяча потоков — нет. Для тестов вывод практический: <code>await</code> вместо <code>.Result</code> освобождает воркеров, и параллельный прогон реально ускоряется.",
  more:["Что такое пул потоков?","Когда всё же нужен свой Thread?"]}
]
},

/* ----------------------------------------------------- 14 */
{
n:14, id:"nunit", title:"NUnit", sub:"Жизненный цикл и утверждения",
lede:"Порядок вызова атрибутов, модель ограничений, параметризация и различия между NUnit 3 и 4 — в этом репозитории живут обе версии сразу.",
body:`
<h2>Порядок выполнения</h2>
<figure class="fig">
<svg viewBox="0 0 620 168" role="img" aria-label="Порядок вызова атрибутов NUnit">
 <g font-family="var(--fm)" font-size="10">
  ${[["OneTimeSetUp","один раз на фикстуру","var(--accent-soft)","var(--accent)"],
     ["SetUp","перед каждым тестом","var(--surface-3)","var(--line)"],
     ["Test","сам тест","var(--ok-soft)","var(--ok)"],
     ["TearDown","после каждого теста","var(--surface-3)","var(--line)"],
     ["OneTimeTearDown","один раз на фикстуру","var(--accent-soft)","var(--accent)"]].map((r,i)=>{
    const y = 10 + i*30;
    return '<rect x="0" y="'+y+'" width="200" height="24" rx="2" fill="'+r[2]+'" stroke="'+r[3]+'"/>'+
      '<text x="10" y="'+(y+16)+'" font-size="10" fill="var(--ink)">'+r[0]+'</text>'+
      '<text x="212" y="'+(y+16)+'" font-size="9" fill="var(--ink-3)">'+r[1]+'</text>'+
      (i<4?'<path d="M100 '+(y+24)+'v6" stroke="var(--line-2)"/>':'');
  }).join("")}
  <text x="0" y="164" font-size="9.5" fill="var(--ink-3)">В наследовании: SetUp базового класса выполняется ПЕРЕД SetUp наследника, TearDown — в обратном порядке.</text>
 </g>
</svg>
<figcaption>Порядок при наследовании спрашивают чаще самого списка атрибутов: базовый Setup идёт первым, базовый TearDown — последним.</figcaption>
</figure>

<h2>Параметризация</h2>
<div class="tw"><table>
<tr><th>Способ</th><th>Когда</th></tr>
<tr><td><code>[TestCase(1, "a")]</code></td><td>несколько наборов прямо в атрибуте; данные простые и их немного</td></tr>
<tr><td><code>[TestCaseSource(nameof(Data))]</code></td><td>наборов много или они вычисляются; источник — статическое свойство или метод</td></tr>
<tr><td><code>[Values(1,2,3)]</code> на параметре</td><td>комбинации значений; NUnit сам перемножит</td></tr>
<tr><td><code>[Random]</code>, <code>[Range]</code></td><td>диапазоны и случайные значения</td></tr>
</table></div>
<div class="note warn"><b class="hd">Измерено в этом репозитории</b><p>Атрибут <code>[TestCase]</code> не используется <b>ни разу</b>, <code>[TestCaseSource]</code> — <b>один</b> раз на 432 тестовых метода. Значит, наборы данных развёрнуты в отдельные методы вручную: отсюда файл на 1919 строк — <code>Portal/Tests/ContactForm/ContactFormPhoneNumberFieldTests.cs</code>. Это готовый ответ на вопрос «где у вас дублирование и как бы вы его убрали».</p></div>

<h2>Модель ограничений</h2>
<pre><code><span class="c">// классический стиль — в NUnit 4 вынесен в отдельный пакет</span>
<span class="k">Assert</span>.AreEqual(expected, actual);

<span class="c">// модель ограничений — рекомендуемая</span>
<span class="k">Assert</span>.That(actual, <span class="k">Is</span>.EqualTo(expected));
<span class="k">Assert</span>.That(list, <span class="k">Has</span>.Count.EqualTo(3).And.All.Not.Null);
<span class="k">Assert</span>.That(text, <span class="k">Does</span>.Contain(<span class="s">"Спасибо"</span>).IgnoreCase);
<span class="k">Assert</span>.That(<span class="k">async</span> () =&gt; <span class="k">await</span> Act(), <span class="k">Throws</span>.TypeOf&lt;TimeoutException&gt;());</code></pre>
<div class="note"><b class="hd">NUnit 3 против NUnit 4 — что будет при обновлении</b><p>Все три проекта решения стоят на NUnit <b>3.14.0</b>. В четвёртой версии классические утверждения (<code>Assert.AreEqual</code>, <code>Assert.IsTrue</code>) убраны из основного пакета в <code>NUnit.Framework.Legacy</code> под именем <code>ClassicAssert</code>. Это самая частая причина, по которой обновление NUnit «ломает всё».</p><p>Хорошая новость про этот репозиторий: он к обновлению готов. Модель ограничений используется повсеместно — <b>442</b> вызова <code>Assert.That</code> и <b>ни одного</b> классического утверждения. Знать это про свой проект — сильный ход: на вопрос «что будет при переходе на NUnit 4» вы отвечаете не догадкой, а фактом.</p></div>

<h2>Assert.Multiple</h2>
<p>Обычные утверждения прерывают тест на первом несовпадении. Когда нужно увидеть все расхождения сразу:</p>
<pre><code><span class="k">Assert</span>.Multiple(() =&gt;
{
    <span class="k">Assert</span>.That(order.Status, <span class="k">Is</span>.EqualTo(<span class="s">"Оплачен"</span>));
    <span class="k">Assert</span>.That(order.Total,  <span class="k">Is</span>.EqualTo(1000));
    <span class="k">Assert</span>.That(order.Items, <span class="k">Has</span>.Count.EqualTo(3));
});</code></pre>
<p>Отчёт покажет все три несовпадения, а не только первое. Для UI-проверок Playwright есть парная конструкция — <code>Expect()</code> с собственным ожиданием.</p>

<h2>Атрибуты организации</h2>
<div class="tw"><table>
<tr><th>Атрибут</th><th>Зачем</th><th>В репозитории</th></tr>
<tr><td><code>[Category("Smoke")]</code></td><td>фильтрация прогона: <code>--filter TestCategory=Smoke</code></td><td>15 использований</td></tr>
<tr><td><code>[Property("XrayTest","QAT-…")]</code></td><td>связь с системой управления тестами</td><td>используется массово</td></tr>
<tr><td><code>[Description("…")]</code></td><td>человеческое описание в отчёте</td><td>используется</td></tr>
<tr><td><code>[Explicit]</code></td><td>запускается только явно, в общий прогон не попадает</td><td>—</td></tr>
<tr><td><code>[Parallelizable]</code></td><td>разрешение на параллельный запуск</td><td><b>0</b> — прогон последовательный</td></tr>
</table></div>
<div class="note trap"><b class="hd">Один тест на четырнадцать случаев Xray</b><p>В <code>ContactFormPositiveTests.cs</code> есть тест с четырнадцатью атрибутами <code>[Property("XrayTest", …)]</code> подряд. Формально покрытие закрыто; практически — при падении неизвестно, какой из четырнадцати случаев сломался, а починка требует прохода по всему сценарию. Это отличный материал для вопроса «расскажите про компромисс в вашем проекте»: экономия на количестве тестов оплачена потерей диагностики.</p></div>
`,
quiz:[
 {q:"Класс-наследник и его база имеют по методу <code>[SetUp]</code>. В каком порядке они выполнятся?",
  opts:["Сначала наследника, потом базы","Сначала базы, потом наследника","Только наследника","Порядок не определён"],
  a:1, why:"SetUp идёт от базового класса к наследнику, TearDown — в обратном порядке. Логика та же, что у конструкторов и деструкторов."},
 {q:"Чем <code>[OneTimeSetUp]</code> отличается от <code>[SetUp]</code>?",
  opts:["Ничем","Выполняется один раз на всю фикстуру, а не перед каждым тестом","Работает только с параметризованными тестами","Выполняется после тестов"],
  a:1, why:"Туда кладут дорогую подготовку — вход в систему, создание общих данных. Плата за это — связанность тестов: испорченное в одном тесте общее состояние отравит остальные."},
 {q:"Обновили NUnit с 3 на 4, и перестал компилироваться <code>Assert.AreEqual</code>. Почему?",
  opts:["Метод переименован","Классические утверждения вынесены в NUnit.Framework.Legacy как ClassicAssert","Нужен новый адаптер","Это ошибка в NUnit"],
  a:1, why:"Основной путь в NUnit 4 — модель ограничений <code>Assert.That</code>. Этот репозиторий на 3.14, но к переходу готов: 442 вызова <code>Assert.That</code> и ни одного классического утверждения."},
 {q:"Зачем нужен <code>Assert.Multiple</code>?",
  opts:["Ускоряет выполнение","Выполняет все утверждения и показывает все расхождения, а не только первое","Позволяет утверждать в цикле","Заменяет TestCase"],
  a:1, why:"Обычное утверждение прерывает тест на первом несовпадении. Когда проверки логически связаны, полезнее увидеть сразу все."},
 {q:"Сколько наборов данных задаёт <code>[TestCaseSource]</code>?",
  opts:["Ровно один","Столько, сколько вернёт источник — статическое свойство или метод","Не больше десяти","Столько же, сколько параметров"],
  a:1, why:"Это способ параметризовать тест данными, которые вычисляются или читаются извне. В этом репозитории он использован один раз — отсюда файлы на тысячи строк."},
 {q:"Что нужно, чтобы NUnit действительно запускал тесты параллельно?",
  opts:["Достаточно NumberOfTestWorkers в runsettings","Нужен атрибут <code>[Parallelizable]</code> — без него фикстуры идут последовательно","Параллельность включена всегда","Нужен флаг dotnet test"],
  a:1, why:"Число воркеров задаёт потолок, а разрешение даёт атрибут. В этом репозитории <code>[Parallelizable]</code> не встречается ни разу, поэтому четыре воркера простаивают."}
],
iv:[
 {q:"Расскажите про жизненный цикл теста в NUnit.",
  probe:"Проверяют не заучивание списка, а понимание, куда что класть и чем платить за общее состояние.",
  a:"Порядок такой: <code>OneTimeSetUp</code> один раз на фикстуру, затем на каждый тест <code>SetUp</code> → тест → <code>TearDown</code>, и в конце <code>OneTimeTearDown</code>. При наследовании базовый <code>SetUp</code> выполняется первым, базовый <code>TearDown</code> — последним. Практический смысл в выборе места: дорогое и общее — в <code>OneTimeSetUp</code>, всё, что должно быть свежим у каждого теста, — в <code>SetUp</code>. Компромисс тут прямой: чем больше вынесено в <code>OneTimeSetUp</code>, тем быстрее прогон и тем сильнее тесты связаны через общее состояние. Я стараюсь держать в нём только то, что тесты не изменяют.",
  more:["Куда положите вход в систему?","Что делать, если тест испортил общие данные?"]},
 {q:"Ваш прогон идёт полтора часа. Как ускорите?",
  probe:"Проверяют системность: начнёте ли с измерения или сразу предложите «добавить параллельность».",
  a:"Сначала измерил бы, из чего складывается время: отчёт NUnit даёт длительность каждого теста. Обычно картина одна и та же — небольшая часть тестов съедает большую часть времени. Дальше по порядку дешевизны. Первое: убрать жёсткие ожидания — в нашем репозитории 284 вызова <code>WaitForTimeoutAsync</code>, почти все в одном проекте; при среднем ожидании в две секунды это больше девяти минут чистого простоя. Второе: включить параллельность — сейчас <code>[Parallelizable]</code> нет ни одного, а воркеров заявлено четыре, то есть потенциал не используется вовсе. Третье: подготовку состояния перенести с интерфейса на API — вход, создание данных, переход в нужный раздел. Четвёртое: разделить прогон на дымовой в каждый коммит и полный по расписанию. Параллельность я ставлю не первой сознательно: она требует изоляции данных, и без неё вы получите не ускорение, а плавающие падения.",
  more:["Что мешает включить параллельность прямо сейчас?","Как поймёте, какие тесты самые долгие?"]},
 {q:"Как вы связываете автотесты с тест-кейсами в системе управления?",
  probe:"Проверяют опыт работы с трассируемостью, а не только с кодом.",
  a:"У нас связь идёт через атрибут: <code>[Property(\"XrayTest\", \"TC-1035\")]</code> на тестовом методе. При прогоне результат уезжает в Xray и проставляется нужным случаям. Плюс схемы в том, что связь живёт в коде рядом с тестом и не рассыпается при переименованиях. Минус — который я бы назвал сам — у нас есть тест с четырнадцатью такими атрибутами: он закрывает четырнадцать случаев сразу, и при падении непонятно, какой именно сломался. Правильнее дробить: один тест — один проверяемый факт, даже если сценарий приходится проходить несколько раз. Отдельно стоит следить за обратной стороной — случаями, у которых нет ни одного связанного теста: это и есть непокрытая часть.",
  more:["Как найдёте непокрытые случаи?","Что делать с тестом на 14 случаев?"]}
]
},

/* ----------------------------------------------------- 15 */
{
n:15, id:"locators", title:"Локаторы и ожидания", sub:"Playwright на практике",
lede:"Главный источник флака и главная тема разговора об автоматизации UI. Здесь же — цифры по этому репозиторию, которые стоит знать наизусть.",
body:`
<h2>Порядок предпочтения локаторов</h2>
<div class="tw"><table>
<tr><th>№</th><th>Способ</th><th>Почему так</th><th>В репозитории</th></tr>
<tr><td>1</td><td><code>GetByRole("button", new(){ Name = "Отправить" })</code></td><td>опирается на роль и доступное имя — то, что видит пользователь и экранная читалка</td><td>114</td></tr>
<tr><td>2</td><td><code>GetByLabel("E-mail")</code></td><td>привязка к подписи поля, устойчива к перевёрстке</td><td>0</td></tr>
<tr><td>3</td><td><code>GetByTestId("submit")</code></td><td>явный контракт с разработкой; не ломается никогда</td><td>0</td></tr>
<tr><td>4</td><td><code>GetByText("Спасибо")</code></td><td>для текстовых узлов; ломается при смене формулировки и локали</td><td>44</td></tr>
<tr><td>5</td><td>CSS: <code>Locator("#email")</code></td><td>годится по идентификатору, хрупок по цепочке классов</td><td>188</td></tr>
<tr><td>6</td><td>XPath: <code>Locator("//div[3]/form/input[1]")</code></td><td>последнее средство; привязан к структуре документа</td><td>178</td></tr>
</table></div>
<div class="note warn"><b class="hd">Что говорят эти цифры</b><p>Тестовых идентификаторов нет вовсе: <code>GetByTestId</code> — ноль. Значит, договорённости с разработкой о <code>data-testid</code> не было, и автоматизация цепляется за то, что есть. Это <b>не</b> недостаток вашей работы — это тема для предложения, и именно так стоит подать её на собеседовании: «вижу проблему, знаю решение, оно требует договорённости с командой разработки».</p></div>

<h2>Автоматические ожидания</h2>
<p>Ключевое отличие Playwright от Selenium: перед каждым действием он сам дожидается, что элемент существует, видим, стабилен (перестал двигаться), доступен для взаимодействия и не перекрыт. Отдельные ожидания в коде почти всегда не нужны.</p>
<div class="tw"><table>
<tr><th>Ждёт автоматически</th><th>Не ждёт</th></tr>
<tr><td><code>ClickAsync</code>, <code>FillAsync</code>, <code>CheckAsync</code>, <code>SelectOptionAsync</code></td><td><code>IsVisibleAsync</code>, <code>IsEnabledAsync</code>, <code>TextContentAsync</code></td></tr>
<tr><td><code>Expect(locator).ToBeVisibleAsync()</code> и прочие <code>Expect</code></td><td><code>CountAsync</code>, <code>AllTextContentsAsync</code></td></tr>
</table></div>
<div class="note trap"><b class="hd">Правило, снимающее большинство флака</b><p>Предикаты (<code>IsVisibleAsync</code>) возвращают состояние <em>сейчас</em>. Проверки (<code>Expect(...).ToBeVisibleAsync()</code>) <em>ждут</em> нужного состояния до таймаута. Поэтому <code>Assert.That(await page.IsVisibleAsync(s), Is.True)</code> — источник плавающих падений, а <code>await Expect(page.Locator(s)).ToBeVisibleAsync()</code> — нет.</p></div>

<h2>Жёсткие ожидания</h2>
<div class="note trap"><b class="hd">284 вызова <code>WaitForTimeoutAsync</code></b><p>278 в <code>Vertex2025</code> и 6 в <code>Portal</code> — проблема почти целиком сосредоточена в одном проекте. Каждый вызов — фиксированная пауза: слишком длинная в лучшем случае и слишком короткая в худшем. Документация Playwright прямо помечает этот метод как «не использовать в продакшен-коде».</p><p>Чем заменять по ситуации:</p>
<div class="tw" style="margin-top:.5em"><table>
<tr><th>Ждали чего</th><th>Чем заменить</th></tr>
<tr><td>появления элемента</td><td><code>await Expect(loc).ToBeVisibleAsync()</code></td></tr>
<tr><td>исчезновения загрузчика</td><td><code>await Expect(spinner).ToBeHiddenAsync()</code></td></tr>
<tr><td>смены адреса</td><td><code>await Page.WaitForURLAsync("**/thanks")</code></td></tr>
<tr><td>ответа сервера</td><td><code>await Page.WaitForResponseAsync(r =&gt; r.Url.Contains("/api"))</code></td></tr>
<tr><td>обновления текста</td><td><code>await Expect(loc).ToHaveTextAsync("Готово")</code></td></tr>
</table></div></div>

<h2>Строгий режим</h2>
<p>Если локатор находит больше одного элемента, Playwright не берёт первый молча, а падает с ошибкой <code>strict mode violation</code> и перечисляет найденное. Это <b>полезное</b> поведение: оно ловит неоднозначность на этапе разработки теста.</p>
<pre><code><span class="c">// упадёт, если кнопок «Далее» на странице две</span>
<span class="k">await</span> Page.GetByRole(<span class="s">"button"</span>, <span class="k">new</span>(){ Name = <span class="s">"Далее"</span> }).ClickAsync();

<span class="c">// осознанное сужение</span>
<span class="k">await</span> Page.GetByTestId(<span class="s">"contact-form"</span>)
          .GetByRole(<span class="s">"button"</span>, <span class="k">new</span>(){ Name = <span class="s">"Далее"</span> }).ClickAsync();</code></pre>
<p>В репозитории 60 обращений к <code>.Nth()</code>, <code>.First</code> и <code>.Last</code>. Каждое — место, где неоднозначность решена выбором по порядку. Иногда это оправданно, но чаще означает, что локатор стоит уточнить контекстом.</p>

<div data-widget="loc"></div>

<h2>Селениум в сравнении</h2>
<div class="tw"><table>
<tr><th></th><th>Selenium</th><th>Playwright</th></tr>
<tr><td>Ожидания</td><td>явные <code>WebDriverWait</code>, неявные <code>ImplicitWait</code></td><td>встроены в действия и проверки</td></tr>
<tr><td>StaleElementReference</td><td>частая проблема: элемент найден и устарел</td><td>отсутствует: локатор вычисляется заново при каждом действии</td></tr>
<tr><td>Сеть</td><td>нужен прокси</td><td>перехват и подмена из коробки</td></tr>
<tr><td>Диагностика</td><td>скриншоты, логи</td><td>трейс с записью действий, DOM и сетью</td></tr>
<tr><td>Экосистема</td><td>шире, стандарт W3C</td><td>моложе, свой протокол</td></tr>
</table></div>
<div class="note"><b class="hd">Про смешивание явных и неявных ожиданий в Selenium</b><p>Вопрос-ловушка, который любят задавать. Одновременное использование неявного и явного ожидания даёт непредсказуемое суммарное время: драйвер может ждать не 10 секунд, а 10 × количество опросов. Рекомендация официальной документации — не смешивать, а неявные не использовать вовсе.</p></div>
`,
quiz:[
 {q:"Почему <code>Assert.That(await page.IsVisibleAsync(s), Is.True)</code> — источник флака?",
  opts:["Медленно работает","<code>IsVisibleAsync</code> не ждёт: возвращает состояние на текущий момент","Не поддерживается в NUnit","Требует явного таймаута"],
  a:1, why:"Ожидание встроено в действия и в <code>Expect</code>, но не в предикаты. Правильная форма — <code>await Expect(locator).ToBeVisibleAsync()</code>."},
 {q:"Какой локатор предпочтительнее остальных?",
  opts:["XPath по структуре документа","<code>GetByRole</code> с доступным именем","CSS по цепочке классов","Поиск по индексу через Nth"],
  a:1, why:"Он опирается на то, что видит пользователь и экранная читалка, и переживает перевёрстку. XPath по структуре ломается от любой обёртки."},
 {q:"Локатор нашёл два элемента. Что сделает Playwright?",
  opts:["Возьмёт первый","Упадёт с strict mode violation и перечислит найденное","Возьмёт последний","Подождёт, пока останется один"],
  a:1, why:"Это полезное поведение: неоднозначность ловится при разработке теста, а не превращается в плавающее падение в CI."},
 {q:"Чем заменить <code>WaitForTimeoutAsync(3000)</code> перед проверкой успеха?",
  opts:["Увеличить до 5000","<code>await Expect(locator).ToBeVisibleAsync()</code>","Добавить [Retry]","Ничем, это нормально"],
  a:1, why:"Ожидание условия завершается сразу по его наступлению и терпит задержку до таймаута. Фиксированная пауза не умеет ни того, ни другого."},
 {q:"Почему в Playwright не бывает StaleElementReferenceException?",
  opts:["Ошибка называется иначе","Локатор — это описание поиска, он вычисляется заново при каждом действии","Playwright кеширует DOM","Она бывает, просто редко"],
  a:1, why:"В Selenium <code>WebElement</code> — ссылка на конкретный узел, и после перерисовки она устаревает. Локатор Playwright ссылки не хранит."},
 {q:"Что означает 0 использований <code>GetByTestId</code> при 178 XPath?",
  opts:["Тестовые идентификаторы не нужны при Playwright","С разработкой не договорились о <code>data-testid</code>, и тесты цепляются за вёрстку","XPath надёжнее","Так задумано фреймворком"],
  a:1, why:"Это не недостаток тестов, а отсутствующая договорённость. Именно так стоит подавать это на собеседовании: проблема названа, решение известно, оно требует работы с командой."}
],
iv:[
 {q:"Как вы выбираете локаторы?",
  probe:"Вопрос-фильтр. Ответ «по id, если есть» — минимальный проходной; ждут иерархию и обоснование.",
  a:"Иду по убыванию устойчивости. Сначала роль и доступное имя — <code>GetByRole</code>: это то, что видит пользователь, и оно переживает перевёрстку. Затем подпись поля, затем тестовый идентификатор, если команда его проставляет. Дальше CSS по идентификатору. XPath — последнее средство, и почти никогда по структуре документа: <code>//div[3]/form/input[1]</code> сломается от добавления обёртки, не меняющей вид страницы. Отдельное правило: если локатор находит несколько элементов, я не беру <code>.First</code>, а сужаю контекстом — <code>GetByTestId(\"contact-form\").GetByRole(\"button\")</code>. В нашем проекте соотношение пока не в пользу правильного: 178 XPath против 114 <code>GetByRole</code> и ноль тестовых идентификаторов. Причём распределение показательное — в одном проекте 86 XPath и один <code>GetByRole</code>, в другом 92 и 113: подход менялся, но старое не переписывали. Это я и предлагаю менять постепенно, начиная с самых часто падающих мест.",
  more:["Как убедите разработчиков проставить data-testid?","Что делать с динамическими идентификаторами?"]},
 {q:"Тест падает раз в десять прогонов на одном и том же шаге. Ваши действия?",
  probe:"Главный практический вопрос автоматизатора. Смотрят, не предложите ли вы сразу ретрай.",
  a:"Ретрай — последнее, что я сделаю, и только осознанно. Сначала соберу факты: включу трейс Playwright, он записывает действия, снимки DOM, консоль и сеть — по нему видно, что было на экране в момент падения. Дальше проверю типовые причины по убыванию частоты. Первая — ожидание времени вместо условия: если рядом стоит <code>WaitForTimeoutAsync</code>, почти наверняка дело в нём. Вторая — предикат без ожидания, <code>IsVisibleAsync</code> в утверждении. Третья — неоднозначный локатор, где выбран <code>.First</code>, и порядок элементов иногда меняется. Четвёртая — данные: тест зависит от состояния, оставленного другим тестом или предыдущим прогоном. Пятая — настоящий плавающий дефект продукта: гонка, двойная отправка. Последнюю проверяю обязательно, потому что именно её ретрай спрячет навсегда.",
  more:["Что покажет трейс?","Когда ретрай всё-таки уместен?"]},
 {q:"Чем Playwright лучше Selenium? А чем хуже?",
  probe:"Ждут сбалансированного ответа. Односторонний восторг считается признаком узкого опыта.",
  a:"Лучше тремя вещами. Первая — встроенные ожидания: перед действием проверяется видимость, стабильность и доступность элемента, и целый класс флака исчезает. Вторая — отсутствие устаревших ссылок на элементы: локатор вычисляется заново при каждом обращении, поэтому <code>StaleElementReference</code> не существует как явление. Третья — диагностика: трейс с записью действий, DOM и сети отвечает на вопрос «что было на экране» без переигрывания. Хуже — экосистемой и зрелостью: Selenium это стандарт W3C с поддержкой в любом облачном сервисе, огромным количеством готовых решений и специалистов на рынке. Плюс Playwright управляет браузером по собственному протоколу, а не через WebDriver, и это иногда упирается в корпоративные ограничения. Если бы выбирал сегодня для нового проекта на вебе — взял бы Playwright; если бы приходил в проект с большой рабочей базой на Selenium — не переписывал бы ради переписывания.",
  more:["Что такое трейс и как вы им пользуетесь?","А если нужен Internet Explorer?"]}
]
},

/* ----------------------------------------------------- 16 */
{
n:16, id:"po", title:"Page Object и архитектура", sub:"Как устроен фреймворк",
lede:"Вопрос «расскажите об архитектуре вашего фреймворка» задают всем. Разберём слои на настоящих файлах этого репозитория — вместе с тем, что в них стоит починить.",
body:`
<h2>Слои</h2>
<figure class="fig">
<svg viewBox="0 0 620 186" role="img" aria-label="Слои тестового фреймворка">
 <g font-family="var(--fm)" font-size="10">
  ${[["Тесты","что проверяем · утверждения · данные случая","Tests/","var(--ok-soft)","var(--ok)"],
     ["Страницы и компоненты","как взаимодействовать · локаторы приватны","Pages/","var(--accent-soft)","var(--accent)"],
     ["Помощники и модели","данные, API, генерация, утилиты","Helpers/ Models/","var(--surface-3)","var(--line)"],
     ["Конфигурация и фикстуры","адреса, учётные данные, браузер","Config/ BaseTest","var(--surface-3)","var(--line)"]].map((r,i)=>{
    const y = 8 + i*42;
    return '<rect x="0" y="'+y+'" width="620" height="34" rx="2" fill="'+r[3]+'" stroke="'+r[4]+'"/>'+
      '<text x="12" y="'+(y+15)+'" font-size="10.5" fill="var(--ink)">'+r[0]+'</text>'+
      '<text x="12" y="'+(y+28)+'" font-size="9" fill="var(--ink-3)">'+r[1]+'</text>'+
      '<text x="608" y="'+(y+21)+'" text-anchor="end" font-size="9" fill="var(--ink-3)">'+r[2]+'</text>';
  }).join("")}
  <text x="0" y="182" font-size="9.5" fill="var(--ink-3)">Правило одного направления: тест знает о странице, страница о тесте — никогда.</text>
 </g>
</svg>
<figcaption>Структура репозитория ровно такая: Tests/, Pages/, Helpers/, Models/, Config/. Вопрос не в наличии папок, а в том, что живёт внутри.</figcaption>
</figure>

<h2>Настоящий Page Object против обёртки над драйвером</h2>
<div data-file="basepage"></div>

<h2>Где должны быть утверждения</h2>
<div class="tw"><table>
<tr><th>Подход</th><th>Довод за</th><th>Довод против</th></tr>
<tr><td>Утверждения только в тестах</td><td>страницу можно переиспользовать в любом сценарии; видно, что именно проверяет тест</td><td>повторение однотипных проверок</td></tr>
<tr><td>Утверждения внутри страниц</td><td>короче тесты</td><td>страница перестаёт быть переиспользуемой: она уже решила, что считать правильным</td></tr>
<tr><td>Отдельный слой проверок</td><td>компромисс: <code>ContactFormAsserts.ShouldShowThanks()</code></td><td>ещё один слой для поддержки</td></tr>
</table></div>
<p>Общепринятый ответ — первый вариант: страница <b>делает</b>, тест <b>проверяет</b>. Исключение делают для технических ожиданий, без которых метод страницы просто не отработает.</p>

<h2>Фикстура: откуда берётся браузер</h2>
<div data-file="basetest"></div>

<h2>Что спросят про архитектуру</h2>
<div class="tw"><table>
<tr><th>Вопрос</th><th>Что показывает ответ</th></tr>
<tr><td>Как страница получает <code>IPage</code>?</td><td>понимаете ли вы жизненный цикл и внедрение зависимостей</td></tr>
<tr><td>Где хранятся локаторы?</td><td>приватные поля страницы — правильно; параметры методов — обёртка</td></tr>
<tr><td>Как переиспользуете шапку и подвал?</td><td>компоненты как отдельные классы, а не копирование</td></tr>
<tr><td>Как страница возвращает следующую страницу?</td><td>метод возвращает объект новой страницы — так строится текучий стиль</td></tr>
<tr><td>Что если элемента может не быть?</td><td>метод возвращает <code>bool</code> или <code>Task&lt;bool&gt;</code>, а решение принимает тест</td></tr>
</table></div>

<h2>SOLID на тестовом коде</h2>
<div class="tw"><table>
<tr><th>Принцип</th><th>Как выглядит в автотестах</th></tr>
<tr><td><b>S</b> — единственная ответственность</td><td>страница отвечает за взаимодействие, помощник за данные, тест за проверку. Класс на 419 строк, как <code>ContactFormPage</code>, — сигнал, что ответственностей несколько</td></tr>
<tr><td><b>O</b> — открытость и закрытость</td><td>новый сценарий добавляется новым методом, а не правкой существующих</td></tr>
<tr><td><b>L</b> — подстановка Лисков</td><td>наследник страницы не должен требовать больше и обещать меньше базовой</td></tr>
<tr><td><b>I</b> — разделение интерфейсов</td><td>не заставлять страницу реализовывать методы, которые ей не нужны</td></tr>
<tr><td><b>D</b> — инверсия зависимостей</td><td>тест зависит от <code>IPage</code>, а не от конкретной реализации браузера</td></tr>
</table></div>
<div class="note ok"><b class="hd">Как отвечать про SOLID, чтобы это не звучало как заучивание</b><p>Назовите принцип, затем сразу пример <b>из своего проекта</b> — лучше отрицательный. «Единственная ответственность: у нас <code>BasePage</code> одновременно и хранит страницу, и предоставляет обёртки над кликами — это две разные роли, и вторая привела к тому, что селекторы разъехались по тестам». Такой ответ невозможно выучить, и он сразу показывает, что вы читали собственный код.</p></div>
`,
quiz:[
 {q:"Метод <code>ClickAsync(string selector)</code> в базовой странице — это признак чего?",
  opts:["Хорошей абстракции","Обёртки над драйвером: селектор приходит снаружи, знание о вёрстке остаётся в тестах","Необходимой оптимизации","Правильного применения SOLID"],
  a:1, why:"Настоящий Page Object предлагает методы на языке задачи и держит селекторы приватными. Иначе перевёрстка правится в тестах, а не на странице."},
 {q:"Где должны находиться утверждения в классической схеме Page Object?",
  opts:["Внутри методов страницы","В тестах: страница делает, тест проверяет","В базовом классе","В отдельном потоке"],
  a:1, why:"Страница с утверждениями внутри перестаёт быть переиспользуемой — она уже решила, что считать правильным поведением."},
 {q:"Почему настройки контекста задают в <code>ContextOptions()</code>, а не в <code>[SetUp]</code>?",
  opts:["Так короче","К моменту SetUp контекст уже создан — размер окна, локаль и учётные данные задать уже нельзя","SetUp не поддерживает async","Это одно и то же"],
  a:1, why:"Playwright.NUnit вызывает <code>ContextOptions()</code> до создания контекста. Это точка расширения именно для настроек уровня контекста."},
 {q:"Чем платят за наследование <code>BaseTest : PageTest</code>?",
  opts:["Ничем","Единственная возможность наследования потрачена: другую иерархию придётся строить композицией","Потерей доступа к Page","Замедлением прогона"],
  a:1, why:"В C# одиночное наследование классов. Взамен даром достаются <code>Page</code>, <code>Context</code>, <code>Expect()</code> и управление жизненным циклом браузера."},
 {q:"Класс страницы разросся до 419 строк. Какой принцип нарушен вероятнее всего?",
  opts:["Открытости и закрытости","Единственной ответственности","Подстановки Лисков","Инверсии зависимостей"],
  a:1, why:"Обычно это значит, что в одном классе живут несколько зон страницы. Их выделяют в компоненты: форма, шапка, модальное окно."},
 {q:"Как страница должна сообщать тесту о переходе на следующую страницу?",
  opts:["Через глобальную переменную","Возвращая объект новой страницы из метода","Через событие","Никак, тест сам создаёт"],
  a:1, why:"Возврат следующей страницы делает сценарий читаемым и позволяет строить текучие цепочки вызовов."}
],
iv:[
 {q:"Расскажите об архитектуре вашего фреймворка.",
  probe:"Самый вероятный вопрос всего интервью. Слушают структуру ответа и то, признаёте ли вы слабые места.",
  a:"Начну со стека: C# и .NET 8, Playwright для браузера, NUnit как движок тестов, прогон через <code>dotnet test</code> в Azure Pipelines, отчёты в ReportPortal, связь с Xray через атрибуты. Структура четырёхслойная: <code>Tests/</code> — сценарии и утверждения, <code>Pages/</code> — взаимодействие со страницами и компонентами, <code>Helpers/</code> и <code>Models/</code> — данные и утилиты, <code>Config/</code> — адреса стендов и учётные данные из окружения. Браузер и страницу даёт базовый класс <code>PageTest</code> из <code>Microsoft.Playwright.NUnit</code>: свежий контекст на каждый тест, настройки — размер окна, базовый адрес, HTTP-аутентификация — задаются в переопределённом <code>ContextOptions()</code>. Дальше честно назову два слабых места: базовый класс страниц сейчас работает скорее обёрткой над драйвером — селекторы приходят снаружи, поэтому знание о вёрстке живёт в тестах; и параллельного прогона нет, потому что нет изоляции данных. Оба знаю, как чинить, и это то, чем я хотел бы заняться.",
  more:["С чего начнёте переделку?","Как убедите команду выделить на это время?"]},
 {q:"Что такое Page Object и зачем он нужен?",
  probe:"Определение знают все. Отличает то, назовёте ли вы цену и границы применимости.",
  a:"Page Object — это класс, описывающий страницу или её часть: он хранит локаторы в приватных полях и предлагает методы на языке задачи — <code>SubmitContactFormAsync(data)</code>, а не <code>Click(\"#btn\")</code>. Цель одна: локализовать изменения. Перевёрстали форму — правится один класс, а не сорок тестов. Из этого следуют и правила: локаторы наружу не торчат, утверждения остаются в тестах, метод перехода возвращает следующую страницу. Про цену тоже стоит сказать: на маленьком наборе тестов Page Object — лишний слой, он окупается начиная примерно с десятков тестов на одну страницу. И про границу: для страниц с повторяющимися блоками лучше не один большой класс, а компоненты — шапка, форма, модальное окно, — иначе класс разрастается и снова начинает нарушать единственную ответственность.",
  more:["Чем компонент отличается от страницы?","Когда Page Object не нужен?"]},
 {q:"Вам дали проект с нуля. Что сделаете в первую неделю?",
  probe:"Проверяют, начнёте ли вы с кода или с понимания задачи и рисков.",
  a:"Код — не первое. Сначала выясню, что вообще нужно: какие сценарии критичны для бизнеса, где чаще всего были инциденты, есть ли уже ручные проверки, которые стоит перенести. Параллельно посмотрю на продукт с точки зрения тестируемости: есть ли стабильные идентификаторы в вёрстке, есть ли API, через который можно готовить состояние вместо кликов, как устроены тестовые данные и можно ли их создавать и удалять. Это определит, сколько усилий уйдёт на борьбу с окружением. Дальше соберу минимальный каркас — конфигурация из окружения, базовая фикстура, одна страница, один сквозной тест — и прогоню его в CI с первого дня: тест, не запускающийся в пайплайне, не считается написанным. И только потом начну наращивать покрытие, от самых дорогих по последствиям сценариев. К концу недели хочу иметь работающий в CI дымовой тест и список договорённостей с разработкой — прежде всего про тестовые идентификаторы.",
  more:["Что если API для подготовки данных нет?","Как выберете первый сценарий?"]}
]
}

);
