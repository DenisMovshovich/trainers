# Тренажёры

Четырнадцать самодостаточных интерактивных материалов по инженерным темам.
Каждый — **один HTML-файл**: вся теория, все проверки и работающий эмулятор
внутри. Ни сборки, ни зависимостей, ни сервера — открывается двойным щелчком.

## Что здесь

| Материал | О чём | Открыть |
|---|---|---|
| [docker](docker/) | Docker: образы, слои, тома, сети, Dockerfile | `docker-offline.html` |
| [rest](rest/) | REST и HTTP: ресурсы, коды, идемпотентность | `rest-offline.html` |
| [bash](bash/) | Bash: конвейеры, перенаправления, sed, grep | `bash-offline.html` |
| [sql](sql/) | SQL на диалекте PostgreSQL | `sql-offline.html` |
| [kafka](kafka/) | Kafka: партиции, ISR, группы потребителей | `kafka-offline.html` |
| [network](network/) | Сеть и отладка HTTP: имена, соединения, сертификаты, CORS | `network-offline.html` |
| [ubuntu](ubuntu/) | Ubuntu Server: права, службы, пакеты, диски, сеть, доступ | `ubuntu-offline.html` |
| [testing](testing/) | QA basics: основы тестирования и тест-дизайн | `testing-offline.html` |
| [git](git/) | Git: индекс, ветки, слияния, rebase, reflog, сервер | `git-offline.html` |
| [k8s](k8s/) | Kubernetes: поды, контроллеры, сервисы, выкатка | `k8s-offline.html` |
| [cicd](cicd/) | CI/CD: задачи, матрицы, кеш, артефакты, выкладка | `cicd-offline.html` |
| [csharp](csharp/) | C#: типы, LINQ, ООП, исключения, async/await | `csharp-offline.html` |
| [dotnet](dotnet/) | .NET-проекты + собеседование Automation QA | `dotnet-offline.html` |
| [anatomy](anatomy/) | C# + Playwright: разбор набора UI-автотестов | `anatomy-offline.html` |

## Сайт
Все четырнадцать доступны по отдельным адресам как статический сайт:

```
/            титульная с карточками
/docker/  /rest/  /bash/  /sql/  /kafka/  /network/  /ubuntu/  /testing/
/git/  /k8s/  /cicd/  /csharp/  /dotnet/  /anatomy/
```

Каталог `docs/` — **результат сборки**, в репозитории он не хранится:
источник истины лежит в `<материал>/src/`. Так дубликаты не попадают
в историю, а сайт не может разойтись с содержимым исходников.

```sh
./tools/build-site.sh          # пересобрать материалы и сайт
./tools/build-site.sh --fast   # только сайт
cd docs && python3 -m http.server 8000   # посмотреть локально
```

Публикация на GitHub Pages идёт через Actions (`.github/workflows/pages.yml`):
при пуше в `main` сайт собирается из исходников и выкладывается.
В настройках репозитория нужно один раз выбрать **Settings → Pages → Source: GitHub Actions**.

## Что делает их не текстом

Внутри двенадцати из четырнадцати работает настоящий эмулятор, написанный на JavaScript:

* **Docker** — движок контейнеров: сборка образов, слои, кеш, тома, сети;
* **REST** — HTTP-сервер с маршрутизацией, кодами ответов и заголовками;
* **Bash** — интерпретатор оболочки: разбор, конвейеры, перенаправления, подстановки;
* **SQL** — движок PostgreSQL: соединения, оконные функции, транзакции, план запроса;
* **Kafka** — кластер из трёх брокеров: репликация, ISR, выборы лидера, смещения;
* **Тестирование** — калькуляторы техник тест-дизайна: границы, попарные наборы, аудит баг-репорта;
* **C#** — интерпретатор языка: классы и структуры, свойства, виртуальные методы, интерфейсы,
  LINQ с отложенным выполнением, исключения, `using`, `async`/`await` с планировщиком;
* **Git** — хранилище объектов с адресацией по содержимому, индекс, ветки, трёхстороннее
  слияние с настоящими конфликтами, интерактивное перебазирование, reflog, bisect
  и отдельный репозиторий на роли сервера;
* **Kubernetes** — кластер: разбор YAML, цикл согласования с контроллерами, планировщик
  с учётом ресурсов и ограничителей, kubelet с пробами, конечные точки Service по меткам
  и плавные обновления с откатом;
* **CI/CD** — конвейер: вычисление выражений с контекстами, граф задач с параллельностью
  и двумя временами, матрицы с fail-fast, кеш с ключами, артефакты, секреты
  и набор тестов с нестабильными случаями;
* **Ubuntu Server** — модель системы: файловая система с правами, особыми битами и владельцами,
  оболочка с конвейерами и перенаправлениями, учётные записи и sudo, systemd с unit-файлами
  и журналом, apt и dpkg с репозиторием и зависимостями, процессы и сигналы, разделы и fstab,
  netplan, ufw и sshd с проверкой прав на ключи;
* **Network** — модель сети: разрешение имён, соединение с различием отказа и потери
  пакетов, проверка сертификатов, HTTP с перенаправлениями и условными запросами,
  прокси и отдельный браузер с правилами источника.

Задания проверяются по-настоящему: движок исполняет то, что вы написали,
и проверка смотрит на результат, а не на текст команды.

## Общий слой интерфейса

Все материалы получают один и тот же слой поверх собственной разметки —
`tools/ui/90_ui.js`. Он подключается последним и ничего не переписывает:
находит уже существующие узлы и достраивает то, чего им не хватало.

| Что добавляет | Зачем |
|---|---|
| Ссылка «к содержанию» | первая по Tab, уводит мимо шапки и навигации |
| Полная разметка вкладок | `aria-controls`, `role="tabpanel"`, стрелки, Home и End |
| Клавиши по разделам | `[` и `]` — соседние разделы, `1`…`4` — вкладки |
| Блок «предыдущий / следующий» | внизу раздела, с названиями соседей |
| Прогресс по разделам | цифрами «3/8» в вертикальном списке, чертой поверх пункта — в горизонтальной полосе |
| Подсказка о прокрутке | край полосы разделов растворяется, если дальше есть ещё |
| Поиск по разделам (`/`) | по заголовкам, теории, вопросам, заданиям и разборам |
| Справка по клавишам (`?`) | окно со всеми сочетаниями |
| Сброс прогресса в два шага | первое нажатие спрашивает, второе выполняет |
| Область живых сообщений | экранный диктор узнаёт про вывод консоли и смену состояния |
| Боковая панель на узком экране | ниже 1120 px открывается кнопкой «Панель» |
| Заголовок вкладки браузера | показывает текущий раздел |

Слой работает с тремя разными диалектами разметки, которые накопились
в материалах, и включает каждую возможность только там, где для неё есть узлы:
в `dotnet` и `anatomy` нет разделов — значит, нет и перехода по ним, а `/`
остаётся за их собственным поиском.

Проверяется отдельно и сразу по всем материалам:

```sh
./tools/build-site.sh
node tools/ui/check-ui.mjs
```

## Как открыть

Возьмите файл с суффиксом **`-offline`** — это полноценный документ.
Файл без суффикса рядом с ним — фрагмент без `<!doctype>` и `<head>`,
он существует только для встраивания и в браузере открывается неправильно.

Из сети подгружаются только шрифты Google Fonts. Без интернета всё работает,
просто подставляются системные шрифты. Прогресс хранится в `localStorage`
и не покидает устройство.

## Пересборка

У каждого материала есть `src/` с частями и скриптом сборки:

```sh
cd <материал>/src && ./assemble.sh   # либо ./build.sh
```

Скрипт собирает обе версии — фрагмент и автономный документ.

## Проверка

Там, где есть `verify.sh`, он прогоняет полный цикл: тесты движка,
эталонные решения заданий, негативный контроль, целостность содержания
и браузерную проверку в Chromium на нескольких размерах и в обеих темах.

```sh
npm i -D playwright-core && npx playwright install chromium
./tools/verify-all.sh              # все материалы подряд, сайт и интерфейс
cd <материал>/src && ./verify.sh   # один материал
node tools/ui/check-ui.mjs         # только общий слой интерфейса
```

Последним шагом `verify-all` прогоняет `tools/ui/check-ui.mjs` — одну проверку
сразу по всем собранным страницам: связность вкладок, клавиатура, поиск,
возврат фокуса, подтверждение сброса и отсутствие горизонтального перелива
на широком и узком экране.

Браузер и путь к нему не зашиты в скрипты: берутся из `node_modules`,
при необходимости переопределяются переменными `PLAYWRIGHT_CORE` и `CHROMIUM_PATH`.

## Данные

Материалы `anatomy` и `dotnet` построены на архитектуре
реального рабочего проекта, но полностью обезличены: пространства имён,
адреса стендов и идентификаторы задач заменены на вымышленные
(`Acme.WebAutomation.*`, `*.example.com`, `TC-####`). Никаких данных
заказчика в репозитории нет.

## Лицензия

MIT — см. [LICENSE](LICENSE).
