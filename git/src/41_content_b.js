
/* ------------------------------------------------ 2 */
{
n:2, id:"commit", title:"Коммит", sub:"Что в нём и как его поправить",
lede:"Коммит — не «сохранение файла», а снимок всего проекта плюс запись о том, откуда он взялся. Отсюда и правила: что в него класть, как называть и почему исправленный коммит — это другой коммит.",
theory:`
<h2>Что такое коммит на самом деле</h2>
<p>Это не разница с предыдущей версией, а <b>полный снимок</b> дерева файлов. Разницу git вычисляет на лету, когда вы просите <code>git diff</code> или <code>git show</code>. Плюс к снимку коммит хранит ссылку на родителя, автора, дату и сообщение.</p>
<pre><code>$ <span class="k">git cat-file -p HEAD</span>
tree 4f3a1c…        <span class="c"># снимок файлов</span>
parent 9b2e7d…      <span class="c"># откуда пришли</span>
author Автор &lt;dev@example.com&gt;

feat: валидация почты</code></pre>

<h2>Хороший коммит</h2>
<div class="tw"><table>
<tr><th>Признак</th><th>Почему это важно</th></tr>
<tr><td>Одно логическое изменение</td><td>его можно отменить целиком, перенести в другую ветку, объяснить одной фразой</td></tr>
<tr><td>Проект собирается и тесты проходят</td><td>иначе <code>git bisect</code> бесполезен, а откат ломает сборку</td></tr>
<tr><td>Сообщение объясняет <b>зачем</b></td><td>что изменилось, видно из диффа; зачем — не видно ниоткуда</td></tr>
</table></div>
<p>Формат сообщения обычно такой: короткая строка до ~50 символов в повелительном наклонении, пустая строка, затем подробности. Распространённая договорённость — префиксы вида <code>feat:</code>, <code>fix:</code>, <code>refactor:</code>, <code>test:</code>: по ним удобно фильтровать историю и собирать список изменений к релизу.</p>
<div class="note"><b class="hd">Чем плохо «исправления»</b><p>Сообщения «фикс», «правки», «update» превращают историю в шум. Через полгода вопрос «почему здесь появилась эта проверка» останется без ответа, а <code>git blame</code> приведёт к коммиту, который ничего не объясняет.</p></div>

<h2>Поправить последний коммит</h2>
<pre><code><span class="c"># забыли файл или опечатались в сообщении</span>
git add забытый.py
git commit --amend -m <span class="s">"feat: валидация почты"</span></code></pre>
<div class="note warn"><b class="hd"><code>--amend</code> не правит коммит — он создаёт новый</b><p>Хеш зависит от содержимого, поэтому изменённый коммит получает <b>другой</b> хеш, а старый остаётся в базе объектов бесхозным (его ещё некоторое время находит reflog). Практическое следствие: если исправляемый коммит уже отправлен на сервер, обычный <code>push</code> будет отклонён — история разошлась. Отсюда правило: <code>--amend</code> свободно применяют к тому, что ещё не ушло к другим.</p></div>

<h2><code>git commit -a</code></h2>
<p>Ключ <code>-a</code> автоматически добавляет в коммит изменённые <b>уже отслеживаемые</b> файлы, минуя <code>git add</code>. Новые файлы он не подхватывает — про них git ещё не знает. Это регулярный источник недоумения: «я сделал <code>commit -am</code>, а нового файла в коммите нет».</p>

<h2>.gitignore и его главная ловушка</h2>
<pre><code><span class="c"># .gitignore</span>
*.log
build/
.env</code></pre>
<div class="note trap"><b class="hd">Игнорирование не действует на уже отслеживаемые файлы</b><p>Если файл однажды попал в коммит, git будет следить за ним и дальше, сколько ни добавляй его в <code>.gitignore</code>. Нужно сначала снять его с учёта:</p>
<pre style="margin:.5em 0"><code>git rm --cached .env
git commit -m <span class="s">"chore: убрать .env из репозитория"</span></code></pre>
<p>И отдельно помните: файл исчезнет только из будущих коммитов. В истории он останется, и если это был секрет — его нужно считать скомпрометированным и менять, а не надеяться на переписывание истории.</p></div>

<h2>Что не кладут в репозиторий</h2>
<div class="tw"><table>
<tr><th>Что</th><th>Почему</th></tr>
<tr><td>Секреты, ключи, пароли</td><td>история публична для всех, у кого есть клон, и остаётся навсегда</td></tr>
<tr><td>Собранные артефакты, <code>bin/</code>, <code>obj/</code></td><td>воспроизводимы из исходников, раздувают репозиторий, дают конфликты на каждом слиянии</td></tr>
<tr><td>Зависимости (<code>node_modules</code>, пакеты)</td><td>их восстанавливает менеджер пакетов по файлу блокировки</td></tr>
<tr><td>Личные настройки IDE</td><td>у каждого свои; общие настройки — отдельный файл, который коммитят осознанно</td></tr>
</table></div>
`,
quiz:[
 {q:"Что физически хранит коммит?",
  opts:["Разницу с предыдущим коммитом","Полный снимок дерева файлов плюс ссылку на родителя","Только изменённые файлы","Список команд"],
  a:1, why:"Разницу git вычисляет на лету при <code>git diff</code>. Хранится снимок — поэтому переключение между коммитами быстрое."},
 {q:"Что делает <code>git commit --amend</code>?",
  opts:["Правит существующий коммит на месте","Создаёт новый коммит взамен последнего — с другим хешем","Отменяет коммит","Добавляет коммит поверх"],
  a:1, why:"Хеш зависит от содержимого. Поэтому <code>--amend</code> уже отправленного коммита приводит к отклонённому push."},
 {q:"<code>git commit -am</code> — новый файл в коммит попадёт?",
  opts:["Да","Нет: <code>-a</code> берёт только уже отслеживаемые файлы","Только с ключом -f","Зависит от .gitignore"],
  a:1, why:"Про новый файл git ещё не знает, для него нужен явный <code>git add</code>."},
 {q:"Файл уже в репозитории. Добавили его в <code>.gitignore</code> — что изменится?",
  opts:["Файл перестанет отслеживаться","Ничего: игнорирование не действует на уже отслеживаемые файлы","Файл удалится","Git выдаст ошибку"],
  a:1, why:"Нужно снять с учёта: <code>git rm --cached файл</code> и закоммитить это."},
 {q:"Секрет случайно попал в коммит и уехал на сервер. Что делать в первую очередь?",
  opts:["Переписать историю и забыть","Считать секрет скомпрометированным и заменить его","Удалить файл новым коммитом","Добавить в .gitignore"],
  a:1, why:"История уже у всех, кто склонировал. Переписывание — потом и для порядка, а сначала ротация ключа."},
 {q:"Что должно объяснять сообщение коммита?",
  opts:["Что изменилось","Зачем изменилось — «что» и так видно из диффа","Кто автор","Какие файлы затронуты"],
  a:1, why:"Через полгода именно «зачем» невозможно восстановить ниоткуда, а <code>git blame</code> приведёт к этому сообщению."}
],
labs:[
 {id:"2a", title:"Забытый файл",
  brief:"<p>Коммит <code>feat: экспорт отчёта</code> уже сделан, но в него не попал файл <code>export/csv.py</code>, без которого код не работает. Отдельный коммит «добавил забытое» заводить не нужно.</p><p>Допишите файл в последний коммит и заодно исправьте сообщение на <code>feat: экспорт отчёта в CSV</code>.</p>",
  hint:"git add нужный файл, затем git commit --amend -m \"новое сообщение\".",
  setup: R => {
    sc(R, "chore: заготовка", {"README.md": "# webapp"});
    sc(R, "feat: экспорт отчёта", {"report.py": "from export.csv import write"});
    R.work["export/csv.py"] = "def write(rows):\n    pass";
  },
  checks:[
   {label:"История не выросла — коммит поправлен, а не добавлен", test:st=>history(st.R, headId(st.R)).length === 2 && tipMsg(st) !== "feat: экспорт отчёта"},
   {label:"Файл теперь в последнем коммите", test:st=>files(st)["export/csv.py"] !== undefined},
   {label:"Сообщение исправлено", test:st=>tipMsg(st) === "feat: экспорт отчёта в CSV"},
   {label:"Использован <code>--amend</code>", test:st=>okRan(st,/--amend/)}
  ]},
 {id:"2b", title:"Убрать секрет из-под наблюдения",
  brief:"<p>Файл <code>.env</code> с настройками попал в репозиторий ещё в первом коммите. Его нужно перестать отслеживать, но <b>оставить на диске</b> — без него приложение не запустится.</p><ul><li>добавьте <code>.env</code> в <code>.gitignore</code>;</li><li>снимите файл с учёта;</li><li>закоммитьте это с сообщением <code>chore: перестать отслеживать .env</code>.</li></ul>",
  hint:"git rm --cached .env — снимает с учёта, не трогая файл на диске.",
  setup: R => {
    sc(R, "chore: init", {"app.py": "print('привет')", ".env": "TOKEN=secret123"});
  },
  checks:[
   {label:"<code>.env</code> больше не в коммите", test:st=>files(st)[".env"] === undefined},
   {label:"Файл остался на диске", test:st=>work(st)[".env"] === "TOKEN=secret123" && files(st)[".env"] === undefined},
   {label:"Он попал в <code>.gitignore</code>", test:st=>/(^|\n)\.env\s*($|\n)/.test(work(st)[".gitignore"] || "")},
   {label:"Статус чистый — файл не мозолит глаза", test:st=>{ const s = statusSets(st.R); return files(st)[".gitignore"] !== undefined && !s.staged.length && !s.untracked.length; }}
  ]}
],
iv:[
 {q:"Что делает git commit --amend и когда его нельзя применять?",
  probe:"Проверяют понимание неизменяемости коммитов, а не знание ключа.",
  a:"Он не правит последний коммит, а создаёт вместо него новый: берёт текущий индекс, прежнее или новое сообщение и того же родителя. Поскольку хеш вычисляется из содержимого, включая родителя и сообщение, новый коммит получает другой идентификатор, а старый остаётся в базе объектов бесхозным — какое-то время его находит reflog. Практическое ограничение отсюда прямое: если исправляемый коммит уже отправлен на сервер и его кто-то мог забрать, обычный push будет отклонён, а форс перепишет чужую отправную точку. Поэтому <code>--amend</code> я применяю свободно к тому, что ещё лежит локально, и очень осторожно — к отправленному, только в своей личной ветке и через <code>--force-with-lease</code>.",
  more:["Что будет со старым коммитом?","Как исправить не последний коммит?"]},
 {q:"В репозиторий попал файл с паролем. Ваши действия?",
  probe:"Смотрят на порядок действий. Начать с переписывания истории — типичная ошибка.",
  a:"Первым делом — считать этот пароль скомпрометированным и заменить его. Всё, что попало в историю, уже есть у каждого, кто клонировал репозиторий, лежит в кеше сервера, могло попасть в резервные копии и в логи CI; переписывание истории этого не отменяет. Дальше — перестать отслеживать файл: <code>git rm --cached</code>, добавить в <code>.gitignore</code>, закоммитить. И только потом, отдельной задачей и по согласованию с командой, чистить историю через <code>git filter-repo</code> или BFG, потому что это перепишет все коммиты после затронутого и всем придётся заново синхронизироваться. Правильный же вывод — организационный: секреты не должны иметь возможности попасть в репозиторий, для этого есть переменные окружения, хранилища секретов и проверки на стороне сервера.",
  more:["Чем filter-repo лучше filter-branch?","Как не допустить этого в будущем?"]}
]
},

/* ------------------------------------------------ 3 */
{
n:3, id:"objects", title:"Объекты", sub:"Из чего сделан репозиторий",
lede:"Внутри git всего четыре типа объектов и одна идея — адресация по содержимому. Понимание этой идеи разом объясняет и скорость веток, и неизменяемость истории, и то, почему rebase меняет хеши.",
theory:`
<h2>Четыре типа объектов</h2>
<div class="tw"><table>
<tr><th>Объект</th><th>Что хранит</th></tr>
<tr><td><b>blob</b></td><td>содержимое файла — и всё: ни имени, ни прав, ни даты</td></tr>
<tr><td><b>tree</b></td><td>каталог: список имён со ссылками на blob'ы и другие tree</td></tr>
<tr><td><b>commit</b></td><td>ссылку на корневой tree, родителей, автора, дату, сообщение</td></tr>
<tr><td><b>tag</b></td><td>аннотированную метку: объект, имя, автора, подпись</td></tr>
</table></div>

<h2>Адресация по содержимому</h2>
<p>Имя объекта — это хеш его содержимого. Отсюда всё остальное:</p>
<div class="tw"><table>
<tr><th>Следствие</th><th>Что это даёт</th></tr>
<tr><td>Одинаковое содержимое — один объект</td><td>файл, не менявшийся сто коммитов, хранится один раз</td></tr>
<tr><td>Изменение содержимого меняет имя</td><td>историю нельзя незаметно подправить: изменится хеш, а за ним и хеши всех потомков</td></tr>
<tr><td>Хеш коммита включает родителя</td><td>цепочка скрепляется: переписали один коммит — переписались все следующие</td></tr>
</table></div>
<div class="note ok"><b class="hd">Проверьте в терминале</b><p>Создайте два файла с одинаковым содержимым и посмотрите <code>git cat-file -p HEAD^{tree}</code> после коммита: у обоих будет один и тот же хеш blob'а. Имя файла живёт в дереве, а не в самом объекте.</p></div>

<h2>Ветка — это файл с одной строкой</h2>
<pre><code>$ <span class="k">cat .git/refs/heads/main</span>
7c4a2f9e1b8d3a5c6f0e2d4b8a1c3e5f7d9b0a2c</code></pre>
<p>Всё. Ветка — указатель на коммит, сорок символов на диске. Поэтому создание ветки мгновенно и ничего не копирует, а «тяжёлое ветвление» из старых систем контроля версий здесь просто не существует как понятие.</p>
<p><code>HEAD</code> — указатель на указатель: обычно там строка <code>ref: refs/heads/main</code>. Когда вместо имени ветки там оказывается хеш коммита, это и называется отсоединённым <code>HEAD</code>.</p>

<h2>Почему история неизменяема</h2>
<figure class="fig"><svg viewBox="0 0 640 116" role="img" aria-label="Цепочка коммитов">
<circle cx="70" cy="52" r="17" fill="none" stroke="currentColor"/><text x="70" y="57" text-anchor="middle" font-size="11" fill="currentColor">A</text>
<circle cx="190" cy="52" r="17" fill="none" stroke="currentColor"/><text x="190" y="57" text-anchor="middle" font-size="11" fill="currentColor">B</text>
<circle cx="310" cy="52" r="17" fill="none" stroke="currentColor"/><text x="310" y="57" text-anchor="middle" font-size="11" fill="currentColor">C</text>
<line x1="173" y1="52" x2="90" y2="52" stroke="currentColor" marker-end="url(#ah3)"/>
<line x1="293" y1="52" x2="210" y2="52" stroke="currentColor" marker-end="url(#ah3)"/>
<text x="131" y="42" text-anchor="middle" font-size="10" fill="currentColor">родитель</text>
<text x="251" y="42" text-anchor="middle" font-size="10" fill="currentColor">родитель</text>
<text x="440" y="40" font-size="11" fill="currentColor">поправили B — у него новый хеш,</text>
<text x="440" y="58" font-size="11" fill="currentColor">значит у C изменился родитель,</text>
<text x="440" y="76" font-size="11" fill="currentColor">значит и у C новый хеш</text>
<text x="70" y="92" text-anchor="middle" font-size="10" fill="currentColor">старший</text>
<text x="310" y="92" text-anchor="middle" font-size="10" fill="currentColor">младший</text>
<defs><marker id="ah3" markerWidth="7" markerHeight="7" refX="6" refY="3" orient="auto">
<path d="M0,0 L6,3 L0,6 z" fill="currentColor"/></marker></defs>
</svg><figcaption>Стрелки направлены в прошлое: коммит знает родителя, но не знает потомков. Именно поэтому git легко ходит по истории назад и не умеет — вперёд без обхода всех веток.</figcaption></figure>
<p>Отсюда главный практический вывод, к которому мы вернёмся в разделе о перебазировании: любая операция, «меняющая» коммит, на самом деле создаёт новую цепочку. Старая остаётся в базе, пока её кто-то держит.</p>

<h2>Метки</h2>
<div class="tw"><table>
<tr><th></th><th>Лёгкая</th><th>Аннотированная</th></tr>
<tr><td>Команда</td><td><code>git tag v1.0</code></td><td><code>git tag -a v1.0 -m "релиз"</code></td></tr>
<tr><td>Что это</td><td>просто указатель на коммит</td><td>отдельный объект: автор, дата, сообщение, подпись</td></tr>
<tr><td>Когда</td><td>личная закладка</td><td>релиз — то, что видят другие</td></tr>
</table></div>
<div class="note warn"><b class="hd">Метки не уезжают сами</b><p><code>git push</code> отправляет коммиты, но не метки. Нужен <code>git push --tags</code> или <code>git push origin v1.0</code>. «Тег есть локально, а в релизах пусто» — ровно этот случай.</p></div>
`,
quiz:[
 {q:"Что хранит объект blob?",
  opts:["Файл вместе с именем и правами","Только содержимое файла — имя живёт в дереве","Разницу между версиями","Ссылку на коммит"],
  a:1, why:"Поэтому два одинаковых по содержимому файла с разными именами — это один blob и две записи в дереве."},
 {q:"Почему нельзя незаметно подправить старый коммит?",
  opts:["Git запрещает запись","Хеш зависит от содержимого, а хеш потомка — от родителя: изменится вся цепочка","Коммиты подписаны","История на сервере"],
  a:1, why:"Правка одного коммита переписывает все следующие. Это и есть техническая основа доверия к истории."},
 {q:"Что такое ветка в git?",
  opts:["Копия всех файлов","Указатель на коммит — сорок символов в файле","Отдельная база объектов","Каталог с версией проекта"],
  a:1, why:"Поэтому создание ветки мгновенно и ничего не копирует. «Тяжёлого ветвления» здесь просто нет."},
 {q:"Что лежит в <code>HEAD</code> при обычной работе?",
  opts:["Хеш последнего коммита","Строка вида <code>ref: refs/heads/main</code> — ссылка на ветку","Список веток","Имя автора"],
  a:1, why:"Когда там оказывается хеш вместо ссылки на ветку, это и называется отсоединённым HEAD."},
 {q:"Куда направлены ссылки между коммитами?",
  opts:["От старых к новым","От нового к родителю — в прошлое","В обе стороны","Ссылок нет"],
  a:1, why:"Коммит знает родителя, но не знает потомков. Поэтому git легко идёт по истории назад."},
 {q:"<code>git push</code> отправит созданную метку <code>v1.0</code>?",
  opts:["Да, вместе с коммитами","Нет — нужен <code>--tags</code> или явное имя метки","Только аннотированную","Только лёгкую"],
  a:1, why:"Классическая причина «тег есть локально, а в релизах пусто»."}
],
labs:[
 {id:"3a", title:"Одинаковое содержимое — один объект",
  brief:"<p>Проверьте адресацию по содержимому на живом репозитории.</p><ul><li>создайте два файла с <b>одинаковым</b> содержимым: <code>a.txt</code> и <code>b.txt</code>, оба со строкой <code>одно и то же</code>;</li><li>создайте третий, <code>c.txt</code>, с другой строкой;</li><li>закоммитьте всё с сообщением <code>test: одинаковые файлы</code>;</li><li>посмотрите дерево коммита командой <code>git cat-file -p HEAD^{tree}</code>.</li></ul><p>У первых двух файлов должен оказаться один и тот же хеш.</p>",
  hint:"git cat-file -p HEAD^{tree} печатает записи дерева: права, тип, хеш и имя.",
  setup: R => { sc(R, "chore: init", {"README.md": "# webapp"}); },
  checks:[
   {label:"Три файла закоммичены", test:st=>files(st)["a.txt"] && files(st)["b.txt"] && files(st)["c.txt"]},
   {label:"У одинаковых файлов один blob", test:st=>{
     const f = files(st);
     return f["a.txt"] !== undefined && blobId(st.R, f["a.txt"]) === blobId(st.R, f["b.txt"]); }},
   {label:"У третьего — другой", test:st=>{
     const f = files(st);
     return f["c.txt"] !== undefined && blobId(st.R, f["a.txt"]) !== blobId(st.R, f["c.txt"]); }},
   {label:"Дерево просмотрено через <code>cat-file</code>", test:st=>okRan(st,/cat-file/)}
  ]},
 {id:"3b", title:"Метка на релиз",
  brief:"<p>Версия <code>1.0</code> — это <b>не</b> последний коммит, а тот, где появился экспорт (<code>feat: экспорт отчёта</code>). После него уже успели влить экспериментальную правку.</p><ul><li>поставьте <b>аннотированную</b> метку <code>v1.0</code> именно на тот коммит, с сообщением <code>релиз 1.0</code>;</li><li>убедитесь, что метка на месте: <code>git log --oneline</code> покажет её рядом с коммитом.</li></ul>",
  hint:"Найдите хеш через git log --oneline, затем git tag -a v1.0 <хеш> -m \"релиз 1.0\".",
  setup: R => {
    sc(R, "chore: init", {"README.md": "# webapp"});
    sc(R, "feat: экспорт отчёта", {"export.py": "def write(): pass"});
    sc(R, "experiment: попробовать другой формат", {"export.py": "def write(): return 'json'"});
  },
  checks:[
   {label:"Метка <code>v1.0</code> создана", test:st=>!!st.R.tags["v1.0"]},
   {label:"Она аннотированная", test:st=>st.R.tags["v1.0"] && st.R.tags["v1.0"].annotated},
   {label:"Стоит на нужном коммите", test:st=>st.R.tags["v1.0"] && obj(st.R, st.R.tags["v1.0"].id).msg === "feat: экспорт отчёта"},
   {label:"Не на последнем", test:st=>st.R.tags["v1.0"] && st.R.tags["v1.0"].id !== headId(st.R)}
  ]}
],
iv:[
 {q:"Что такое ветка в git?",
  probe:"Обманчиво простой вопрос. Ответ «это отдельная линия разработки» — описание, а не устройство.",
  a:"Технически ветка — это файл с одной строкой: именем коммита, на который она указывает. Всё. Никакого копирования файлов, никакой отдельной области — поэтому создание ветки мгновенно и ничего не стоит, а в системах вроде старого SVN ветвление было дорогой операцией и его избегали. Когда вы делаете коммит, git создаёт объект коммита и просто переписывает в этом файле сорок символов. <code>HEAD</code> — указатель на указатель: обычно в нём лежит ссылка на ветку, и если там оказывается прямой хеш коммита, вы в отсоединённом состоянии. Практический вывод: ветки в git дёшевы, поэтому нормально заводить их на каждую задачу и не бояться их количества.",
  more:["Что происходит при коммите с указателем?","Что такое detached HEAD?"]},
 {q:"Почему git считают надёжным хранилищем истории?",
  probe:"Проверяют понимание адресации по содержимому.",
  a:"Потому что имя каждого объекта — это хеш его содержимого, а хеш коммита включает хеш дерева файлов и хеши родителей. Получается цепочка, скреплённая криптографически: подменить старый коммит незаметно нельзя, потому что у него изменится имя, а значит изменится родитель следующего, а значит и его имя, и так до самой вершины. Любой, у кого есть клон, обнаружит расхождение. Из той же адресации следует ещё пара практичных вещей: одинаковое содержимое хранится один раз, поэтому репозиторий с длинной историей не растёт линейно, и целостность проверяется локально командой <code>git fsck</code>. Оговорка про SHA-1 и коллизии существует, но git давно применяет защиту от известной атаки и переходит на SHA-256.",
  more:["Что происходит при rebase с хешами?","Зачем нужен git fsck?"]}
]
},
