
/* ------------------------------------------------ 4 */
{
n:4, id:"branch", title:"Ветки", sub:"Указатели и HEAD",
lede:"Ветка — сорок символов на диске. Когда это перестаёт быть абстракцией, исчезает страх перед detached HEAD и «потерянными» коммитами.",
theory:`
<h2>Что происходит при коммите</h2>
<pre><code>до:     A ← B ← C   ← main ← HEAD
после:  A ← B ← C ← D   ← main ← HEAD</code></pre>
<p>Создаётся объект коммита, и указатель ветки, на которую смотрит <code>HEAD</code>, переезжает на него. Остальные ветки не двигаются — они по-прежнему показывают туда, где были.</p>

<h2>Переключение</h2>
<div class="tw"><table>
<tr><th>Задача</th><th>Современно</th><th>Старая форма</th></tr>
<tr><td>Перейти на ветку</td><td><code>git switch main</code></td><td><code>git checkout main</code></td></tr>
<tr><td>Создать и перейти</td><td><code>git switch -c feature</code></td><td><code>git checkout -b feature</code></td></tr>
<tr><td>Вернуть файл</td><td><code>git restore file</code></td><td><code>git checkout -- file</code></td></tr>
</table></div>
<p><code>switch</code> и <code>restore</code> появились именно потому, что <code>checkout</code> делал слишком разные вещи: переключал ветки и затирал файлы одной командой, различающейся парой символов. В работе встречается и то и другое — старую форму вы точно увидите в чужих инструкциях.</p>

<h2>Незакоммиченные изменения при переключении</h2>
<p>Git переносит их за вами, если файл в целевой ветке такой же. Если файл различается, переключение отклоняется:</p>
<pre><code>error: Your local changes to the following files would be
overwritten by checkout: forms.py</code></pre>
<p>Выбор: закоммитить, спрятать (<code>git stash</code>) или выбросить (<code>git restore</code>). Тихо затирать git не станет.</p>

<h2>Отсоединённый HEAD</h2>
<pre><code>$ <span class="k">git checkout HEAD~2</span>
You are in 'detached HEAD' state.</code></pre>
<p>Это не поломка, а нормальное состояние: <code>HEAD</code> показывает прямо на коммит, а не на ветку. Так удобно посмотреть старую версию или собрать проект на определённом коммите.</p>
<div class="note trap"><b class="hd">Опасно тут одно: коммитить и уйти</b><p>Коммиты, сделанные в отсоединённом состоянии, ни одной веткой не удерживаются. Переключились обратно — и на них не осталось ни одной ссылки. Они не исчезают немедленно, их находит <code>git reflog</code>, но по умолчанию через несколько недель сборщик мусора их удалит.</p><p>Лечение на месте: <code>git switch -c имя-ветки</code> — ветка создастся прямо здесь и удержит работу.</p></div>

<h2>Полезное про имена</h2>
<div class="tw"><table>
<tr><th>Команда</th><th>Что делает</th></tr>
<tr><td><code>git branch -v</code></td><td>ветки с последним коммитом и отслеживаемой веткой</td></tr>
<tr><td><code>git branch -d feature</code></td><td>удалить — <b>только если влита</b></td></tr>
<tr><td><code>git branch -D feature</code></td><td>удалить в любом случае</td></tr>
<tr><td><code>git switch -</code></td><td>вернуться на предыдущую ветку</td></tr>
<tr><td><code>git log --oneline --graph --all</code></td><td>увидеть форму истории целиком</td></tr>
</table></div>
<div class="note ok"><b class="hd">Отказ удалить — это подсказка, а не помеха</b><p><code>git branch -d</code> проверяет, влиты ли коммиты ветки куда-нибудь ещё. Отказ означает ровно одно: работа существует только здесь. Если это осознанно — <code>-D</code>. И даже после <code>-D</code> коммиты находятся через <code>git reflog</code>, пока не истёк срок хранения.</p></div>

<h2>Как обращаться к коммитам</h2>
<div class="tw"><table>
<tr><th>Запись</th><th>Что это</th></tr>
<tr><td><code>HEAD~1</code>, <code>HEAD~3</code></td><td>на столько шагов назад по первому родителю</td></tr>
<tr><td><code>HEAD^</code></td><td>то же, что <code>HEAD~1</code></td></tr>
<tr><td><code>HEAD^2</code></td><td><b>второй родитель</b> — имеет смысл только у коммита слияния</td></tr>
<tr><td><code>main@{1}</code></td><td>где ветка была одну запись reflog назад</td></tr>
<tr><td><code>a1b2c3d</code></td><td>сокращённый хеш — хватает первых семи символов</td></tr>
</table></div>
`,
quiz:[
 {q:"Что происходит с указателем ветки при коммите?",
  opts:["Ничего, он статичен","Ветка, на которую смотрит HEAD, переезжает на новый коммит","Все ветки сдвигаются","Создаётся новая ветка"],
  a:1, why:"Остальные ветки остаются на месте — они по-прежнему показывают туда, где были."},
 {q:"Что означает «отсоединённый HEAD»?",
  opts:["Репозиторий повреждён","HEAD указывает прямо на коммит, а не на ветку","Ветка удалена","Нет связи с сервером"],
  a:1, why:"Нормальное состояние для просмотра старой версии. Опасно только коммитить в нём и уйти, не создав ветку."},
 {q:"Сделали коммиты в отсоединённом HEAD и переключились на main. Что с ними?",
  opts:["Они удалены немедленно","На них нет ссылок; найти можно через reflog, но сборщик мусора со временем удалит","Они автоматически в main","Git не даст переключиться"],
  a:1, why:"Правильно было создать ветку прямо там: <code>git switch -c имя</code> удержит работу."},
 {q:"Почему <code>git branch -d</code> иногда отказывается удалять ветку?",
  opts:["Ветка занята","Её коммиты никуда не влиты — работа существует только здесь","Нет прав","Ветка на сервере"],
  a:1, why:"Это подсказка, а не помеха. Осознанно — <code>-D</code>, и даже тогда коммиты находит reflog."},
 {q:"Что означает <code>HEAD^2</code>?",
  opts:["Два коммита назад","Второй родитель — имеет смысл только у коммита слияния","Вторую ветку","Ошибку синтаксиса"],
  a:1, why:"Два коммита назад — это <code>HEAD~2</code>. Тильда идёт вглубь, крышка выбирает родителя."},
 {q:"Зачем ввели <code>git switch</code> и <code>git restore</code>?",
  opts:["Старые команды удалили","<code>checkout</code> делал слишком разное: переключал ветки и затирал файлы","Они быстрее","Для совместимости"],
  a:1, why:"Две операции с разными последствиями различались парой символов. Старую форму вы всё равно встретите в чужих инструкциях."}
],
labs:[
 {id:"4a", title:"Спасти работу из detached HEAD",
  brief:"<p>Вы ушли посмотреть старый коммит, увлеклись и сделали там работу. Сейчас <code>HEAD</code> отсоединён, а коммит <code>fix: поправить кодировку</code> не удерживается ни одной веткой.</p><p>Создайте прямо здесь ветку <code>hotfix</code>, чтобы работа не потерялась, и вернитесь на <code>main</code>.</p>",
  hint:"git switch -c hotfix создаст ветку в текущей позиции и переключит на неё.",
  setup: R => {
    sc(R, "chore: init", {"README.md": "# webapp"});
    sc(R, "feat: разбор ответа", {"parse.py": "def parse(s): return s"});
    const back = headId(R);
    sc(R, "feat: экспорт", {"export.py": "def write(): pass"});
    switchTo(R, short(back), {});
    R.index["parse.py"] = "def parse(s): return s.encode('utf-8')";
    R.work["parse.py"] = "def parse(s): return s.encode('utf-8')";
    R.now += 3600;
    const id = doCommit(R, "fix: поправить кодировку", [headId(R)]);
    moveHead(R, id, "commit: fix: поправить кодировку");
    checkoutTree(R, id);
  },
  checks:[
   {label:"Ветка <code>hotfix</code> существует", test:st=>hasBranch(st, "hotfix")},
   {label:"Она удерживает нужный коммит", test:st=>{
     const id = st.R.refs["refs/heads/hotfix"];
     return !!id && obj(st.R, id).msg === "fix: поправить кодировку"; }},
   {label:"HEAD снова присоединён", test:st=>!detached(st.R)},
   {label:"Вы вернулись на <code>main</code>", test:st=>branchName(st.R) === "main"}
  ]},
 {id:"4b", title:"Переключиться, не потеряв правку",
  brief:"<p>Вы правите <code>forms.py</code> в ветке <code>main</code>, и тут срочная задача в ветке <code>release</code>. Правка не закончена, коммитить её нельзя, а в <code>release</code> этот файл другой — git переключиться не даст.</p><ul><li>спрячьте незаконченную работу;</li><li>перейдите на <code>release</code>;</li><li>вернитесь на <code>main</code> и достаньте правку обратно.</li></ul>",
  hint:"git stash прячет, git stash pop возвращает.",
  setup: R => {
    sc(R, "chore: init", {"forms.py": "def submit(d): return d"});
    scBranch(R, "release");
    const rel = R.refs["refs/heads/release"];
    switchTo(R, "release", {});
    sc(R, "fix: релизная правка форм", {"forms.py": "def submit(d):\n    return dict(d)"});
    switchTo(R, "main", {});
    R.work["forms.py"] = "def submit(d):\n    # незакончено\n    return d";
  },
  checks:[
   {label:"Побывали в ветке <code>release</code>", test:st=>okRan(st,/(switch|checkout)\s+release/)},
   {label:"И вернулись на <code>main</code>", test:st=>branchName(st.R) === "main" && okRan(st,/(switch|checkout)\s+release/)},
   {label:"Правка пережила переключение", test:st=>/незакончено/.test(work(st)["forms.py"] || "") && okRan(st,/(switch|checkout)\s+release/)},
   {label:"И осталась незакоммиченной", test:st=>!/незакончено/.test(files(st)["forms.py"] || "") && okRan(st,/(switch|checkout)\s+release/)},
   {label:"Использован <code>git stash</code>", test:st=>okRan(st,/git\s+stash/)}
  ]}
],
iv:[
 {q:"Что такое detached HEAD и чем он опасен?",
  probe:"Частый вопрос: пугает новичков, и по ответу видно, разобрались ли вы в устройстве.",
  a:"Это состояние, когда <code>HEAD</code> указывает не на ветку, а прямо на коммит. Попадают в него, когда переключаются на хеш, на метку или на <code>HEAD~2</code>. Само по себе состояние нормальное и полезное: так смотрят старую версию, собирают проект на конкретном коммите, проверяют поведение до изменения. Опасность ровно одна: если сделать здесь коммиты и переключиться на ветку, на эти коммиты не останется ни одной ссылки. Они не пропадают сразу — их находит <code>git reflog</code>, и вернуть можно созданием ветки на нужном хеше, — но по умолчанию через несколько недель сборщик мусора их удалит. Поэтому, как только в отсоединённом состоянии захотелось закоммитить, я сразу делаю <code>git switch -c</code> с именем ветки.",
  more:["Как вернуть такие коммиты?","Когда detached HEAD полезен?"]}
]
},

/* ------------------------------------------------ 5 */
{
n:5, id:"merge", title:"Слияние", sub:"Fast-forward, база и конфликты",
lede:"Слияние выглядит страшным из-за конфликтов, а на деле git разрешает почти всё сам. Разобраться стоит в двух вещах: когда получается перемотка и что git считает базой.",
theory:`
<h2>Два исхода слияния</h2>
<div class="tw"><table>
<tr><th>Ситуация</th><th>Что делает git</th></tr>
<tr><td>Целевая ветка не двигалась с момента ответвления</td><td><b>fast-forward</b>: просто переставляет указатель вперёд, нового коммита нет</td></tr>
<tr><td>Обе ветки получили коммиты</td><td><b>коммит слияния</b> с двумя родителями</td></tr>
</table></div>
<pre><code><span class="c"># перемотка: main не двигался</span>
A ← B ← C(main) ← D ← E(feature)
после merge:  main указывает на E

<span class="c"># настоящее слияние: разошлись</span>
A ← B ← C ← F(main)
        ↖ D ← E(feature)
после merge:  A ← B ← C ← F ← M(main), где у M два родителя: F и E</code></pre>

<h2><code>--no-ff</code>: зачем отказываться от перемотки</h2>
<p>Перемотка не оставляет следа: по истории невозможно понять, что здесь была ветка. <code>git merge --no-ff</code> создаёт коммит слияния принудительно, и в истории остаётся видимая группа: «эти пять коммитов — одна задача». Многие команды делают так для веток задач, а некоторые, наоборот, требуют линейной истории. Это соглашение, а не техническое требование.</p>

<h2>База слияния</h2>
<p>Git сравнивает не две версии, а три: вашу, чужую и <b>общего предка</b>. Общий предок — это база слияния, находится командой <code>git merge-base main feature</code>. Именно поэтому git понимает, кто что менял: если строку менял только один, вопросов нет; если оба и по-разному — конфликт.</p>
<div class="note ok"><b class="hd">Отсюда важное следствие</b><p>Правки в разных файлах и даже в разных местах одного файла сливаются автоматически. Конфликт возникает только там, где обе стороны изменили <b>одни и те же строки</b> — или одна изменила, а другая удалила файл.</p></div>

<h2>Как выглядит конфликт</h2>
<pre><code><span class="k">&lt;&lt;&lt;&lt;&lt;&lt;&lt; HEAD</span>
timeout = 30
<span class="k">=======</span>
timeout = 60
<span class="k">&gt;&gt;&gt;&gt;&gt;&gt;&gt; feature</span></code></pre>
<p>Сверху — ваша версия (та ветка, где вы находитесь), снизу — приходящая. Разрешение конфликта — это обычное редактирование файла: убрать маркеры и оставить правильный текст. Он не обязан совпадать ни с одной из сторон — часто правильный ответ третий.</p>
<pre><code>git add forms.py     <span class="c"># «конфликт улажен» — это именно add</span>
git commit           <span class="c"># сообщение git подставит сам</span></code></pre>
<div class="note trap"><b class="hd">Маркеры, уехавшие в коммит</b><p><code>git add</code> не проверяет, что маркеров не осталось: для git это обычный текст. Забытые <code>&lt;&lt;&lt;&lt;&lt;&lt;&lt;</code> в коде — обычное дело на ревью. Помогают проверка перед коммитом и линтер в CI.</p></div>

<h2>Выходы, если пошло не так</h2>
<div class="tw"><table>
<tr><th>Команда</th><th>Что делает</th></tr>
<tr><td><code>git merge --abort</code></td><td>вернуть всё как было до слияния</td></tr>
<tr><td><code>git checkout --ours file</code></td><td>взять целиком свою версию файла</td></tr>
<tr><td><code>git checkout --theirs file</code></td><td>взять целиком приходящую</td></tr>
<tr><td><code>git merge --squash feature</code></td><td>принести изменения одним коммитом, без связи с веткой</td></tr>
</table></div>
<div class="note warn"><b class="hd">«Наши» и «их» меняются местами при rebase</b><p>При слиянии <code>ours</code> — ваша ветка. При перебазировании <code>ours</code> — это ветка, <b>на которую</b> вы перебазируетесь, а <code>theirs</code> — ваши собственные переносимые коммиты. Логично, если вспомнить, что rebase проигрывает ваши коммиты поверх чужих, но путает почти всех.</p></div>
`,
quiz:[
 {q:"Когда слияние проходит fast-forward?",
  opts:["Всегда, если нет конфликтов","Когда целевая ветка не получала коммитов с момента ответвления","Когда ветки в одном файле","Когда указан --no-ff"],
  a:1, why:"Git просто переставляет указатель вперёд. Нового коммита при этом не появляется."},
 {q:"Зачем сливать с <code>--no-ff</code>, если перемотка возможна?",
  opts:["Так безопаснее","Чтобы в истории остался след ветки — видимая группа коммитов одной задачи","Так быстрее","Иначе будет конфликт"],
  a:1, why:"Это соглашение команды, а не техническое требование. Другие команды, наоборот, требуют линейной истории."},
 {q:"Сколько версий сравнивает git при слиянии?",
  opts:["Две — вашу и чужую","Три: вашу, чужую и общего предка","Все коммиты обеих веток","Одну"],
  a:1, why:"Общий предок — база слияния. Благодаря ей git понимает, кто именно что менял."},
 {q:"В каком случае возникает конфликт?",
  opts:["Всегда при слиянии двух веток","Когда обе стороны изменили одни и те же строки","Когда правок много","Когда ветки давно разошлись"],
  a:1, why:"Правки в разных файлах и в разных местах одного файла сливаются сами."},
 {q:"Как сказать git, что конфликт улажен?",
  opts:["<code>git merge --continue</code>","<code>git add файл</code> — добавление и означает «улажено»","<code>git resolve</code>","Ничего не нужно"],
  a:1, why:"После этого <code>git commit</code> завершит слияние, сообщение git подставит сам."},
 {q:"При <code>git rebase</code> что означает <code>ours</code>?",
  opts:["Ваша ветка, как при merge","Ветка, на которую вы перебазируетесь — ваши коммиты становятся <code>theirs</code>","Общий предок","Последний коммит"],
  a:1, why:"Rebase проигрывает ваши коммиты поверх чужих, поэтому «наша» сторона — принимающая. Путает почти всех."}
],
labs:[
 {id:"5a", title:"След ветки в истории",
  brief:"<p>Ветка <code>feature/поиск</code> готова, <code>main</code> с момента ответвления не двигался — обычный <code>merge</code> сделал бы перемотку и не оставил следа.</p><p>Влейте ветку в <code>main</code> так, чтобы в истории <b>остался коммит слияния</b> с сообщением <code>Merge branch 'feature/поиск'</code>.</p>",
  hint:"git merge --no-ff feature/поиск — принудительный коммит слияния.",
  setup: R => {
    sc(R, "chore: init", {"app.py": "print('привет')"});
    scNew(R, "feature/поиск");
    sc(R, "feat: индекс для поиска", {"search.py": "INDEX = {}"});
    sc(R, "feat: запрос к индексу", {"search.py": "INDEX = {}\ndef find(q): return INDEX.get(q)"});
    scGo(R, "main");
  },
  checks:[
   {label:"Слияние выполнено в <code>main</code>", test:st=>branchName(st.R) === "main" && parentsOf(st).length === 2},
   {label:"Появился коммит слияния с двумя родителями", test:st=>parentsOf(st).length === 2},
   {label:"Работа ветки на месте", test:st=>/def find/.test(files(st)["search.py"] || "")},
   {label:"Использован <code>--no-ff</code>", test:st=>okRan(st,/--no-ff/)}
  ]},
 {id:"5b", title:"Разрешить конфликт",
  brief:"<p>В ветке <code>feature/таймауты</code> таймаут подняли до 60, а в <code>main</code> параллельно опустили до 15. Обе правки — в одной строке <code>config.py</code>.</p><ul><li>слейте <code>feature/таймауты</code> в <code>main</code>;</li><li>разрешите конфликт: правильное значение — <b>компромиссное <code>timeout = 30</code></b>, ни одна из сторон целиком не подходит;</li><li>завершите слияние.</li></ul><p>В файле не должно остаться маркеров конфликта.</p>",
  hint:'После merge посмотрите cat config.py, затем echo "timeout = 30" > config.py, git add config.py, git commit -m "..."',
  setup: R => {
    sc(R, "chore: init", {"config.py": "timeout = 30"});
    sc(R, "chore: конфигурация клиента", {"config.py": "timeout = 45"});
    scNew(R, "feature/таймауты");
    sc(R, "perf: поднять таймаут", {"config.py": "timeout = 60"});
    scGo(R, "main");
    sc(R, "fix: снизить таймаут", {"config.py": "timeout = 15"});
  },
  checks:[
   {label:"Слияние завершено коммитом", test:st=>parentsOf(st).length === 2},
   {label:"Значение компромиссное", test:st=>/timeout\s*=\s*30/.test(files(st)["config.py"] || "")},
   {label:"Маркеров конфликта не осталось", test:st=>parentsOf(st).length === 2 && !/[<>=]{7}/.test(files(st)["config.py"] || "")},
   {label:"Рабочая копия чистая", test:st=>clean(st) && parentsOf(st).length === 2}
  ]},
 {id:"5c", title:"Передумать посреди слияния",
  brief:"<p>Начали слияние <code>experiment</code> в <code>main</code>, получили конфликт и поняли, что вливать пока рано.</p><p>Отмените слияние целиком: <code>main</code> должен остаться там же, где был, а рабочая копия — вернуться к состоянию до попытки.</p>",
  hint:"git merge --abort возвращает всё как было.",
  setup: R => {
    sc(R, "chore: init", {"core.py": "VERSION = 1"});
    scNew(R, "experiment");
    sc(R, "experiment: переписать ядро", {"core.py": "VERSION = 99"});
    scGo(R, "main");
    sc(R, "fix: поднять версию", {"core.py": "VERSION = 2"});
    const r = runGit(R, "git merge experiment");
  },
  checks:[
   {label:"Слияние отменено", test:st=>!st.R.merging},
   {label:"Файл вернулся к версии <code>main</code>", test:st=>work(st)["core.py"] === "VERSION = 2"},
   {label:"Коммита слияния не появилось", test:st=>!st.R.merging && parentsOf(st).length === 1},
   {label:"Ветка <code>experiment</code> цела", test:st=>!st.R.merging && hasBranch(st, "experiment")}
  ]}
],
iv:[
 {q:"Чем fast-forward отличается от настоящего слияния?",
  probe:"Проверяют понимание формы истории, а не знание ключей.",
  a:"Fast-forward возможен, когда целевая ветка не получала коммитов с момента ответвления: вся работа ветки лежит прямо поверх её вершины. Тогда git не создаёт ничего нового, а просто переставляет указатель вперёд — история остаётся линейной, и по ней невозможно понять, что здесь была отдельная ветка. Настоящее слияние происходит, когда обе ветки ушли вперёд: git создаёт коммит с двумя родителями, содержащий результат трёхстороннего сравнения. Практический выбор между ними — это выбор формы истории. Мне ближе <code>--no-ff</code> для веток задач: в истории остаётся видимая группа коммитов, относящихся к одной задаче, и такую группу целиком легко откатить одним <code>git revert -m 1</code>. Но если команда договорилась о линейной истории, это тоже рабочее соглашение.",
  more:["Как откатить целую влитую ветку?","Что даёт --first-parent в логе?"]},
 {q:"Что происходит при конфликте слияния и как вы его разрешаете?",
  probe:"Практический вопрос. Ждут понимания, что add — это и есть «разрешено».",
  a:"Git сравнивает три версии файла: общего предка, мою и приходящую. Там, где строку менял кто-то один, он берёт изменение сам. Конфликт остаётся только на строках, изменённых обеими сторонами по-разному, — тогда git пишет в файл обе версии между маркерами и останавливается, а <code>git status</code> показывает файл в списке unmerged. Дальше это обычное редактирование: я открываю файл, смотрю обе стороны, а нередко и историю через <code>git log -p</code>, чтобы понять намерение каждой, и пишу правильный вариант — он не обязан совпадать ни с одной из сторон. Затем <code>git add</code> — это и означает «конфликт улажен», — и <code>git commit</code>. Если стало ясно, что сливать рано, есть <code>git merge --abort</code>, который возвращает всё как было.",
  more:["Что если правильный вариант — ни один из двух?","Как не оставить маркеры в коде?"]}
]
},
