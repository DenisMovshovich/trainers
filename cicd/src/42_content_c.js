
/* ------------------------------------------------ 5 */
{
n:5, id:"matrix", title:"Матрица", sub:"Один текст, много запусков",
lede:"Матрица размножает задачу по набору значений. Для автоматизатора это главный инструмент ускорения: тесты, которые шли двадцать минут, идут пять.",
theory:`
<h2>Как это выглядит</h2>
<pre><code><span class="k">jobs</span>:
  <span class="k">test</span>:
    <span class="k">runs-on</span>: ubuntu-latest
    <span class="k">strategy</span>:
      <span class="k">matrix</span>:
        <span class="k">shard</span>: [1, 2, 3, 4]
    <span class="k">steps</span>:
      - <span class="k">uses</span>: actions/checkout@v4
      - <span class="k">run</span>: dotnet test --shard \${{ matrix.shard }}/4</code></pre>
<p>Получается четыре независимых запуска задачи, каждый со своим значением <code>matrix.shard</code>. Они идут одновременно, поэтому время конвейера падает примерно вчетверо, а машинное время остаётся тем же.</p>
<div class="note ok"><b class="hd">Проверьте разницу</b><p>Запустите конвейер с шардированием и без, сравните две строки итога. Время конвейера изменится сильно, машинное — почти нет. Это и есть цена параллельности.</p></div>

<h2>Несколько измерений</h2>
<pre><code><span class="k">matrix</span>:
  <span class="k">os</span>: [ubuntu-latest, windows-latest]
  <span class="k">dotnet</span>: [<span class="s">"8.0"</span>, <span class="s">"9.0"</span>]</code></pre>
<p>Это уже четыре запуска — все сочетания. Матрица растёт умножением, и три измерения по три значения дают двадцать семь задач: следить за этим стоит, иначе конвейер съест всю квоту.</p>

<h2>Уточнения</h2>
<div class="tw"><table>
<tr><th>Ключ</th><th>Что делает</th></tr>
<tr><td><code>exclude</code></td><td>убрать конкретные сочетания</td></tr>
<tr><td><code>include</code></td><td>добавить сочетание или дописать поле к существующим</td></tr>
<tr><td><code>fail-fast</code></td><td>отменять ли остальные при первом падении — по умолчанию <b>да</b></td></tr>
<tr><td><code>max-parallel</code></td><td>сколько запускать одновременно</td></tr>
</table></div>
<pre><code><span class="k">strategy</span>:
  <span class="k">fail-fast</span>: false        <span class="c"># дать всем дойти до конца</span>
  <span class="k">matrix</span>:
    <span class="k">os</span>: [ubuntu-latest, windows-latest]
    <span class="k">dotnet</span>: [<span class="s">"8.0"</span>, <span class="s">"9.0"</span>]
    <span class="k">exclude</span>:
      - <span class="k">os</span>: windows-latest
        <span class="k">dotnet</span>: <span class="s">"8.0"</span></code></pre>

<h2><code>fail-fast</code>: когда включать, когда нет</h2>
<div class="tw"><table>
<tr><th></th><th><code>fail-fast: true</code> (по умолчанию)</th><th><code>fail-fast: false</code></th></tr>
<tr><td>Что делает</td><td>первое падение отменяет остальные</td><td>все доходят до конца</td></tr>
<tr><td>Хорошо</td><td>экономит машинное время</td><td>видно полную картину</td></tr>
<tr><td>Плохо</td><td>не видно, сломано ли только на одной платформе</td><td>дороже</td></tr>
</table></div>
<p>Для матрицы платформ обычно ставят <code>false</code>: важно знать, упало везде или только на Windows. Для шардирования одного набора тестов — оставляют <code>true</code>: там результат и так один.</p>

<h2>Шардирование тестов</h2>
<div class="note trap"><b class="hd">Делить нужно осмысленно</b><p>Простое деление «каждый N-й тест» работает, только если тесты примерно равны по длительности и не зависят друг от друга. Если один шард содержит все медленные тесты интерфейса, выигрыша не будет: время конвейера равно самому долгому шарду. Хорошие раннеры умеют делить по историческому времени выполнения — если такого нет, шарды разбивают вручную по группам.</p></div>
`,
quiz:[
 {q:"Что делает матрица?",
  opts:["Запускает задачу повторно при падении","Размножает задачу по набору значений — запуски идут одновременно","Ускоряет отдельный шаг","Группирует задачи"],
  a:1, why:"Время конвейера падает, машинное время остаётся тем же — это цена параллельности."},
 {q:"Матрица из трёх измерений по три значения — сколько задач?",
  opts:["3","27 — все сочетания","9","6"],
  a:1, why:"Матрица растёт умножением. Следить за этим стоит, иначе конвейер съест всю квоту."},
 {q:"Что делает <code>fail-fast: true</code> (по умолчанию)?",
  opts:["Останавливает шаг","Первое падение отменяет остальные варианты матрицы","Ускоряет запуск","Перезапускает упавшее"],
  a:1, why:"Экономит машинное время, но не даёт понять, сломано ли только на одной платформе."},
 {q:"Когда стоит поставить <code>fail-fast: false</code>?",
  opts:["Всегда","Для матрицы платформ — важно знать, упало везде или только на одной","Для шардов","Никогда"],
  a:1, why:"Для шардирования одного набора тестов результат и так один, там можно оставить умолчание."},
 {q:"Чем плохо простое деление тестов «каждый N-й»?",
  opts:["Ничем","Если один шард соберёт все медленные тесты, выигрыша не будет: время равно самому долгому шарду","Оно нестабильно","Оно требует прав"],
  a:1, why:"Хорошие раннеры делят по историческому времени; если такого нет — разбивают вручную по группам."},
 {q:"Что делает <code>exclude</code> в матрице?",
  opts:["Пропускает шаг","Убирает конкретные сочетания из перебора","Отключает задачу","Исключает файлы"],
  a:1, why:"Парный ему <code>include</code> добавляет сочетание или дописывает поле к существующим."}
],
labs:[
 {id:"5a", title:"Разбить тесты на шарды",
  brief:"<p>Тесты интерфейса идут долго одной задачей. Разбейте их на <b>три</b> шарда матрицей.</p><ul><li>добавьте задаче <code>strategy.matrix</code> с ключом <code>shard</code> и значениями 1, 2, 3;</li><li>передайте номер в команду: <code>dotnet test --filter ui --shard N/3</code>;</li><li>убедитесь, что время конвейера стало меньше машинного.</li></ul>",
  hint:"В команде подставьте \${{ matrix.shard }} вместо номера. Знаменатель — 3.",
  setup: () => newScenario({flakyFixed: true, files:{
    "src/App.cs": "class App { }",
    ".github/workflows/ci.yml":
`name: CI
on: [push]

jobs:
  ui:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: dotnet test --filter ui`}}),
  checks:[
   {label:"Матрица описана", test:st=>{
     const w = wf(st); return !!w && !!((w.jobs.ui || {}).strategy || {}).matrix; }},
   {label:"Получилось три запуска", test:st=>insts(st, "ui").length === 3},
   {label:"Каждый берёт свой шард", test:st=>{
     const w = wf(st); if(!w) return false;
     return [].concat((w.jobs.ui || {}).steps || []).some(s => /matrix\.shard/.test(String(s.run || ""))); }},
   {label:"Все три прошли", test:st=>insts(st, "ui").every(i => i.status === "success") && insts(st, "ui").length === 3},
   {label:"Время конвейера меньше машинного", test:st=>{ const r = last(st); return !!r && r.wall < r.total; }}
  ]},
 {id:"5b", title:"Увидеть полную картину",
  brief:"<p>Матрица по платформам: один из вариантов падает, и остальные отменяются по <code>fail-fast</code>. Из-за этого непонятно, сломано везде или только там.</p><ul><li>настройте стратегию так, чтобы все варианты доходили до конца;</li><li>запустите и убедитесь, что отменённых нет, а падает ровно один вариант.</li></ul>",
  hint:"strategy.fail-fast: false — на том же уровне, что и matrix.",
  setup: () => newScenario({files:{
    "src/App.cs": "class App { }",
    ".github/workflows/ci.yml":
`name: CI
on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        os: [linux, windows, macos]
    steps:
      - if: matrix.os == 'windows'
        run: exit 1
      - run: sleep 20`}}),
  checks:[
   {label:"<code>fail-fast</code> отключён", test:st=>{
     const w = wf(st);
     return !!w && ((w.jobs.test || {}).strategy || {})["fail-fast"] === false; }},
   {label:"Отменённых вариантов нет", test:st=>insts(st, "test").length === 3 &&
     !insts(st, "test").some(i => i.status === "cancelled")},
   {label:"Падает ровно один", test:st=>insts(st, "test").filter(i => i.status === "failure").length === 1},
   {label:"Два других прошли до конца", test:st=>insts(st, "test").filter(i => i.status === "success").length === 2}
  ]}
],
iv:[
 {q:"Как ускорить конвейер, в котором тесты идут двадцать минут?",
  probe:"Один из самых практичных вопросов для автоматизатора. Ждут несколько рычагов, а не один.",
  a:"Первым делом я смотрю, где именно уходит время, — обычно это видно по журналу шагов. Дальше рычаги примерно в таком порядке. Кеш зависимостей: восстановление пакетов и браузеров часто занимает больше, чем сами тесты, и кешируется почти бесплатно. Параллельность: разложить независимые проверки на отдельные задачи, чтобы время конвейера равнялось самой длинной ветке, а не сумме. Шардирование: матрица разбивает один набор тестов на несколько одновременных запусков, и двадцать минут превращаются в пять — при условии, что тесты независимы и делятся равномерно, иначе выигрыша не будет, потому что время равно самому долгому шарду. И разделение по скорости: быстрые тесты на каждый запрос слияния, медленные — отдельной задачей или ночью. Отдельно я всегда проверяю, не тратится ли время на перезапуски из-за нестабильных тестов: это тот случай, когда ускорять нечего, надо чинить.",
  more:["Что кешировать в первую очередь?","Как делить тесты на шарды?"]}
]
},

/* ------------------------------------------------ 6 */
{
n:6, id:"cache", title:"Кеш", sub:"Не скачивать одно и то же",
lede:"Кеш экономит и время конвейера, и деньги. Но настроенный неправильно он либо не срабатывает вовсе, либо подсовывает устаревшее — и второе хуже.",
theory:`
<h2>Как устроено</h2>
<pre><code>- <span class="k">uses</span>: actions/cache@v4
  <span class="k">with</span>:
    <span class="k">path</span>: ~/.nuget/packages
    <span class="k">key</span>: nuget-\${{ hashFiles('Directory.Packages.props') }}
    <span class="k">restore-keys</span>: |
      nuget-</code></pre>
<div class="tw"><table>
<tr><th>Поле</th><th>Что означает</th></tr>
<tr><td><code>path</code></td><td>что складывать в кеш</td></tr>
<tr><td><code>key</code></td><td>точный ключ: совпал — восстановили, не совпал — соберём и сохраним под ним</td></tr>
<tr><td><code>restore-keys</code></td><td>запасные префиксы: взять хоть что-то похожее, чтобы не качать всё с нуля</td></tr>
</table></div>
<p>Логика такая: точное совпадение — восстанавливаем и в конце ничего не сохраняем. Точного нет, но нашёлся запасной — восстанавливаем частично, а в конце сохраняем под новым точным ключом. Ничего не нашли — полная подготовка и сохранение.</p>

<h2>Ключ решает всё</h2>
<div class="note trap"><b class="hd">Ключ без отпечатка зависимостей — худший вариант</b><p>Ключ вида <code>nuget-cache</code> совпадает всегда. Значит, кеш восстанавливается всегда — и никогда не обновляется. Добавили пакет, а конвейер продолжает работать со старым набором: либо падает непонятно, либо, что хуже, проходит на устаревших зависимостях.</p><p>Правильный ключ включает <code>hashFiles</code> от файла блокировки: <code>nuget-\${{ hashFiles('**/packages.lock.json') }}</code>. Меняются зависимости — меняется ключ — кеш пересобирается сам.</p></div>
<div class="note ok"><b class="hd">Проверьте оба случая</b><p>Запустите конвейер дважды подряд — второй раз пройдёт заметно быстрее. Потом измените файл зависимостей и запустите снова: время вернётся к первоначальному, а в кеше появится вторая запись. Команда <code>ci cache</code> показывает, что там лежит.</p></div>

<h2>Что кешировать</h2>
<div class="tw"><table>
<tr><th>Кешируют</th><th>Не кешируют</th></tr>
<tr><td>пакеты: <code>~/.nuget/packages</code>, <code>~/.npm</code></td><td>результаты сборки — их передают артефактом</td></tr>
<tr><td>браузеры для тестов интерфейса</td><td>исходный код — он и так выгружается</td></tr>
<tr><td>образы промежуточных слоёв сборки</td><td>что-то, что меняется каждый запуск</td></tr>
</table></div>
<p>Отдельно про браузеры: установка браузеров для тестов интерфейса — одна из самых долгих операций в конвейере автоматизатора и кешируется отлично, потому что меняется редко и зависит только от версии библиотеки.</p>

<h2>Кеш — это не хранилище</h2>
<div class="note warn"><b class="hd">На кеш нельзя полагаться</b><p>Записи вытесняются по времени и по размеру, кеш может быть недоступен, и конвейер обязан работать без него — просто медленнее. Поэтому шаг восстановления кеша никогда не делают обязательным условием, а на его результат (<code>cache-hit</code>) можно опираться только для ускорения, но не для правильности.</p></div>
<pre><code>- <span class="k">id</span>: cache
  <span class="k">uses</span>: actions/cache@v4
  <span class="k">with</span>: ...

- <span class="k">if</span>: steps.cache.outputs.cache-hit != 'true'
  <span class="k">run</span>: dotnet restore     <span class="c"># пропустить, если всё уже на месте</span></code></pre>
`,
quiz:[
 {q:"Чем плох ключ кеша <code>nuget-cache</code> без <code>hashFiles</code>?",
  opts:["Слишком короткий","Совпадает всегда — кеш восстанавливается, но никогда не обновляется","Не поддерживается","Занимает много места"],
  a:1, why:"Добавили пакет — а конвейер работает со старым набором: падает непонятно либо проходит на устаревших зависимостях."},
 {q:"Что делают <code>restore-keys</code>?",
  opts:["Задают запасные ключи для частичного восстановления, если точного совпадения нет","Удаляют старый кеш","Шифруют кеш","Ограничивают размер"],
  a:0, why:"Тогда конвейер не качает всё с нуля, а в конце сохраняет кеш под новым точным ключом."},
 {q:"Что происходит при точном совпадении ключа?",
  opts:["Кеш восстанавливается и в конце пересохраняется","Кеш восстанавливается, сохранять нечего","Ничего","Кеш удаляется"],
  a:1, why:"Пересохранение нужно только когда точного совпадения не было."},
 {q:"Что не стоит класть в кеш?",
  opts:["Пакеты зависимостей","Результаты сборки — их передают артефактом","Браузеры для тестов","Слои образов"],
  a:1, why:"Кеш ускоряет подготовку, артефакт передаёт результат. Это разные механизмы."},
 {q:"Можно ли полагаться на наличие кеша?",
  opts:["Да","Нет: записи вытесняются, кеш может быть недоступен — конвейер обязан работать без него","Только на своих раннерах","Только для пакетов"],
  a:1, why:"На <code>cache-hit</code> можно опираться для ускорения, но не для правильности."},
 {q:"Что из перечисленного кешируется особенно выгодно для автоматизатора?",
  opts:["Исходный код","Браузеры для тестов интерфейса","Отчёты о прогоне","Логи"],
  a:1, why:"Установка браузеров — одна из самых долгих операций и меняется редко."}
],
labs:[
 {id:"6a", title:"Добавить кеш",
  brief:"<p>Каждый запуск заново скачивает пакеты. Добавьте кеширование:</p><ul><li>шаг <code>actions/cache@v4</code> <b>после</b> выгрузки кода и <b>до</b> <code>dotnet restore</code>;</li><li>путь — <code>~/.nuget/packages</code>;</li><li>ключ обязан включать <code>hashFiles('Directory.Packages.props')</code>.</li></ul><p>Запустите конвейер дважды и убедитесь, что второй раз быстрее.</p>",
  hint:"key: nuget-\${{ hashFiles('Directory.Packages.props') }} — обратите внимание на одинарные кавычки внутри.",
  setup: () => newScenario({files:{
    "src/App.cs": "class App { }",
    "Directory.Packages.props": "<Project><PackageVersion Include=\"Xunit\" Version=\"2.6\"/></Project>",
    ".github/workflows/ci.yml":
`name: CI
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: dotnet restore
      - run: dotnet build`}}),
  checks:[
   {label:"Шаг кеширования добавлен", test:st=>{
     const w = wf(st); if(!w) return false;
     return [].concat((w.jobs.build || {}).steps || []).some(s => /actions\/cache/.test(String(s.uses || ""))); }},
   {label:"Ключ включает <code>hashFiles</code>", test:st=>{
     const w = wf(st); if(!w) return false;
     return [].concat((w.jobs.build || {}).steps || []).some(s =>
       /actions\/cache/.test(String(s.uses || "")) && /hashFiles/.test(String((s["with"] || {}).key || ""))); }},
   {label:"Конвейер запускался не меньше двух раз", test:st=>st.C.runs.length >= 2},
   {label:"Второй запуск быстрее первого", test:st=>{
     const r = st.C.runs;
     return r.length >= 2 && r[r.length - 1].wall < r[0].wall; }},
   {label:"В кеше появилась запись", test:st=>Object.keys(st.C.cache).length >= 1}
  ]},
 {id:"6b", title:"Кеш, который не обновляется",
  brief:"<p>Кеш настроен, но с постоянным ключом. Проверьте, к чему это приводит, и почините:</p><ul><li>запустите конвейер;</li><li>измените файл <code>Directory.Packages.props</code> во вкладке «Конвейер» — добавьте любой пакет;</li><li>запустите снова: кеш подхватится тот же самый, хотя зависимости изменились;</li><li>исправьте ключ так, чтобы он зависел от содержимого файла, и добейтесь второй записи в кеше.</li></ul>",
  hint:"ci cache показывает записи. После починки ключа их должно стать две — по одной на каждое состояние зависимостей.",
  setup: () => newScenario({files:{
    "src/App.cs": "class App { }",
    "Directory.Packages.props": "<Project><PackageVersion Include=\"Xunit\" Version=\"2.6\"/></Project>",
    ".github/workflows/ci.yml":
`name: CI
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: nuget-cache
      - run: dotnet restore`}}),
  checks:[
   {label:"Ключ стал зависеть от содержимого", test:st=>{
     const w = wf(st); if(!w) return false;
     return [].concat((w.jobs.build || {}).steps || []).some(s =>
       /actions\/cache/.test(String(s.uses || "")) && /hashFiles/.test(String((s["with"] || {}).key || ""))); }},
   {label:"Файл зависимостей изменялся", test:st=>
     st.C.files["Directory.Packages.props"].indexOf("Xunit") >= 0 &&
     st.C.files["Directory.Packages.props"].length > 70},
   {label:"В кеше две записи — по одной на состояние", test:st=>Object.keys(st.C.cache).length >= 2},
   {label:"Кеш просматривался командой", test:st=>okRan(st,/ci\s+cache/)}
  ]}
],
iv:[
 {q:"Как правильно настроить кеш зависимостей?",
  probe:"Проверяют, понимаете ли вы роль ключа. Ответ «добавляю actions/cache» недостаточен.",
  a:"Главное — ключ. Он должен зависеть от содержимого файла, который описывает зависимости: обычно это <code>hashFiles</code> от файла блокировки. Тогда логика получается правильной сама собой: зависимости не менялись — ключ совпал, кеш восстановился, сохранять нечего; зависимости изменились — ключ другой, кеш собирается заново и сохраняется под новым ключом. Ключ-константа — самая частая ошибка: кеш восстанавливается всегда и никогда не обновляется, и в какой-то момент конвейер начинает работать на устаревших пакетах. Полезно добавить <code>restore-keys</code> с префиксом, чтобы при промахе восстановить хотя бы часть и не качать всё с нуля. И важная оговорка: на кеш нельзя полагаться — записи вытесняются, хранилище может быть недоступно, поэтому конвейер обязан отрабатывать и без него, просто медленнее.",
  more:["Чем кеш отличается от артефакта?","Что кешируете в проекте с UI-тестами?"]}
]
},

/* ------------------------------------------------ 7 */
{
n:7, id:"artifacts", title:"Артефакты", sub:"Что забрать из прогона",
lede:"Упавший тест в конвейере бесполезен, если после него ничего не осталось. Артефакты — это то, ради чего вообще стоит смотреть на красную сборку.",
theory:`
<h2>Кеш и артефакт — разные вещи</h2>
<div class="tw"><table>
<tr><th></th><th>Кеш</th><th>Артефакт</th></tr>
<tr><td>Зачем</td><td>ускорить подготовку</td><td>сохранить результат</td></tr>
<tr><td>Между чем</td><td>между запусками</td><td>между задачами и для человека</td></tr>
<tr><td>Можно потерять</td><td>да, это нормально</td><td>нет — за ним и приходят</td></tr>
</table></div>

<h2>Как сохранить</h2>
<pre><code>- <span class="k">if</span>: always()
  <span class="k">uses</span>: actions/upload-artifact@v4
  <span class="k">with</span>:
    <span class="k">name</span>: test-results
    <span class="k">path</span>: |
      TestResults/**
      test-results/**
    <span class="k">if-no-files-found</span>: warn
    <span class="k">retention-days</span>: 7</code></pre>
<div class="note trap"><b class="hd">Без <code>if: always()</code> артефакт не сохранится именно тогда, когда нужен</b><p>Шаги после упавшего пропускаются. Значит, при падении тестов шаг выгрузки отчётов не выполнится — и вы останетесь ровно с той сборкой, где всё интересное потеряно. Это, пожалуй, самая обидная ошибка в конвейерах: настроили красиво, а в момент истины ничего нет.</p></div>

<h2>Что забирать автоматизатору</h2>
<div class="tw"><table>
<tr><th>Что</th><th>Зачем</th></tr>
<tr><td>Отчёт о прогоне</td><td>какие тесты упали и с каким сообщением</td></tr>
<tr><td>Трассировки и видео</td><td>что происходило на экране в момент падения</td></tr>
<tr><td>Снимки экрана</td><td>быстрый взгляд без разворачивания трассировки</td></tr>
<tr><td>Логи приложения</td><td>что было на стороне сервера</td></tr>
<tr><td>Собранные артефакты сборки</td><td>чтобы следующая задача не собирала заново</td></tr>
</table></div>
<div class="note ok"><b class="hd">Хорошая практика</b><p>Тяжёлые вещи — видео и трассировки — сохранять только при падении: <code>if: failure()</code>. Отчёт о прогоне — всегда: <code>if: always()</code>. Так и разбирать есть что, и хранилище не забивается гигабайтами успешных прогонов.</p></div>

<h2>Передача между задачами</h2>
<pre><code><span class="c"># в задаче build</span>
- <span class="k">uses</span>: actions/upload-artifact@v4
  <span class="k">with</span>:
    <span class="k">name</span>: build-output
    <span class="k">path</span>: bin/**

<span class="c"># в задаче deploy</span>
- <span class="k">uses</span>: actions/download-artifact@v4
  <span class="k">with</span>:
    <span class="k">name</span>: build-output</code></pre>
<p>Это единственный штатный способ передать файлы между задачами. Стоит он времени на выгрузку и загрузку, поэтому для мелочей проще пересобрать, а для чего-то тяжёлого и долгого — передать.</p>

<h2>Сроки хранения</h2>
<p><code>retention-days</code> задаёт, сколько держать. Для отчётов о прогоне недели обычно достаточно, для артефактов релиза — наоборот, хранят долго. Значение по умолчанию великовато, и в активном проекте хранилище заполняется быстрее, чем кажется.</p>
`,
quiz:[
 {q:"Чем артефакт отличается от кеша?",
  opts:["Ничем","Кеш ускоряет подготовку и его не жалко потерять; артефакт сохраняет результат, за которым приходят","Артефакт быстрее","Кеш только для пакетов"],
  a:1, why:"Разные механизмы под разные задачи. Результаты сборки — артефакт, пакеты — кеш."},
 {q:"Почему шаг выгрузки отчётов помечают <code>if: always()</code>?",
  opts:["Для скорости","Иначе он пропустится после падения теста — именно тогда, когда отчёт и нужен","Так требует действие","Чтобы не дублировать"],
  a:1, why:"Самая обидная ошибка в конвейерах: настроили красиво, а в момент истины ничего не осталось."},
 {q:"Что разумно сохранять только при падении?",
  opts:["Отчёт о прогоне","Видео и трассировки — они тяжёлые","Логи сборки","Ничего"],
  a:1, why:"Так и разбирать есть что, и хранилище не забивается гигабайтами успешных прогонов."},
 {q:"Как передать файлы из одной задачи в другую?",
  opts:["Через кеш","Через артефакт: выгрузить и загрузить","Через переменную","Автоматически"],
  a:1, why:"Это единственный штатный способ. Стоит времени, поэтому для мелочей проще пересобрать."},
 {q:"Что задаёт <code>retention-days</code>?",
  opts:["Срок хранения артефакта","Время жизни кеша","Таймаут задачи","Число попыток"],
  a:0, why:"Значение по умолчанию великовато: в активном проекте хранилище заполняется быстрее, чем кажется."},
 {q:"Что делает <code>if-no-files-found: error</code>?",
  opts:["Ничего","Роняет шаг, если по указанному пути ничего не нашлось","Создаёт пустой артефакт","Пропускает шаг"],
  a:1, why:"Полезно, когда отсутствие файлов само по себе означает поломку — например, отчёт обязан быть."}
],
labs:[
 {id:"7a", title:"Сохранить отчёт о падении",
  brief:"<p>Тесты интерфейса нестабильны и иногда падают, но после прогона ничего не остаётся — разбирать нечего.</p><ul><li>добавьте шаг выгрузки артефакта с именем <code>test-results</code> и путём <code>test-results/**</code>;</li><li>сделайте так, чтобы он выполнялся <b>даже при падении</b> тестов;</li><li>запустите и убедитесь через <code>ci artifacts</code>, что артефакт есть.</li></ul>",
  hint:"if: always() ставится у шага рядом с uses, на том же уровне отступа.",
  setup: () => newScenario({seed: 11, files:{
    "src/App.cs": "class App { }",
    ".github/workflows/ci.yml":
`name: CI
on: [push]

jobs:
  ui:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: dotnet test --filter ui`}}),
  checks:[
   {label:"Шаг выгрузки добавлен", test:st=>{
     const w = wf(st); if(!w) return false;
     return [].concat((w.jobs.ui || {}).steps || []).some(s => /upload-artifact/.test(String(s.uses || ""))); }},
   {label:"Он выполняется при падении", test:st=>{
     const w = wf(st); if(!w) return false;
     return [].concat((w.jobs.ui || {}).steps || []).some(s =>
       /upload-artifact/.test(String(s.uses || "")) && /always|failure/.test(String(s["if"] || ""))); }},
   {label:"Тесты действительно упали", test:st=>{ const r = last(st); return !!r && r.status === "failure"; }},
   {label:"Артефакт сохранён", test:st=>{ const r = last(st); return !!r && !!r.artifacts["test-results"]; }},
   {label:"В нём есть трассировки", test:st=>{
     const r = last(st);
     return !!r && !!r.artifacts["test-results"] && r.artifacts["test-results"].files.some(f => /trace/.test(f)); }}
  ]},
 {id:"7b", title:"Передать сборку дальше",
  brief:"<p>Задача <code>report</code> должна получить отчёт, который сформировала задача <code>test</code>. Сейчас она падает: файла нет, потому что задачи выполняются на разных машинах.</p><ul><li>в задаче <code>test</code> выгрузите артефакт с именем <code>trx</code> и путём <code>TestResults/**</code>;</li><li>в задаче <code>report</code> загрузите его.</li></ul>",
  hint:"actions/upload-artifact@v4 в первой задаче и actions/download-artifact@v4 во второй, имя должно совпадать.",
  setup: () => newScenario({flakyFixed: true, files:{
    "src/App.cs": "class App { }",
    ".github/workflows/ci.yml":
`name: CI
on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: dotnet test --filter unit

  report:
    needs: [test]
    runs-on: ubuntu-latest
    steps:
      - run: cat TestResults/results.trx`}}),
  checks:[
   {label:"Артефакт выгружается в <code>test</code>", test:st=>{
     const w = wf(st); if(!w) return false;
     return [].concat((w.jobs.test || {}).steps || []).some(s => /upload-artifact/.test(String(s.uses || ""))); }},
   {label:"И загружается в <code>report</code>", test:st=>{
     const w = wf(st); if(!w) return false;
     return [].concat((w.jobs.report || {}).steps || []).some(s => /download-artifact/.test(String(s.uses || ""))); }},
   {label:"Имена совпадают — артефакт назван <code>trx</code>", test:st=>{
     const r = last(st); return !!r && !!r.artifacts["trx"]; }},
   {label:"Задача <code>report</code> проходит", test:st=>{
     const j = jobOf(st, "report"); return !!j && j.status === "success"; }},
   {label:"Конвейер целиком зелёный", test:st=>{ const r = last(st); return !!r && r.status === "success"; }}
  ]}
],
iv:[
 {q:"Что вы забираете из упавшего прогона автотестов?",
  probe:"Профильный вопрос для автоматизатора. По ответу видно, разбирал ли человек падения в CI.",
  a:"Минимум — отчёт о прогоне: какие тесты упали, с каким сообщением и на каком шаге. Для тестов интерфейса к этому обязательно добавляются трассировка и видео, а часто и снимок экрана в момент падения: без них разбор превращается в гадание, потому что воспроизвести на своей машине удаётся не всегда. Плюс логи приложения со стороны сервера, если оно поднимается в том же конвейере. Всё это сохраняется артефактами, и ключевая деталь — шаг выгрузки должен быть помечен <code>if: always()</code>, иначе после падения теста он просто не выполнится, и вы останетесь ровно с той сборкой, где всё интересное потеряно. Тяжёлые вещи вроде видео я обычно сохраняю только при падении через <code>if: failure()</code>, а отчёт — всегда, и ставлю разумный срок хранения, чтобы не забивать хранилище успешными прогонами.",
  more:["Чем артефакт отличается от кеша?","Как настроить трассировку только для упавших?"]}
]
},

/* ------------------------------------------------ 8 */
{
n:8, id:"conditions", title:"Условия", sub:"success, failure, always",
lede:"Условия управляют тем, что выполняется, а что пропускается. Три функции состояния объясняют почти всё поведение конвейера при падениях.",
theory:`
<h2>Поведение по умолчанию</h2>
<div class="tw"><table>
<tr><th>Что произошло</th><th>Что дальше</th></tr>
<tr><td>Шаг упал</td><td>следующие шаги задачи пропускаются</td></tr>
<tr><td>Задача упала</td><td>зависимые задачи пропускаются</td></tr>
<tr><td>У шага есть <code>if</code></td><td>поведение по умолчанию отменяется — условие решает всё</td></tr>
</table></div>
<p>Последняя строка важна: как только вы пишете <code>if</code>, неявное «только при успехе» исчезает. Поэтому <code>if: github.ref_name == 'main'</code> выполнится и после падения предыдущего шага, если не добавить <code>success() &amp;&amp;</code>.</p>

<h2>Функции состояния</h2>
<div class="tw"><table>
<tr><th>Функция</th><th>Когда истинна</th><th>Типичное применение</th></tr>
<tr><td><code>success()</code></td><td>всё до этого прошло — подразумевается по умолчанию</td><td>выкладка</td></tr>
<tr><td><code>failure()</code></td><td>что-то упало</td><td>уведомление, сбор диагностики</td></tr>
<tr><td><code>always()</code></td><td>всегда, даже при отмене</td><td>выгрузка отчётов, уборка</td></tr>
<tr><td><code>cancelled()</code></td><td>запуск отменили</td><td>редко, для точной уборки</td></tr>
</table></div>
<pre><code>- <span class="k">name</span>: Тесты
  <span class="k">run</span>: dotnet test

- <span class="k">name</span>: Отчёт
  <span class="k">if</span>: always()
  <span class="k">uses</span>: actions/upload-artifact@v4

- <span class="k">name</span>: Уведомить команду
  <span class="k">if</span>: failure()
  <span class="k">run</span>: echo <span class="s">"сборка упала"</span>

- <span class="k">name</span>: Выложить
  <span class="k">if</span>: success() &amp;&amp; github.ref_name == 'main'
  <span class="k">run</span>: echo <span class="s">"выкладываем"</span></code></pre>

<h2><code>continue-on-error</code></h2>
<div class="note warn"><b class="hd">Мягкое падение</b><p>Шаг с <code>continue-on-error: true</code> может упасть, но задача останется зелёной. Уместно для необязательных проверок: анализатор, замер покрытия, отправка метрики. Опасно для тестов — так конвейер превращается в декорацию. Если проверка не должна блокировать, честнее вынести её в отдельную необязательную задачу, чтобы падение было видно.</p></div>

<h2>Условия на уровне задачи</h2>
<pre><code><span class="k">deploy</span>:
  <span class="k">needs</span>: [build, test]
  <span class="k">if</span>: github.ref_name == 'main' &amp;&amp; github.event_name == 'push'
  <span class="k">runs-on</span>: ubuntu-latest</code></pre>
<p>Так выкладка не срабатывает на запросах слияния и на ветках задач. Задача с невыполненным условием получает статус «пропущена» — это не падение, и последующие задачи не ломаются.</p>

<h2>Порядок разбора при падении</h2>
<div class="tw"><table>
<tr><th>Шаг</th><th>Что смотреть</th></tr>
<tr><td>1</td><td><code>ci jobs</code> — какая задача и какой вариант упали</td></tr>
<tr><td>2</td><td><code>ci logs &lt;задача&gt;</code> — на каком шаге и с каким сообщением</td></tr>
<tr><td>3</td><td>артефакты — отчёт, трассировка, логи</td></tr>
<tr><td>4</td><td>только потом — гипотезы</td></tr>
</table></div>
`,
quiz:[
 {q:"Что произойдёт с шагом, у которого есть <code>if: github.ref_name == 'main'</code>, если предыдущий шаг упал?",
  opts:["Пропустится","Выполнится — свой <code>if</code> отменяет неявное «только при успехе»","Упадёт","Задача остановится"],
  a:1, why:"Чтобы сохранить поведение по умолчанию, пишут <code>success() &amp;&amp; ...</code>"},
 {q:"Когда истинна <code>always()</code>?",
  opts:["При успехе","Всегда — даже при падении и отмене","При падении","Никогда"],
  a:1, why:"Типичное применение — выгрузка отчётов и уборка после себя."},
 {q:"Чем опасен <code>continue-on-error: true</code> у тестов?",
  opts:["Замедляет","Задача остаётся зелёной, хотя тесты упали — конвейер превращается в декорацию","Ломает артефакты","Ничем"],
  a:1, why:"Если проверка не должна блокировать, честнее вынести её в отдельную необязательную задачу."},
 {q:"Задача с невыполненным <code>if</code> — это падение?",
  opts:["Да","Нет, она «пропущена» — последующие задачи не ломаются","Зависит от настройки","Она вообще не создаётся"],
  a:1, why:"Так выкладку и ограничивают: на запросах слияния она просто пропускается."},
 {q:"Что означает <code>if: failure()</code> у шага?",
  opts:["Шаг упадёт","Шаг выполнится, только если что-то до него упало","Шаг отключён","Шаг повторится"],
  a:1, why:"Типично для уведомлений и сбора дополнительной диагностики."},
 {q:"С чего начинать разбор упавшего конвейера?",
  opts:["С гипотез","Со списка задач и журнала: что упало и на каком шаге","С перезапуска","С правки конвейера"],
  a:1, why:"Потом артефакты — отчёт, трассировка, логи. Гипотезы в последнюю очередь."}
],
labs:[
 {id:"8a", title:"Уведомление и отчёт",
  brief:"<p>Задача с тестами падает. Доработайте её так, чтобы:</p><ul><li>шаг с именем <code>отчёт</code> выполнялся <b>всегда</b> и печатал <code>отчёт готов</code>;</li><li>шаг с именем <code>уведомление</code> выполнялся <b>только при падении</b> и печатал <code>сборка упала</code>;</li><li>шаг с именем <code>выкладка</code> выполнялся <b>только при успехе</b> и печатал <code>выложено</code>.</li></ul><p>При текущем состоянии первые два должны сработать, третий — нет.</p>",
  hint:"if: always(), if: failure(), if: success() — по одному на каждый шаг.",
  setup: () => newScenario({files:{
    "src/App.cs": "class App { }",
    ".github/workflows/ci.yml":
`name: CI
on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: тесты
        run: exit 1
      - name: отчёт
        run: echo "отчёт готов"
      - name: уведомление
        run: echo "сборка упала"
      - name: выкладка
        run: echo "выложено"`}}),
  checks:[
   {label:"Отчёт выполнился", test:st=>logHas(st, /отчёт готов/)},
   {label:"Уведомление выполнилось", test:st=>logHas(st, /сборка упала/)},
   {label:"Выкладка пропущена", test:st=>{
     const s = stepsOf(st, "test").filter(x => x.name === "выкладка")[0];
     return !!s && s.status === "skipped"; }},
   {label:"Задача осталась упавшей", test:st=>{
     const j = jobOf(st, "test"); return !!j && j.status === "failure"; }}
  ]},
 {id:"8b", title:"Выкладка только с основной ветки",
  brief:"<p>Задача <code>deploy</code> сейчас выполняется на любом запуске. Ограничьте её: выкладка должна происходить только когда событие <code>push</code> <b>и</b> ветка <code>main</code>.</p><p>Сценарий запускается на ветке <code>feature/x</code>, поэтому после правки задача должна быть пропущена, а остальные — пройти.</p>",
  hint:"if на уровне задачи, рядом с needs: github.ref_name == 'main' && github.event_name == 'push'",
  setup: () => newScenario({branch: "feature/x", files:{
    "src/App.cs": "class App { }",
    ".github/workflows/ci.yml":
`name: CI
on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: dotnet test --filter unit

  deploy:
    needs: [test]
    runs-on: ubuntu-latest
    steps:
      - run: echo "выкладываем в бой"`}}),
  checks:[
   {label:"У задачи появилось условие", test:st=>{
     const w = wf(st); return !!w && (w.jobs.deploy || {})["if"] !== undefined; }},
   {label:"Условие проверяет и ветку, и событие", test:st=>{
     const w = wf(st); if(!w) return false;
     const c = String((w.jobs.deploy || {})["if"] || "");
     return /ref_name|ref\b/.test(c) && /event_name/.test(c); }},
   {label:"На ветке задачи выкладка пропускается", test:st=>{
     const j = jobOf(st, "deploy"); return !!j && j.status === "skipped"; }},
   {label:"Тесты при этом прошли", test:st=>{
     const j = jobOf(st, "test"); return !!j && j.status === "success"; }},
   {label:"Строки о выкладке в журнале нет", test:st=>{
     const j = jobOf(st, "deploy");
     return !!j && j.status === "skipped" && !logHas(st, /выкладываем в бой/); }}
  ]}
],
iv:[
 {q:"Как устроено поведение конвейера при падении шага?",
  probe:"Проверяют понимание неявных правил, а не знание списка функций.",
  a:"По умолчанию у каждого шага подразумевается условие «выполняться, только если всё до этого прошло». Поэтому упавший шаг обрывает задачу: остальные шаги пропускаются, а задача помечается упавшей, и зависимые от неё задачи тоже пропускаются. Ключевая тонкость: как только у шага появляется собственный <code>if</code>, неявное правило исчезает целиком. То есть условие вида «только на основной ветке» выполнится и после падения, если не написать <code>success() &amp;&amp;</code> явно. Отсюда практика: выгрузку отчётов и уборку помечают <code>always()</code>, уведомления — <code>failure()</code>, выкладку — <code>success()</code> вместе с проверкой ветки. Есть ещё <code>continue-on-error</code>, который позволяет шагу упасть, не роняя задачу; для необязательных проверок это уместно, а для тестов — способ превратить конвейер в декорацию.",
  more:["Почему свой if отменяет умолчание?","Когда уместен continue-on-error?"]}
]
},
