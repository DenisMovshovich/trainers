/* ============================================================
   Содержание курса
   ============================================================ */
const ran  = (st, re) => st.log.some(e => re.test(e.cmd));
const okRan = (st, re) => st.log.some(e => !e.err && re.test(e.cmd));
const errRan = (st, re) => st.log.some(e => e.err && re.test(e.err));
const outHas = (st, re) => st.log.some(e => !e.err && re.test(e.lines.join("\n")));
const topic = (st, n) => st.c.topics[n];
const grp = (st, g) => st.c.groups[g];
const allRecs = (st, n) => { const t = topic(st, n); return t ? t.partitions.reduce((a, p) => a.concat(p.log), []) : []; };

const MODULES = [

/* ------------------------------------------------ 1 */
{
n:1, id:"why", title:"Зачем брокер", sub:"Очередь против журнала",
lede:"Что именно решает брокер сообщений, чем журнал Kafka отличается от классической очереди и когда эта разница определяет выбор.",
theory:`
<p>Два сервиса могут общаться напрямую: один вызывает HTTP-метод другого и ждёт ответа. Пока сервисов двое, это лучший вариант — просто и понятно. Проблемы начинаются, когда их становится десять.</p>

<h2>Что ломается при прямых вызовах</h2>
<div class="tw"><table>
<tr><th>Проблема</th><th>Как проявляется</th></tr>
<tr><td><b>Связность</b></td><td>отправитель обязан знать адрес каждого получателя; новый потребитель события требует правки отправителя</td></tr>
<tr><td><b>Доступность</b></td><td>получатель лежит — отправитель либо ждёт, либо теряет данные</td></tr>
<tr><td><b>Всплески</b></td><td>тысяча событий в секунду при пропускной способности получателя в сто; сглаживать нечем</td></tr>
<tr><td><b>Скорость</b></td><td>отправитель работает со скоростью самого медленного получателя</td></tr>
</table></div>
<p>Брокер разрывает эту связь. Отправитель («продюсер») пишет событие брокеру и продолжает работу; получатели («консьюмеры») читают когда и как успевают. Отправитель не знает, кто читает и сколько их.</p>

<h2>Очередь против журнала</h2>
<figure class="fig">
<svg viewBox="0 0 620 218" role="img" aria-label="Классическая очередь против журнала Kafka">
 <g font-family="var(--fm)" font-size="10">
  <text x="0" y="11" font-size="10.5" font-weight="700" fill="var(--ink2)">КЛАССИЧЕСКАЯ ОЧЕРЕДЬ</text>
  <text x="0" y="24" font-size="9" fill="var(--ink3)">RabbitMQ, ActiveMQ, SQS</text>
  ${[0,1,2,3].map(i=>'<rect x="'+(i*44)+'" y="34" width="40" height="26" rx="2" fill="var(--surf3)" stroke="var(--line)"/>'+
      '<text x="'+(i*44+20)+'" y="51" text-anchor="middle" font-size="9" fill="var(--ink2)">'+(i<2?"—":"м"+(i-1))+'</text>').join("")}
  <path d="M180 47h30" stroke="var(--acc)" stroke-width="1.3"/><path d="M210 47l-8-4.5v9z" fill="var(--acc)"/>
  <rect x="214" y="34" width="86" height="26" rx="2" fill="var(--acc-s)" stroke="var(--acc)"/>
  <text x="257" y="51" text-anchor="middle" font-size="9" fill="var(--ink)">потребитель</text>
  <text x="0" y="76" font-size="9" fill="var(--ink3)">Прочитанное и подтверждённое сообщение брокер удаляет.</text>
  <text x="0" y="89" font-size="9" fill="var(--ink3)">Второму потребителю его уже не прочитать: оно исчезло.</text>

  <line x1="0" y1="104" x2="620" y2="104" stroke="var(--line)"/>

  <text x="0" y="124" font-size="10.5" font-weight="700" fill="var(--acc)">ЖУРНАЛ</text>
  <text x="0" y="137" font-size="9" fill="var(--ink3)">Kafka, Pulsar, Kinesis</text>
  ${[0,1,2,3,4,5].map(i=>'<rect x="'+(i*44)+'" y="147" width="40" height="26" rx="2" fill="'+(i<4?"var(--surf3)":"var(--ok-s)")+'" stroke="var(--line)"/>'+
      '<text x="'+(i*44+20)+'" y="164" text-anchor="middle" font-size="9" fill="var(--ink2)">м'+i+'</text>'+
      '<text x="'+(i*44+20)+'" y="184" text-anchor="middle" font-size="8.5" fill="var(--ink3)">'+i+'</text>').join("")}
  <path d="M270 152h44" stroke="var(--key)" stroke-width="1.2"/><path d="M314 152l-7-4v8z" fill="var(--key)"/>
  <text x="322" y="155" font-size="9" fill="var(--key)">группа А читает со смещения 4</text>
  <path d="M114 168h200" stroke="var(--acc)" stroke-width="1.2"/><path d="M314 168l-7-4v8z" fill="var(--acc)"/>
  <text x="322" y="171" font-size="9" fill="var(--acc)">группа Б читает со смещения 2</text>
  <text x="0" y="203" font-size="9" fill="var(--ink3)">Чтение ничего не удаляет. Записи лежат положенный срок, каждый читатель держит своё место.</text>
  <text x="0" y="214" font-size="9" fill="var(--ink3)">Можно перечитать прошлое: отмотать смещение назад и прогнать всё заново.</text>
 </g>
</svg>
<figcaption>Ключевое различие не в скорости, а в том, что происходит после чтения. Очередь раздаёт задачи; журнал хранит факты.</figcaption>
</figure>

<h2>Из чего состоит Kafka</h2>
<div class="tw"><table>
<tr><th>Понятие</th><th>Что это</th></tr>
<tr><td><b>Брокер</b></td><td>один узел кластера: хранит данные, обслуживает запросы</td></tr>
<tr><td><b>Топик</b></td><td>именованный поток записей, например <code>orders</code></td></tr>
<tr><td><b>Партиция</b></td><td>часть топика: собственно журнал, упорядоченная последовательность записей</td></tr>
<tr><td><b>Смещение</b></td><td>номер записи внутри партиции, растёт и никогда не переиспользуется</td></tr>
<tr><td><b>Продюсер</b></td><td>клиент, который пишет</td></tr>
<tr><td><b>Консьюмер</b></td><td>клиент, который читает</td></tr>
<tr><td><b>Группа</b></td><td>несколько консьюмеров, делящих партиции между собой</td></tr>
</table></div>
<div class="note ok"><b class="hd">Как устроена практика</b><p>Консоль внизу управляет настоящим учебным кластером из трёх брокеров: топики и партиции, репликация с ISR, выборы лидера, группы потребителей, смещения, срок хранения и уплотнение. Команды повторяют утилиты Kafka: <code>kafka-topics</code>, <code>kafka-consumer-groups</code>, <code>kafka-console-consumer</code>. Наберите <code>help</code>, чтобы увидеть список.</p><p>Состояние кластера всегда видно справа, а журнал партиций — на вкладке «Журнал» рядом с выводом команды.</p></div>

<h2>Чего Kafka не делает</h2>
<div class="note warn"><b class="hd">Это не замена базе данных и не брокер задач</b><p>Kafka не умеет запрашивать запись по произвольному условию — только читать журнал подряд с какого-то места. В ней нет приоритетов сообщений, отложенной доставки и повторной постановки одного сообщения в конец очереди. Если нужна очередь задач с приоритетами и индивидуальными подтверждениями — это RabbitMQ или SQS, а не Kafka.</p></div>
`,
quiz:[
 {q:"Что происходит с записью в Kafka после того, как её прочитал потребитель?",
  opts:["Брокер удаляет её","Ничего: она лежит до истечения срока хранения","Она помечается прочитанной для всех","Перемещается в архивный топик"],
  a:1, why:"Чтение не меняет журнал. Именно поэтому несколько независимых групп могут читать один поток, а любую из них можно отмотать назад и прогнать историю заново."},
 {q:"Чем журнал принципиально отличается от классической очереди?",
  opts:["Он быстрее","Чтение не удаляет запись, и каждый читатель держит собственное место","В нём есть приоритеты","Он хранит данные в памяти"],
  a:1, why:"Очередь раздаёт задачи и после подтверждения забывает сообщение. Журнал хранит факты и позволяет каждому потребителю идти по ним в своём темпе."},
 {q:"Что такое смещение (offset)?",
  opts:["Идентификатор потребителя","Номер записи внутри партиции","Время записи","Номер брокера"],
  a:1, why:"Смещение уникально в пределах партиции, монотонно растёт и никогда не используется повторно. У одинаковых номеров в разных партициях нет ничего общего."},
 {q:"Для какой задачи Kafka подходит плохо?",
  opts:["Поток событий для нескольких потребителей","Очередь задач с приоритетами и отложенным повтором отдельного сообщения","Буфер перед медленным хранилищем","Журнал изменений для аудита"],
  a:1, why:"В журнале нет приоритетов и повторной постановки одного сообщения — записи читаются подряд по смещению. Для таких сценариев берут RabbitMQ или SQS."},
 {q:"Продюсер отправил событие. Откуда он знает, кто его прочитает?",
  opts:["Указывает получателя в заголовке","Никак: он пишет в топик и не знает потребителей","Брокер сообщает список подписчиков","Через реестр сервисов"],
  a:1, why:"В этом и смысл развязки: новый потребитель добавляется без единой правки на стороне отправителя."},
 {q:"Зачем брокер нужен при всплесках нагрузки?",
  opts:["Он ускоряет получателя","Он принимает поток на скорости отправителя и отдаёт на скорости получателя","Он отбрасывает лишние события","Он сжимает трафик"],
  a:1, why:"Брокер работает буфером: отправитель не замедляется до скорости самого медленного читателя, а всплеск превращается в отставание, которое потом рассасывается."}
],
labs:[
 {id:"1a", title:"Первый топик",
  brief:"<ul><li>Наберите <code>help</code> и посмотрите список команд</li><li>Создайте топик <code>orders</code> с 3 партициями и фактором репликации 2</li><li>Посмотрите список топиков и подробное описание <code>orders</code></li><li>Запишите три события: <code>produce --topic orders --records a,b,c</code></li><li>Прочитайте их: <code>kafka-console-consumer --topic orders --from-beginning</code></li></ul>",
  hint:"kafka-topics --create --topic orders --partitions 3 --replication-factor 2",
  checks:[
   {label:"Топик <code>orders</code> создан с 3 партициями", test:st=>{const t=topic(st,"orders"); return !!t&&t.partitions.length===3;}},
   {label:"Фактор репликации 2", test:st=>{const t=topic(st,"orders"); return !!t&&t.rf===2;}},
   {label:"Выполнено <code>--describe</code>", test:st=>outHas(st,/Leader:/)},
   {label:"Записаны три события", test:st=>allRecs(st,"orders").length>=3},
   {label:"События прочитаны с начала", test:st=>okRan(st,/kafka-console-consumer[\s\S]*--from-beginning/)}
  ]},
 {id:"1b", title:"Чтение не удаляет",
  brief:"<ul><li>В топике <code>orders</code> прочитайте всё с начала ещё раз — записи на месте</li><li>Убедитесь через <code>dump --topic orders</code>, что журнал не изменился</li><li>Прочитайте <b>без</b> <code>--from-beginning</code> — ничего не вернётся: подписка без указания начала читает только будущее</li><li>Запишите ещё одно событие и повторите чтение с начала: записей стало больше</li></ul>",
  hint:"dump показывает сырое содержимое журнала: смещения, ключи, значения, а также logStart, LEO и высокую отметку.",
  checks:[
   {label:"Журнал просмотрен командой <code>dump</code>", test:st=>okRan(st,/^\s*dump\b/)},
   {label:"Чтение с начала выполнено дважды", test:st=>st.log.filter(e=>!e.err&&/--from-beginning/.test(e.cmd)).length>=2},
   {label:"Чтение без <code>--from-beginning</code> ничего не вернуло", test:st=>st.log.some(e=>!e.err&&/kafka-console-consumer/.test(e.cmd)&&!/--from-beginning/.test(e.cmd)&&/ничего не прочитано/.test(e.lines.join("\n")))},
   {label:"После нового события записей стало больше трёх", test:st=>allRecs(st,"orders").length>=4}
  ]}
]
},

/* ------------------------------------------------ 2 */
{
n:2, id:"part", title:"Партиции и смещения", sub:"Устройство журнала",
lede:"Партиция — единица порядка, параллелизма и хранения одновременно. Отсюда следует почти всё остальное в Kafka.",
theory:`
<p>Топик сам по себе ничего не хранит: это имя для набора <b>партиций</b>. Хранит и упорядочивает именно партиция — файл, в который записи добавляются только в конец.</p>

<figure class="fig">
<svg viewBox="0 0 620 190" role="img" aria-label="Топик из трёх партиций">
 <g font-family="var(--fm)" font-size="10">
  <text x="0" y="12" font-size="10" fill="var(--ink3)">топик orders</text>
  ${[0,1,2].map(p=>{
    const y = 24 + p*54, n = [6,4,5][p];
    let s = '<text x="0" y="'+(y+18)+'" font-size="9.5" fill="var(--acc)">п'+p+'</text>';
    for(let i=0;i<n;i++){
      s += '<rect x="'+(30+i*46)+'" y="'+y+'" width="42" height="26" rx="2" fill="var(--surf3)" stroke="var(--line)"/>'+
           '<text x="'+(30+i*46+21)+'" y="'+(y+17)+'" text-anchor="middle" font-size="9" fill="var(--ink2)">'+i+'</text>';
    }
    s += '<rect x="'+(30+n*46)+'" y="'+y+'" width="42" height="26" rx="2" fill="none" stroke="var(--line2)" stroke-dasharray="3 3"/>'+
         '<text x="'+(30+n*46+21)+'" y="'+(y+17)+'" text-anchor="middle" font-size="9" fill="var(--ink3)">'+n+'</text>'+
         '<text x="'+(30+n*46+52)+'" y="'+(y+17)+'" font-size="9" fill="var(--ink3)">← сюда пишется следующая</text>';
    return s;
  }).join("")}
  <text x="0" y="186" font-size="9" fill="var(--ink3)">Порядок есть внутри каждой строки. Между строками порядка нет: записи п0-3 и п1-3 несравнимы по времени.</text>
 </g>
</svg>
<figcaption>Смещения нумеруются независимо в каждой партиции. Число 3 в разных партициях не значит ничего общего.</figcaption>
</figure>

<h2>Три роли партиции</h2>
<div class="tw"><table>
<tr><th>Роль</th><th>Что из этого следует</th></tr>
<tr><td><b>Единица порядка</b></td><td>записи упорядочены внутри партиции и <b>только</b> внутри неё</td></tr>
<tr><td><b>Единица параллелизма</b></td><td>одну партицию в группе читает ровно один потребитель; больше потребителей, чем партиций, — лишние простаивают</td></tr>
<tr><td><b>Единица размещения</b></td><td>партиция целиком лежит на брокере (и её реплики — на других)</td></tr>
</table></div>

<h2>Сколько партиций делать</h2>
<p>Число партиций задаёт потолок параллелизма группы: десять партиций — максимум десять работающих потребителей. Поэтому берут с запасом относительно текущей нагрузки.</p>
<div class="note warn"><b class="hd">Но не «побольше на всякий случай»</b><p>Каждая партиция — это открытые файлы, память под буферы у продюсера, отдельные запросы репликации и запись в разные места диска. Тысячи партиций на брокер увеличивают время выборов лидеров и восстановления после сбоя. Разумная отправная точка — десятки, а не тысячи.</p></div>

<h2>Партиции можно только добавлять</h2>
<pre><code>kafka-topics --alter --topic orders --partitions 6</code></pre>
<div class="note trap"><b class="hd">Добавление партиций ломает привязку ключа</b><p>Партиция для ключа считается как <code>murmur2(key) % число_партиций</code>. Меняется знаменатель — меняется результат: события одного пользователя, раньше ложившиеся в партицию 2, начинают попадать в партицию 5. Порядок между старыми и новыми событиями этого ключа больше не гарантирован.</p><p>Уменьшить число партиций нельзя вовсе: пришлось бы куда-то девать уже записанные данные и их смещения. Если нужно меньше — создают новый топик и переливают данные.</p></div>

<h2>Смещения, отметки и границы</h2>
<div class="tw"><table>
<tr><th>Величина</th><th>Смысл</th></tr>
<tr><td><code>logStartOffset</code></td><td>самое раннее доступное смещение; растёт, когда старое удаляется по сроку хранения</td></tr>
<tr><td><code>LEO</code> (log end offset)</td><td>смещение, которое получит следующая записанная запись</td></tr>
<tr><td><code>HW</code> (high watermark)</td><td>граница, до которой данные есть во всех репликах ISR; <b>только до неё потребителям видно</b></td></tr>
</table></div>
<p>Разрыв между HW и LEO — это записи, принятые лидером, но ещё не размноженные по репликам. Их не видно снаружи именно потому, что при падении лидера они могут исчезнуть. Подробнее об этом — в модулях о гарантиях записи и репликации.</p>
`,
quiz:[
 {q:"Где Kafka гарантирует порядок записей?",
  opts:["Во всём топике","Внутри одной партиции","Внутри группы потребителей","В пределах одного брокера"],
  a:1, why:"Между партициями порядка нет вовсе: они пишутся и читаются независимо и параллельно. Нужен порядок для сущности — кладите её события в одну партицию через общий ключ."},
 {q:"В топике 4 партиции, в группе 6 потребителей. Что произойдёт?",
  opts:["Партиции поделятся дробно","Два потребителя останутся без партиций и будут простаивать","Возникнет ошибка","Kafka добавит партиции"],
  a:1, why:"Партиция в группе принадлежит ровно одному потребителю. Число партиций — жёсткий потолок параллелизма группы."},
 {q:"Что означает высокая отметка (high watermark)?",
  opts:["Максимальный размер партиции","Граница, до которой данные есть во всех репликах ISR и видны потребителям","Смещение последней зафиксированной записи группы","Порог срабатывания очистки"],
  a:1, why:"Записи выше отметки существуют у лидера, но ещё не размножены. Потребителям их не показывают, потому что при сбое лидера они могут пропасть."},
 {q:"Можно ли уменьшить число партиций у топика?",
  opts:["Да, командой --alter","Нет: пришлось бы куда-то девать уже записанные данные и их смещения","Да, но только на пустом топике","Да, при остановленном кластере"],
  a:1, why:"Kafka такой операции не предоставляет. Уменьшение делают переливкой в новый топик с нужным числом партиций."},
 {q:"Чем опасно добавление партиций к работающему топику?",
  opts:["Придётся перезапустить брокеры","Меняется <code>murmur2(key) % N</code> — ключ переезжает в другую партицию, и порядок для него рвётся","Старые записи будут удалены","Потребители потеряют смещения"],
  a:1, why:"Старые записи ключа остаются в прежней партиции, новые уходят в другую, и относительный порядок между ними больше ничем не гарантирован."},
 {q:"Записи с одинаковым смещением 5 в партициях 0 и 1 — что их связывает?",
  opts:["Записаны одновременно","Ничего: нумерация независима","Имеют одинаковый ключ","Лежат на одном брокере"],
  a:1, why:"Смещение уникально и осмысленно только в пределах своей партиции."}
],
labs:[
 {id:"2a", title:"Разложить по партициям",
  brief:"<ul><li>Создайте топик <code>events</code> с 3 партициями и репликацией 1</li><li>Запишите три события <b>без ключа</b>: <code>produce --topic events --records a,b,c</code></li><li>Посмотрите <code>dump --topic events</code> — они разошлись по разным партициям</li><li>Запишите три события с <b>одинаковым</b> ключом: <code>--records u1:x,u1:y,u1:z</code></li><li>Снова загляните в <code>dump</code>: все три легли в одну партицию, подряд</li></ul>",
  hint:"Без ключа продюсер раскладывает записи по кругу. С ключом партиция определяется как murmur2(key) % число партиций — для одного ключа она всегда одна.",
  checks:[
   {label:"Топик <code>events</code> создан с 3 партициями", test:st=>{const t=topic(st,"events"); return !!t&&t.partitions.length===3;}},
   {label:"Записи без ключа разошлись по трём партициям", test:st=>{const t=topic(st,"events"); return !!t&&t.partitions.filter(p=>p.log.some(r=>r.key===null)).length===3;}},
   {label:"Три записи с ключом <code>u1</code> записаны", test:st=>allRecs(st,"events").filter(r=>r.key==="u1").length>=3},
   {label:"Все записи ключа <code>u1</code> в одной партиции", test:st=>{const t=topic(st,"events"); if(!t) return false;
     const ps=t.partitions.filter(p=>p.log.some(r=>r.key==="u1")); return ps.length===1&&ps[0].log.filter(r=>r.key==="u1").length>=3;}},
   {label:"Журнал просмотрен через <code>dump</code>", test:st=>okRan(st,/^\s*dump\b/)}
  ]},
 {id:"2b", title:"Границы журнала",
  brief:"<ul><li>Посмотрите в <code>dump --topic events</code> значения <code>logStart</code>, <code>LEO</code> и <code>HW</code></li><li>Убедитесь, что смещения в разных партициях нумеруются с нуля независимо</li><li>Попробуйте уменьшить число партиций: <code>kafka-topics --alter --topic events --partitions 2</code> — получите отказ</li><li>Увеличьте до 6 и прочитайте предупреждение о привязке ключа</li><li>Запишите ещё три события с ключом <code>u1</code> и проверьте в <code>dump</code>, в какую партицию они попали теперь</li></ul>",
  hint:"После расширения топика murmur2('u1') % 6 может дать не ту партицию, что murmur2('u1') % 3.",
  checks:[
   {label:"Уменьшение числа партиций отклонено", test:st=>errRan(st,/только увеличивать/)},
   {label:"Число партиций увеличено до 6", test:st=>{const t=topic(st,"events"); return !!t&&t.partitions.length===6;}},
   {label:"Предупреждение о смене партиции для ключа показано", test:st=>outHas(st,/распределение по ключу изменилось/)},
   {label:"После расширения записан ещё один пакет с ключом <code>u1</code>", test:st=>allRecs(st,"events").filter(r=>r.key==="u1").length>=6},
   {label:"Смещения в партициях начинаются с нуля независимо", test:st=>{const t=topic(st,"events"); if(!t) return false;
     const used=t.partitions.filter(p=>p.log.length); return used.length>=2&&used.every(p=>p.log[0].offset===0);}}
  ]}
]
},
