/* ------------------------------------------------ 9 */
{
n:9, id:"store", title:"Хранение и очистка", sub:"Срок жизни, сегменты, уплотнение",
lede:"Сколько данные лежат в Kafka, как удаляется старое и чем уплотнение по ключу отличается от удаления по времени.",
theory:`
<p>Kafka — не бесконечное хранилище. Данные лежат ровно столько, сколько задано политикой очистки, и удаляются независимо от того, прочитал их кто-нибудь или нет.</p>
<div class="note trap"><b class="hd">Срок хранения не связан с потреблением</b><p>Записи исчезнут по истечении <code>retention.ms</code>, даже если ни одна группа их не читала. И наоборот: прочитанные записи никуда не денутся до срока. Отставшая группа, чей лаг превысил объём хранения, получит <code>OffsetOutOfRange</code> и, в зависимости от <code>auto.offset.reset</code>, либо прыгнет в начало, либо в конец, — то есть либо перечитает всё, либо молча пропустит непрочитанное.</p></div>

<h2>Сегменты</h2>
<p>Партиция физически состоит из <b>сегментов</b> — файлов фиксированного размера. Пишут всегда в последний, <b>активный</b> сегмент; когда он заполняется до <code>segment.bytes</code> или живёт дольше <code>segment.ms</code>, он закрывается и начинается новый.</p>
<figure class="fig">
<svg viewBox="0 0 620 156" role="img" aria-label="Сегменты партиции и удаление по сроку">
 <g font-family="var(--fm)" font-size="10">
  <text x="0" y="12" font-size="9.5" fill="var(--ink3)">партиция = цепочка сегментов</text>
  ${[["00000.log","0–999","истёк","var(--trap-s)","var(--trap)"],
     ["01000.log","1000–1999","истёк","var(--trap-s)","var(--trap)"],
     ["02000.log","2000–2999","хранится","var(--surf3)","var(--line)"],
     ["03000.log","3000–…","активный","var(--ok-s)","var(--ok)"]].map((s,i)=>{
    const x = i*152;
    return '<rect x="'+x+'" y="24" width="144" height="46" rx="3" fill="'+s[3]+'" stroke="'+s[4]+'"/>'+
      '<text x="'+(x+72)+'" y="42" text-anchor="middle" font-size="9.5" fill="var(--ink)">'+s[0]+'</text>'+
      '<text x="'+(x+72)+'" y="55" text-anchor="middle" font-size="9" fill="var(--ink3)">смещения '+s[1]+'</text>'+
      '<text x="'+(x+72)+'" y="66" text-anchor="middle" font-size="8.5" fill="'+s[4]+'">'+s[2]+'</text>';
  }).join("")}
  <path d="M0 82h296" stroke="var(--trap)" stroke-width="1.2"/><path d="M0 82l0 0" fill="none"/>
  <text x="0" y="98" font-size="9" fill="var(--trap)">удаляются целиком, файлами — не по одной записи</text>
  <text x="456" y="98" font-size="9" fill="var(--ok)">активный не удаляется никогда</text>
  <text x="0" y="122" font-size="9" fill="var(--ink2)">logStartOffset после удаления становится 2000: смещения 0–1999 больше недоступны.</text>
  <text x="0" y="136" font-size="9" fill="var(--ink2)">Смещения оставшихся записей не меняются — нумерация сквозная и неизменная.</text>
  <text x="0" y="152" font-size="9" fill="var(--ink3)">Поэтому фактический объём хранения всегда чуть больше заданного: он кратен сегменту.</text>
 </g>
</svg>
<figcaption>Удаление посегментное объясняет, почему <code>retention.ms=60000</code> не означает «ровно минуту»: сегмент уйдёт, когда устареет вся его последняя запись.</figcaption>
</figure>

<h2>Две политики очистки</h2>
<div class="tw"><table>
<tr><th><code>cleanup.policy</code></th><th>Что делает</th><th>Для чего</th></tr>
<tr><td><code>delete</code> (умолчание)</td><td>удаляет сегменты старше <code>retention.ms</code> или сверх <code>retention.bytes</code></td><td>поток событий: заказы, клики, метрики</td></tr>
<tr><td><code>compact</code></td><td>оставляет последнее значение для каждого ключа, старые версии удаляет</td><td>текущее состояние: профиль пользователя, остаток на складе</td></tr>
<tr><td><code>compact,delete</code></td><td>уплотняет и вдобавок удаляет по сроку</td><td>состояние с ограниченным сроком жизни</td></tr>
</table></div>

<h2>Уплотнение</h2>
<p>Уплотнённый топик — это, по сути, таблица «ключ → последнее значение», развёрнутая во времени. Прочитав его с начала, вы восстановите текущее состояние всех ключей. На этом построены хранилища состояния Kafka Streams и топик <code>__consumer_offsets</code>.</p>
<div class="tw"><table>
<tr><th>Понятие</th><th>Смысл</th></tr>
<tr><td><b>надгробие</b> (tombstone)</td><td>запись с ключом и значением <code>null</code>: после уплотнения ключ исчезает совсем</td></tr>
<tr><td><code>delete.retention.ms</code></td><td>сколько надгробие живёт после уплотнения, чтобы отставшие потребители успели увидеть удаление</td></tr>
<tr><td><b>смещения</b></td><td>сохраняются: в журнале появляются дыры, но нумерация не сдвигается</td></tr>
</table></div>
<div class="note warn"><b class="hd">Активный сегмент не уплотняется — и это регулярно удивляет</b><p>Уплотнение работает только с закрытыми сегментами. Свежие записи, ещё лежащие в активном сегменте, остаются как есть — вместе со своими предыдущими версиями из уже закрытых сегментов, если сборщик до них не дошёл. Поэтому сразу после записи в уплотнённом топике вполне может быть видно две версии одного ключа. Это не ошибка, а нормальная работа: гарантируется, что <b>последняя</b> версия ключа не исчезнет никогда.</p><p>Записи без ключа уплотнение не трогает вовсе — их просто нечем сравнивать.</p></div>

<h2>Сколько это стоит на диске</h2>
<p>Оценка простая: <code>объём = скорость_потока × retention × replication.factor</code>. Поток в 10 МБ/с при недельном хранении и трёх копиях — это около 18 ТБ. Сжатие на стороне продюсера (<code>zstd</code>, <code>lz4</code>) обычно сокращает объём в 3–5 раз для текстовых данных и применяется почти всегда.</p>
`,
quiz:[
 {q:"Записи удалятся по истечении <code>retention.ms</code>, если их никто не прочитал?",
  opts:["Нет, Kafka дождётся всех групп","Да: срок хранения не связан с потреблением","Только если нет активных групп","Только при cleanup.policy=compact"],
  a:1, why:"Отставшая группа получит OffsetOutOfRange и, в зависимости от auto.offset.reset, либо перечитает всё с начала, либо молча пропустит потерянное."},
 {q:"Какими порциями удаляются данные при <code>cleanup.policy=delete</code>?",
  opts:["По одной записи","Целыми сегментами — файлами","По партициям","По топикам"],
  a:1, why:"Поэтому фактический объём хранения всегда чуть больше заданного и кратен размеру сегмента, а активный сегмент не удаляется никогда."},
 {q:"Что делает <code>cleanup.policy=compact</code>?",
  opts:["Сжимает данные архиватором","Оставляет последнее значение для каждого ключа","Удаляет старые сегменты","Объединяет партиции"],
  a:1, why:"Получается «таблица» ключ → текущее значение. На этом построены хранилища состояния Kafka Streams и топик __consumer_offsets."},
 {q:"Что такое надгробие (tombstone)?",
  opts:["Запись об ошибке","Запись с ключом и значением <code>null</code>: уплотнение удалит ключ совсем","Метка конца партиции","Признак удалённого топика"],
  a:1, why:"Надгробие живёт ещё delete.retention.ms после уплотнения, чтобы отставшие потребители успели увидеть сам факт удаления."},
 {q:"Что происходит со смещениями после уплотнения?",
  opts:["Пересчитываются подряд","Сохраняются: в журнале появляются дыры","Обнуляются","Сдвигаются на число удалённых"],
  a:1, why:"Смещение записи неизменно с момента записи. Именно поэтому после уплотнения журнал может выглядеть как 2, 5, 9, 10."},
 {q:"В уплотнённом топике сразу после записи видно две версии одного ключа. Это ошибка?",
  opts:["Да, сборщик сломан","Нет: активный сегмент не уплотняется","Да, нужен перезапуск брокера","Нет, но данные будут потеряны"],
  a:1, why:"Уплотнение работает только с закрытыми сегментами. Гарантируется лишь то, что последняя версия ключа не исчезнет никогда."}
],
labs:[
 {id:"9a", title:"Срок хранения",
  brief:"<ul><li>Создайте топик <code>metrics</code> с 1 партицией и репликацией 1</li><li>Задайте маленькие значения: <code>kafka-configs --alter --topic metrics --add-config retention.ms=60000,segment.bytes=2</code></li><li>Запишите 6 событий и посмотрите <code>dump</code>: <code>logStart=0</code></li><li>Сдвиньте условное время: <code>advance-time 10m</code></li><li>Снова загляните в <code>dump</code>: старые сегменты удалены, <code>logStart</code> вырос, но смещения оставшихся записей не изменились</li><li>Убедитесь, что активный сегмент на месте — журнал не опустел</li></ul>",
  hint:"segment.bytes здесь считается в записях, а не в байтах — чтобы эффект был виден на шести событиях.",
  checks:[
   {label:"Топик <code>metrics</code> настроен на короткий срок хранения", test:st=>{const t=topic(st,"metrics"); return !!t&&t.config["retention.ms"]<=60000;}},
   {label:"Записано не меньше 6 событий", test:st=>st.log.some(e=>!e.err&&/produce[\s\S]*metrics/.test(e.cmd))},
   {label:"Условное время сдвинуто", test:st=>okRan(st,/^\s*advance-time/)},
   {label:"Старые сегменты удалены: <code>logStart</code> вырос", test:st=>{const t=topic(st,"metrics"); return !!t&&t.partitions[0].logStartOffset>0;}},
   {label:"Активный сегмент уцелел — журнал не пуст", test:st=>{const t=topic(st,"metrics"); return !!t&&t.partitions[0].log.length>0;}},
   {label:"Смещения оставшихся записей не пересчитаны", test:st=>{const t=topic(st,"metrics"); if(!t) return false;
     const p=t.partitions[0]; return p.log.length>0&&p.log[0].offset===p.logStartOffset&&p.logStartOffset>0;}}
  ]},
 {id:"9b", title:"Уплотнение по ключу",
  brief:"<ul><li>Создайте топик <code>profile</code> с 1 партицией, репликация 1</li><li>Включите уплотнение: <code>--add-config cleanup.policy=compact,segment.bytes=1</code></li><li>Запишите историю изменений: <code>--records u2:alice,u1:name1,u1:name2,u3:bob --partition 0</code></li><li>Выполните <code>compact --topic profile</code> и посмотрите <code>dump</code>: смещения сохранились, появились дыры</li><li>Удалите ключ надгробием: <code>produce --topic profile --key u2 --value null --partition 0</code></li><li>Запишите ещё одну запись, чтобы надгробие ушло из активного сегмента, и снова выполните <code>compact</code></li><li>Убедитесь, что ключ <code>u2</code> исчез из журнала</li></ul>",
  hint:"Уплотнение не трогает активный сегмент, поэтому после надгробия нужна ещё одна запись, чтобы оно попало в уплотняемую часть.",
  checks:[
   {label:"У топика <code>profile</code> включено уплотнение", test:st=>{const t=topic(st,"profile"); return !!t&&String(t.config["cleanup.policy"]).indexOf("compact")>=0;}},
   {label:"Записана история изменений ключа <code>u1</code>", test:st=>st.log.some(e=>!e.err&&/produce[\s\S]*profile/.test(e.cmd))},
   {label:"Уплотнение выполнено", test:st=>okRan(st,/^\s*compact\b/)},
   {label:"В журнале появились дыры в смещениях", test:st=>{const t=topic(st,"profile"); if(!t) return false;
     const o=t.partitions[0].log.map(r=>r.offset); return o.length>1&&o.some((v,i)=>i>0&&v!==o[i-1]+1);}},
   {label:"Записано надгробие для <code>u2</code>", test:st=>st.log.some(e=>!e.err&&/produce[\s\S]*--key\s+u2[\s\S]*--value\s+null/.test(e.cmd))},
   {label:"После повторного уплотнения ключ <code>u2</code> исчез", test:st=>{const t=topic(st,"profile"); return !!t&&
     st.log.filter(e=>!e.err&&/^\s*compact\b/.test(e.cmd)).length>=2&&!t.partitions[0].log.some(r=>r.key==="u2");}}
  ]}
]
},

/* ------------------------------------------------ 10 */
{
n:10, id:"schema", title:"Формат и схемы", sub:"Сериализация и совместимость",
lede:"Что на самом деле лежит в значении записи, зачем нужен реестр схем и какие изменения схемы безопасны, а какие ломают потребителей.",
theory:`
<p>Для Kafka ключ и значение — просто массивы байтов. Брокер не знает и не проверяет, что внутри. Смысл им придают сериализаторы на стороне клиентов, и договориться о формате должны сами продюсер с потребителем.</p>

<h2>Чем сериализуют</h2>
<div class="tw"><table>
<tr><th>Формат</th><th>Плюсы</th><th>Минусы</th></tr>
<tr><td><b>JSON</b></td><td>читается глазами, нет инструментов</td><td>объёмный, схема нигде не закреплена, типы неточны</td></tr>
<tr><td><b>Avro</b></td><td>компактный, схема обязательна, эволюция продумана</td><td>нужен реестр схем</td></tr>
<tr><td><b>Protobuf</b></td><td>компактный, схема в коде, знаком по gRPC</td><td>эволюция строже, чем кажется</td></tr>
<tr><td><b>JSON Schema</b></td><td>компромисс: читаемо и проверяемо</td><td>объём остаётся большим</td></tr>
</table></div>

<h2>Реестр схем</h2>
<p>Реестр (Schema Registry) хранит схемы и выдаёт им числовые идентификаторы. Продюсер записывает в начало значения идентификатор схемы, потребитель по нему запрашивает схему и разбирает данные. Схема передаётся один раз, а не с каждым сообщением.</p>
<figure class="fig">
<svg viewBox="0 0 620 152" role="img" aria-label="Работа реестра схем">
 <g font-family="var(--fm)" font-size="10">
  <rect x="0" y="26" width="120" height="34" rx="3" fill="var(--surf3)" stroke="var(--line)"/>
  <text x="60" y="47" text-anchor="middle" font-size="9.5" fill="var(--ink)">продюсер</text>
  <rect x="250" y="26" width="120" height="34" rx="3" fill="var(--key-s)" stroke="var(--key)"/>
  <text x="310" y="41" text-anchor="middle" font-size="9.5" fill="var(--ink)">реестр схем</text>
  <text x="310" y="53" text-anchor="middle" font-size="8.5" fill="var(--ink3)">схема → id</text>
  <rect x="500" y="26" width="120" height="34" rx="3" fill="var(--surf3)" stroke="var(--line)"/>
  <text x="560" y="47" text-anchor="middle" font-size="9.5" fill="var(--ink)">потребитель</text>
  <path d="M124 36h122" stroke="var(--key)" stroke-width="1.2"/><path d="M246 36l-7-4v8z" fill="var(--key)"/>
  <text x="185" y="30" text-anchor="middle" font-size="8.5" fill="var(--key)">регистрирует</text>
  <path d="M496 43h-122" stroke="var(--key)" stroke-width="1.2"/><path d="M374 43l7-4v8z" fill="var(--key)"/>
  <text x="435" y="37" text-anchor="middle" font-size="8.5" fill="var(--key)">запрашивает по id</text>
  <rect x="0" y="84" width="620" height="30" rx="3" fill="var(--acc-s)" stroke="var(--acc-l)"/>
  <text x="10" y="97" font-size="9" fill="var(--ink)">запись в топике:  [магический байт][id схемы — 4 байта][данные в двоичном виде]</text>
  <text x="10" y="109" font-size="8.5" fill="var(--ink3)">схема не дублируется в каждом сообщении — только ссылка на неё</text>
  <text x="0" y="136" font-size="9" fill="var(--ink3)">Реестр проверяет совместимость новой версии со старыми и отклоняет ломающие изменения при регистрации,</text>
  <text x="0" y="148" font-size="9" fill="var(--ink3)">то есть ошибка обнаруживается при выкладке продюсера, а не у потребителя в проде.</text>
 </g>
</svg>
<figcaption>Главная ценность реестра — не экономия байтов, а запрет на несовместимые изменения до того, как они попадут в топик.</figcaption>
</figure>

<h2>Виды совместимости</h2>
<div class="tw"><table>
<tr><th>Режим</th><th>Что гарантирует</th><th>Кого обновлять первым</th></tr>
<tr><td><code>BACKWARD</code> (умолчание)</td><td>новая схема читает данные, записанные старой</td><td>потребителей</td></tr>
<tr><td><code>FORWARD</code></td><td>старая схема читает данные, записанные новой</td><td>продюсеров</td></tr>
<tr><td><code>FULL</code></td><td>и то и другое</td><td>в любом порядке</td></tr>
<tr><td><code>NONE</code></td><td>ничего</td><td>—</td></tr>
</table></div>
<div class="note ok"><b class="hd">Правило, покрывающее большинство случаев</b><p>Добавляйте поля <b>только со значением по умолчанию</b> и никогда не удаляйте и не переименовывайте существующие. Переименование = удаление плюс добавление, то есть ломающее изменение. Нужно другое имя — заведите новое поле, переведите потребителей, и только потом удаляйте старое.</p></div>

<h2>Ключ тоже сериализуется</h2>
<div class="note trap"><b class="hd">Смена сериализатора ключа переносит все ключи в другие партиции</b><p>Партиция считается от <b>байтов</b> ключа. Строка <code>"42"</code> в UTF-8 и число <code>42</code> в Avro — разные байты и, скорее всего, разные партиции. Смена сериализатора ключа на живом топике рвёт порядок для всех сущностей сразу — эффект тот же, что от увеличения числа партиций, только незаметнее.</p><p>Поэтому ключи почти всегда делают простыми строками и не меняют их формат никогда.</p></div>
`,
quiz:[
 {q:"Что брокер Kafka знает о содержимом значения записи?",
  opts:["Разбирает JSON","Ничего: для него это массив байтов","Проверяет по схеме","Знает тип из заголовка"],
  a:1, why:"Смысл байтам придают сериализаторы клиентов. Именно поэтому договорённость о формате — задача команд, а не брокера."},
 {q:"Что записывается в начало значения при работе с реестром схем?",
  opts:["Вся схема","Магический байт и числовой идентификатор схемы","Имя топика","Контрольная сумма"],
  a:1, why:"Схема передаётся один раз при регистрации, а в каждом сообщении едет только ссылка на неё — пять байтов вместо килобайтов."},
 {q:"Режим <code>BACKWARD</code>: кого обновляют первым?",
  opts:["Продюсеров","Потребителей","Порядок не важен","Брокеры"],
  a:1, why:"BACKWARD означает, что новая схема прочитает старые данные. Сначала переводят читателей, потом писателей."},
 {q:"Какое изменение схемы безопасно почти всегда?",
  opts:["Переименование поля","Добавление поля со значением по умолчанию","Удаление обязательного поля","Смена типа с int на string"],
  a:1, why:"Значение по умолчанию позволяет читателю восстановить поле в данных, где его нет. Переименование — это удаление плюс добавление, то есть ломающее изменение."},
 {q:"Чем опасна смена сериализатора ключа на работающем топике?",
  opts:["Вырастет объём данных","Изменятся байты ключа — и все ключи переедут в другие партиции","Потребители не смогут подключиться","Ничем"],
  a:1, why:"Эффект тот же, что от увеличения числа партиций: порядок для существующих сущностей рвётся, но заметить это гораздо труднее."},
 {q:"Главная польза реестра схем — это…",
  opts:["Экономия трафика","Отклонение несовместимых схем при регистрации, до попадания данных в топик","Ускорение сериализации","Шифрование данных"],
  a:1, why:"Ошибка обнаруживается при выкладке продюсера, а не у потребителя на проде — и это ценнее сэкономленных байтов."}
],
labs:[
 {id:"10a", title:"Ключ — это байты",
  brief:"<p>Покажите себе, что партиция считается от байтов ключа.</p><ul><li>Создайте топик <code>ser</code> с 4 партициями, репликация 1</li><li>Запишите записи с ключом-строкой: <code>--records 42:a,42:b</code></li><li>Теперь ключи, выглядящие так же, но состоящие из других байтов: <code>produce --topic ser --key \" 42\" --value c</code> и <code>produce --topic ser --key \"42 \" --value d</code></li><li>Посмотрите <code>dump</code>: похожие с виду ключи попали в разные партиции</li><li>Проверьте это же для <code>u1</code> и <code>U1</code> — регистр тоже меняет байты</li></ul>",
  hint:"Ключи с пробелами берите в кавычки и задавайте через --key: produce --topic ser --key \" 42\" --value c",
  checks:[
   {label:"Топик <code>ser</code> создан с 4 партициями", test:st=>{const t=topic(st,"ser"); return !!t&&t.partitions.length===4;}},
   {label:"Записан ключ <code>42</code>", test:st=>allRecs(st,"ser").some(r=>r.key==="42")},
   {label:"Записаны ключи с другими байтами", test:st=>allRecs(st,"ser").some(r=>r.key!==null&&r.key!=="42"&&r.key.replace(/\s/g,"")==="42")},
   {label:"Похожие ключи оказались в разных партициях", test:st=>{const t=topic(st,"ser"); if(!t) return false;
     const ps=new Set(); t.partitions.forEach(p=>p.log.forEach(r=>{ if(r.key!==null&&r.key.replace(/\s/g,"")==="42") ps.add(p.id); }));
     return ps.size>=2;}},
   {label:"Проверена разница регистра в ключе", test:st=>{const rs=allRecs(st,"ser"); return rs.some(r=>r.key==="u1")&&rs.some(r=>r.key==="U1");}}
  ]},
 {id:"10b", title:"Эволюция формата",
  brief:"<p>Смоделируйте переход на новую версию формата без остановки потребителей.</p><ul><li>Создайте топик <code>users</code> с 2 партициями, репликация 1</li><li>Запишите две записи «старого формата»: <code>--records u1:v1|alice,u2:v1|bob</code></li><li>Запишите две записи «нового формата» с добавленным полем: <code>--records u3:v2|carol|ru,u4:v2|dave|en</code></li><li>Прочитайте топик с начала с показом ключей: <code>kafka-console-consumer --topic users --from-beginning --property print.key=true</code></li><li>Убедитесь, что обе версии лежат в одном топике и читаются — совместимость обеспечивает потребитель, а не брокер</li></ul>",
  hint:"Вертикальную черту в значении можно писать как есть: produce --topic users --records u1:v1|alice",
  checks:[
   {label:"Топик <code>users</code> создан", test:st=>!!topic(st,"users")},
   {label:"Записи старого формата на месте", test:st=>allRecs(st,"users").some(r=>/^v1\|/.test(String(r.value)))},
   {label:"Записи нового формата на месте", test:st=>allRecs(st,"users").some(r=>/^v2\|/.test(String(r.value)))},
   {label:"Обе версии сосуществуют в одном топике", test:st=>{const rs=allRecs(st,"users");
     return rs.some(r=>/^v1\|/.test(String(r.value)))&&rs.some(r=>/^v2\|/.test(String(r.value)));}},
   {label:"Прочитано с показом ключей", test:st=>okRan(st,/print\.key=true/)}
  ]}
]
},

/* ------------------------------------------------ 11 */
{
n:11, id:"ops", title:"Эксплуатация", sub:"Лаг, узкие места, симптомы",
lede:"На что смотреть, когда система работает; какие цифры сигнализируют о беде и что делать с растущим отставанием.",
theory:`
<h2>Лаг — главный показатель</h2>
<p><b>Лаг</b> партиции — разница между её концом и зафиксированным смещением группы: сколько записей потребитель ещё не обработал.</p>
<pre><code>kafka-consumer-groups --describe --group billing

TOPIC   PARTITION  CURRENT-OFFSET  LOG-END-OFFSET  LAG   CONSUMER-ID
orders  0          104521          104533          12    c-1
orders  1          98430           131002          32572 c-2</code></pre>
<div class="note ok"><b class="hd">Смотрят не на величину, а на производную</b><p>Лаг в 30 тысяч сам по себе не значит ничего: он может быть нормой для пакетной обработки и катастрофой для платежей. Тревожный признак — <b>устойчивый рост</b>: значит, потребление медленнее производства, и разрыв не закроется сам.</p><p>Второй важный признак — <b>перекос между партициями</b>. В примере выше партиция 1 отстаёт в три тысячи раз сильнее: почти наверняка либо горячий ключ, либо конкретный потребитель <code>c-2</code> застрял.</p></div>

<h2>Что делать с растущим лагом</h2>
<div class="tw"><table>
<tr><th>Причина</th><th>Как распознать</th><th>Что делать</th></tr>
<tr><td>потребителей меньше, чем партиций</td><td>у части участников по нескольку партиций</td><td>добавить экземпляров — до числа партиций</td></tr>
<tr><td>партиций меньше, чем нужно</td><td>потребителей уже столько же, сколько партиций</td><td>добавить партиций (помня о смене привязки ключей)</td></tr>
<tr><td>горячий ключ</td><td>лаг растёт в одной-двух партициях</td><td>дробить ключ или выносить нагрузку</td></tr>
<tr><td>медленная обработка</td><td>лаг растёт равномерно везде</td><td>оптимизировать обработчик, выносить внешние вызовы, батчить запись в базу</td></tr>
<tr><td>цикл перебалансировок</td><td>лаг скачет, поколение группы быстро растёт</td><td>уменьшить <code>max.poll.records</code>, поднять <code>max.poll.interval.ms</code></td></tr>
</table></div>
<div class="note trap"><b class="hd">Добавление потребителей сверх числа партиций не ускоряет ничего</b><p>Это первое, что пробуют, и это не работает: лишние экземпляры просто простаивают. Проверьте <code>--describe</code>: если у кого-то из участников пусто в назначении, узкое место не в потребителях.</p></div>

<h2>Что ещё смотрят</h2>
<div class="tw"><table>
<tr><th>Показатель</th><th>О чём говорит</th></tr>
<tr><td><code>UnderReplicatedPartitions</code></td><td>реплики отстают или брокер недоступен; в норме должен быть 0</td></tr>
<tr><td><code>OfflinePartitionsCount</code></td><td>партиции без лидера; в норме 0, любое другое значение — авария</td></tr>
<tr><td><code>ActiveControllerCount</code></td><td>по кластеру должен быть ровно 1</td></tr>
<tr><td>частота перебалансировок</td><td>рост означает нестабильных потребителей</td></tr>
<tr><td>распределение лидерства</td><td>перекос делает один брокер узким местом</td></tr>
</table></div>

<h2>Типичные аварии</h2>
<div class="tw"><table>
<tr><th>Симптом</th><th>Обычная причина</th></tr>
<tr><td>потребитель читает, но не продвигается</td><td>обработка падает на «ядовитой» записи, смещение не фиксируется</td></tr>
<tr><td>дубликаты после каждого развёртывания</td><td>смещения не фиксируются перед остановкой; повтор с последней фиксации</td></tr>
<tr><td>часть событий «пропала»</td><td><code>auto.offset.reset=latest</code> плюс лаг больше срока хранения</td></tr>
<tr><td>продюсер тормозит, растёт задержка</td><td>буфер полон, брокер не успевает; <code>acks=all</code> при усохшем ISR</td></tr>
<tr><td>после расширения топика поехал порядок</td><td>ключи переехали в другие партиции</td></tr>
</table></div>
`,
quiz:[
 {q:"Что такое лаг группы потребителей?",
  opts:["Задержка сети до брокера","Разница между концом партиции и зафиксированным смещением","Время обработки одной записи","Отставание реплики от лидера"],
  a:1, why:"Отставание реплики от лидера — другая величина, она относится к репликации. Лаг измеряется в записях и относится к потреблению."},
 {q:"Лаг стабильно держится на 50 тысячах и не растёт. Это авария?",
  opts:["Да, всегда","Не обязательно: важен рост, а не величина","Да, если больше 10 тысяч","Только при acks=all"],
  a:1, why:"Постоянный лаг означает, что потребление успевает за производством с некоторым буфером. Тревожен именно устойчивый рост."},
 {q:"В группе 6 потребителей и 6 партиций, лаг растёт. Поможет ли седьмой потребитель?",
  opts:["Да, ускорит на 17%","Нет: он просто будет простаивать","Да, если увеличить heartbeat","Да, при RoundRobinAssignor"],
  a:1, why:"Партиция принадлежит ровно одному участнику. Нужно либо ускорять обработку, либо добавлять партиции."},
 {q:"Лаг растёт только в одной партиции из двенадцати. Наиболее вероятная причина?",
  opts:["Сбой брокера","Горячий ключ или застрявший потребитель этой партиции","Нехватка диска","Неверный acks"],
  a:1, why:"Равномерная нагрузка дала бы равномерный лаг. Перекос по одной партиции указывает на перекос по ключу либо на конкретный экземпляр."},
 {q:"Каким должен быть <code>UnderReplicatedPartitions</code> в здоровом кластере?",
  opts:["Равен числу партиций","Ноль","Равен фактору репликации","Не важен"],
  a:1, why:"Любое ненулевое значение означает, что часть данных существует в меньшем числе копий, чем задумано, и кластер потерял запас прочности."},
 {q:"После каждого развёртывания сервиса появляются дубликаты обработки. Причина?",
  opts:["Ошибка продюсера","Смещения не фиксируются перед остановкой — обработка идёт с последней фиксации","Слишком много партиций","Неверная схема"],
  a:1, why:"При корректном завершении потребитель должен зафиксировать смещения. Иначе повторяется всё, что было обработано после последней автоматической фиксации."}
],
labs:[
 {id:"11a", title:"Прочитать лаг",
  brief:"<ul><li>Создайте топик <code>orders2</code> с 2 партициями, репликация 1</li><li>Запишите 10 событий с ключами, дающими перекос: 8 с ключом <code>ru</code> и 2 с ключом <code>en</code> (при необходимости подберите ключи, чтобы они попали в разные партиции)</li><li>Заведите группу <code>bill</code> с потребителем <code>c1</code>, <code>--reset earliest</code></li><li>Прочитайте <b>2</b> записи и зафиксируйте: <code>poll --max 2</code>, затем <code>commit</code></li><li>Посмотрите <code>kafka-consumer-groups --describe --group bill</code> и найдите строку с наибольшим LAG</li><li>Дочитайте до конца и убедитесь, что итоговый лаг стал нулевым</li></ul>",
  hint:"Если оба ключа попали в одну партицию, попробуйте другие: например ru и de.",
  checks:[
   {label:"Топик <code>orders2</code> создан с 2 партициями", test:st=>{const t=topic(st,"orders2"); return !!t&&t.partitions.length===2;}},
   {label:"Записано не меньше 10 событий", test:st=>allRecs(st,"orders2").length>=10},
   {label:"Нагрузка легла на партиции неравномерно", test:st=>{const t=topic(st,"orders2"); if(!t) return false;
     const a=t.partitions.map(p=>p.log.length); return Math.max.apply(null,a)>=2*Math.min.apply(null,a.filter(x=>x>0)).valueOf();}},
   {label:"Показан ненулевой лаг", test:st=>st.log.some(e=>!e.err&&/--describe[\s\S]*bill/.test(e.cmd)&&/итоговый лаг: [1-9]/.test(e.lines.join("\n")))},
   {label:"Лаг доведён до нуля", test:st=>st.log.some(e=>!e.err&&/--describe[\s\S]*bill/.test(e.cmd)&&/итоговый лаг: 0/.test(e.lines.join("\n")))}
  ]},
 {id:"11b", title:"Узкое место",
  brief:"<p>Определите, где предел масштабирования.</p><ul><li>В группе <code>bill</code> добавьте потребителей <code>c2</code> и <code>c3</code> на топик <code>orders2</code></li><li>Посмотрите <code>consumer list --group bill</code>: у топика 2 партиции, значит один участник останется без работы</li><li>Расширьте топик до 4 партиций и снова посмотрите распределение — простаивающих не осталось</li><li>Посмотрите <code>cluster</code> и убедитесь, что все партиции реплицированы</li><li>Остановите брокера и посмотрите, как <code>cluster</code> сообщает о неполном ISR</li><li>Поднимите брокера обратно</li></ul>",
  hint:"consumer list --group bill показывает назначения; cluster даёт сводку по кластеру целиком.",
  checks:[
   {label:"В группе <code>bill</code> три участника", test:st=>{const g=grp(st,"bill"); return !!g&&g.members.length>=3;}},
   {label:"До расширения кто-то простаивал", test:st=>outHas(st,/ничего не назначено/)},
   {label:"Топик расширен до 4 партиций", test:st=>{const t=topic(st,"orders2"); return !!t&&t.partitions.length>=4;}},
   {label:"После расширения простаивающих не осталось", test:st=>{const g=grp(st,"bill"); return !!g&&g.members.every(m=>m.assignment.length>0);}},
   {label:"Просмотрена сводка <code>cluster</code>", test:st=>outHas(st,/Кластер: брокеров/)},
   {label:"Все брокеры снова работают", test:st=>okRan(st,/broker\s+stop/)&&st.c.brokers.every(b=>b.alive)}
  ]}
]
},

/* ------------------------------------------------ 12 */
{
n:12, id:"choose", title:"Выбор и антипаттерны", sub:"Kafka против очередей",
lede:"Когда журнал уместен, когда лучше классическая очередь и какие решения регулярно оказываются ошибкой.",
theory:`
<h2>Kafka против брокеров очередей</h2>
<div class="tw"><table>
<tr><th></th><th>Kafka</th><th>RabbitMQ / SQS</th></tr>
<tr><td>модель</td><td>журнал: чтение не удаляет</td><td>очередь: подтверждение удаляет</td></tr>
<tr><td>доставка</td><td>потребитель тянет (pull)</td><td>брокер отдаёт (push) либо тянет</td></tr>
<tr><td>порядок</td><td>строгий внутри партиции</td><td>в RabbitMQ — внутри очереди, в SQS FIFO — внутри группы</td></tr>
<tr><td>повтор одного сообщения</td><td>нет: только сдвиг смещения всей партиции</td><td>да: индивидуальное подтверждение и возврат</td></tr>
<tr><td>приоритеты, отложенная доставка</td><td>нет</td><td>есть</td></tr>
<tr><td>перечитать историю</td><td>да, штатно</td><td>нет</td></tr>
<tr><td>число потребителей</td><td>ограничено числом партиций</td><td>практически не ограничено</td></tr>
<tr><td>маршрутизация</td><td>по топикам и партициям</td><td>гибкая: обменники, ключи маршрутизации, фильтры</td></tr>
</table></div>
<div class="note ok"><b class="hd">Короткое правило выбора</b><p><b>Журнал</b>, если событие интересно нескольким потребителям, историю нужно уметь перечитать, а поток велик и однороден.</p><p><b>Очередь</b>, если это задача для одного исполнителя, нужны приоритеты, отложенный повтор конкретного сообщения или сложная маршрутизация.</p><p>Сомневаетесь между «событие» и «команда»: событие — факт, который уже произошёл («заказ оплачен»), у него может быть сколько угодно читателей. Команда — просьба что-то сделать («отправь письмо»), у неё ровно один исполнитель. Факты — в журнал, команды — в очередь.</p></div>

<h2>Антипаттерны</h2>
<div class="tw"><table>
<tr><th>Что делают</th><th>Чем плохо</th><th>Как правильно</th></tr>
<tr><td>топик на каждую сущность или клиента</td><td>тысячи топиков и партиций, медленные выборы и восстановление</td><td>один топик, разделение по ключу или полю записи</td></tr>
<tr><td>Kafka как база данных</td><td>нет выборки по условию, только последовательное чтение</td><td>журнал — источник, состояние — в базе или уплотнённом топике</td></tr>
<tr><td>запрос-ответ через два топика</td><td>задержка, корреляция вручную, сложная отладка</td><td>синхронный вызов HTTP или gRPC</td></tr>
<tr><td>огромные сообщения (файлы, изображения)</td><td>раздувается журнал и трафик репликации</td><td>файл в объектное хранилище, в Kafka — ссылка</td></tr>
<tr><td>одна партиция ради глобального порядка</td><td>потолок в одного потребителя навсегда</td><td>порядок по ключу сущности, а не глобальный</td></tr>
<tr><td><code>auto.offset.reset=latest</code> в важном сервисе</td><td>при потере смещений непрочитанное молча пропадает</td><td><code>earliest</code> плюс идемпотентная обработка</td></tr>
<tr><td>бесконечный повтор упавшей записи</td><td>партиция стоит, лаг растёт</td><td>топик ошибок и продвижение смещения</td></tr>
</table></div>

<h2>Проверочный список перед запуском</h2>
<div class="tw"><table>
<tr><th>Вопрос</th><th>Разумный ответ по умолчанию</th></tr>
<tr><td>Фактор репликации?</td><td>3 для рабочей среды</td></tr>
<tr><td><code>min.insync.replicas</code>?</td><td>2 при rf=3</td></tr>
<tr><td><code>acks</code>?</td><td><code>all</code>, кроме заведомо необязательных данных</td></tr>
<tr><td>Ключ?</td><td>идентификатор сущности, для которой нужен порядок</td></tr>
<tr><td>Число партиций?</td><td>с запасом к пиковому числу потребителей, но десятки, а не тысячи</td></tr>
<tr><td>Срок хранения?</td><td>исходя из того, за сколько нужно уметь перечитать</td></tr>
<tr><td>Фиксация смещений?</td><td>ручная, после успешной обработки</td></tr>
<tr><td>Обработка идемпотентна?</td><td>да — иначе гарантии не работают</td></tr>
<tr><td>Что с «ядовитой» записью?</td><td>топик ошибок настроен заранее</td></tr>
<tr><td>Мониторинг?</td><td>лаг по партициям, недореплицированные партиции, частота перебалансировок</td></tr>
</table></div>
`,
quiz:[
 {q:"Нужно отправить письмо ровно одним исполнителем, с приоритетами и отложенным повтором. Что выбрать?",
  opts:["Kafka","Классическую очередь: RabbitMQ или SQS","Kafka с одной партицией","Kafka с транзакциями"],
  a:1, why:"Это команда, а не факт. Приоритеты и повтор отдельного сообщения — родные возможности очередей, которых в журнале нет."},
 {q:"Чем плох топик на каждого клиента?",
  opts:["Ничем, так делают","Тысячи топиков и партиций замедляют выборы лидеров и восстановление","Kafka не поддерживает больше 100 топиков","Растёт задержка записи"],
  a:1, why:"Разделение делают ключом или полем внутри записи, а не размножением топиков."},
 {q:"Зачем в Kafka кладут ссылку на файл вместо самого файла?",
  opts:["Kafka не принимает двоичные данные","Большие сообщения раздувают журнал и трафик репликации","Так требует реестр схем","Из-за ограничения в 1 КБ"],
  a:1, why:"Каждое сообщение размножается по репликам и хранится весь срок. Файл кладут в объектное хранилище, в топик — ссылку."},
 {q:"Чем плоха одна партиция ради глобального порядка?",
  opts:["Kafka это запрещает","Потолок навсегда — один потребитель, масштабировать нечем","Порядок всё равно не гарантирован","Растёт объём хранения"],
  a:1, why:"Глобальный порядок почти никогда не нужен на самом деле. Обычно достаточно порядка внутри сущности — его и обеспечивает ключ."},
 {q:"Чем опасен <code>auto.offset.reset=latest</code> в ответственном сервисе?",
  opts:["Ничем","При потере или устаревании смещений непрочитанное молча пропадёт","Приведёт к дублям","Увеличит лаг"],
  a:1, why:"Потребитель прыгнет в конец и продолжит с новых записей, не сообщив о пропуске. С earliest хуже другое — дубли, но они лечатся идемпотентностью."},
 {q:"Как отличить событие от команды?",
  opts:["По размеру","Событие — свершившийся факт с любым числом читателей; команда — просьба с одним исполнителем","По формату сериализации","По числу партиций"],
  a:1, why:"Факты естественно ложатся в журнал, команды — в очередь. Это простейший способ выбрать инструмент."}
],
labs:[
 {id:"12a", title:"Собрать по правилам",
  brief:"<p>Соберите топик так, как его собирают для рабочей среды.</p><ul><li>Создайте топик <code>payments</code>: 3 партиции, фактор репликации 3</li><li>Задайте <code>min.insync.replicas=2</code></li><li>Запишите три события с ключом-идентификатором заказа и <code>--acks all</code></li><li>Проверьте <code>kafka-topics --describe --topic payments</code>: ISR полон, лидеры распределены</li><li>Заведите группу <code>settle</code> с двумя потребителями и <code>--reset earliest</code></li><li>Прочитайте и зафиксируйте смещения; убедитесь, что итоговый лаг равен нулю</li><li>Остановите один брокер и убедитесь, что запись с <code>--acks all</code> по-прежнему проходит</li></ul>",
  hint:"rf=3 плюс min.insync.replicas=2 переживают падение одного брокера без потери данных и без остановки записи.",
  checks:[
   {label:"Топик <code>payments</code>: 3 партиции, репликация 3", test:st=>{const t=topic(st,"payments"); return !!t&&t.partitions.length===3&&t.rf===3;}},
   {label:"Задан <code>min.insync.replicas=2</code>", test:st=>{const t=topic(st,"payments"); return !!t&&t.config["min.insync.replicas"]===2;}},
   {label:"Записано с ключом и <code>acks=all</code>", test:st=>st.log.some(e=>!e.err&&/produce[\s\S]*payments[\s\S]*--acks\s+all/.test(e.cmd))&&allRecs(st,"payments").some(r=>r.key!==null)},
   {label:"В группе <code>settle</code> два потребителя", test:st=>{const g=grp(st,"settle"); return !!g&&g.members.length>=2;}},
   {label:"Смещения зафиксированы, лаг нулевой", test:st=>st.log.some(e=>!e.err&&/--describe[\s\S]*settle/.test(e.cmd)&&/итоговый лаг: 0/.test(e.lines.join("\n")))},
   {label:"Запись пережила падение брокера", test:st=>{const i=st.log.findIndex(e=>!e.err&&/broker\s+stop/.test(e.cmd));
     return i>=0&&st.log.slice(i).some(e=>!e.err&&/produce[\s\S]*payments[\s\S]*--acks\s+all/.test(e.cmd));}}
  ]},
 {id:"12b", title:"Разбор аварии",
  brief:"<p>Три ситуации — воспроизведите и объясните каждую.</p><ul><li><b>Пропавшие события.</b> Создайте топик <code>lost</code> (1 партиция), запишите 4 записи. Заведите группу <code>late</code> с потребителем и <code>--reset latest</code>. Выполните <code>poll</code> — вернётся пусто, хотя записи есть</li><li>Исправьте: уберите потребителя, сбросьте смещения группы в начало с <code>--execute</code>, верните потребителя и прочитайте всё</li><li><b>Простаивающие потребители.</b> В группе <code>late</code> добавьте ещё двух участников на топик с одной партицией и убедитесь, что двое без работы</li><li><b>Отказ записи.</b> Задайте <code>lost</code> настройку <code>min.insync.replicas=2</code> при rf=1 — команда отклонит значение, потому что запись стала бы невозможной</li></ul>",
  hint:"Сброс смещений работает только на неактивной группе: сначала consumer remove.",
  checks:[
   {label:"Топик <code>lost</code> создан, записи есть", test:st=>allRecs(st,"lost").length>=4},
   {label:"Потребитель с <code>--reset latest</code> ничего не прочитал", test:st=>st.log.some(e=>!e.err&&/^\s*poll/.test(e.cmd)&&/пусто/.test(e.lines.join("\n")))},
   {label:"Смещения сброшены в начало", test:st=>okRan(st,/--reset-offsets[\s\S]*lost[\s\S]*--execute/)},
   {label:"После сброса записи прочитаны", test:st=>{const i=st.log.findIndex(e=>!e.err&&/--reset-offsets[\s\S]*--execute/.test(e.cmd));
     return i>=0&&st.log.slice(i).some(e=>!e.err&&/^\s*poll/.test(e.cmd)&&/прочитано записей: [1-9]/.test(e.lines.join("\n")));}},
   {label:"Простаивающие потребители обнаружены", test:st=>{const g=grp(st,"late"); return !!g&&g.members.filter(m=>m.assignment.length===0).length>=2;}},
   {label:"Недопустимый <code>min.insync.replicas</code> отклонён", test:st=>errRan(st,/больше фактора репликации/)}
  ]}
]
}

];
