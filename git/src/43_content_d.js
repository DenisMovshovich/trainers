
/* ------------------------------------------------ 6 */
{
n:6, id:"rebase", title:"Перебазирование", sub:"Переписать, а не слить",
lede:"Rebase не двигает коммиты — он создаёт новые с тем же содержимым. Из этого одного факта следует и вся его польза, и единственное правило, которое нельзя нарушать.",
theory:`
<h2>Что делает <code>git rebase main</code></h2>
<pre><code>было:   A ← B ← C(main)
              ↖ D ← E(feature)

стало:  A ← B ← C(main) ← D' ← E'(feature)</code></pre>
<p>Git берёт коммиты вашей ветки, которых нет в <code>main</code>, и <b>проигрывает</b> их поверх её вершины по одному. <code>D'</code> и <code>E'</code> — новые коммиты: то же содержимое изменений, другой родитель, а значит и другой хеш. Старые <code>D</code> и <code>E</code> остаются в базе объектов, пока их держит reflog.</p>

<h2>Rebase против merge</h2>
<div class="tw"><table>
<tr><th></th><th><code>merge</code></th><th><code>rebase</code></th></tr>
<tr><td>История</td><td>ветвистая, видно, что было параллельно</td><td>линейная, как будто работали по очереди</td></tr>
<tr><td>Хеши</td><td>сохраняются</td><td>меняются</td></tr>
<tr><td>Конфликты</td><td>один раз, все сразу</td><td>по разу на каждый переносимый коммит</td></tr>
<tr><td>Безопасность для чужих клонов</td><td>полная</td><td>только для неопубликованного</td></tr>
</table></div>
<p>Частая рабочая схема: <code>rebase</code> внутри своей ветки, пока никто её не видел, чтобы привести в порядок; <code>merge</code> — когда вливаешь в общую.</p>

<h2>Золотое правило</h2>
<div class="note trap"><b class="hd">Не перебазируйте то, что уже видят другие</b><p>После rebase ваши коммиты получили новые хеши, а у коллеги в клоне лежат старые. Его git считает их разошедшимися историями и предложит слияние, после которого в общей ветке окажутся <b>обе</b> версии каждого коммита — дубли, которые потом долго разгребают. Отсюда правило: перебазировать можно свою ветку, которую никто не забирал, и общую — никогда.</p><p>Исключение, о котором стоит договориться заранее: личная ветка задачи, которую вы отправили только ради pull request. Её перебазировать нормально, но отправлять потом нужно через <code>--force-with-lease</code>.</p></div>

<h2>Интерактивное перебазирование</h2>
<pre><code>$ <span class="k">git rebase -i HEAD~3</span>

pick a1b2c3d feat: начало поиска
pick d4e5f6a wip
pick 7b8c9d0 опечатка</code></pre>
<p>Git открывает список коммитов от старых к новым, и вы решаете судьбу каждого:</p>
<div class="tw"><table>
<tr><th>Слово</th><th>Что делает</th></tr>
<tr><td><code>pick</code></td><td>взять как есть</td></tr>
<tr><td><code>reword</code></td><td>взять, но изменить сообщение</td></tr>
<tr><td><code>squash</code></td><td>слить с предыдущим, сообщения объединить</td></tr>
<tr><td><code>fixup</code></td><td>слить с предыдущим, сообщение выбросить</td></tr>
<tr><td><code>drop</code></td><td>выбросить коммит целиком</td></tr>
<tr><td><code>edit</code></td><td>остановиться на этом коммите, чтобы что-то поправить</td></tr>
</table></div>
<p>Порядок строк — это порядок коммитов: поменяли строки местами, поменялся и порядок в истории. Удалили строку — то же, что <code>drop</code>.</p>
<div class="note ok"><b class="hd">Здесь список редактируется прямо в интерфейсе</b><p>Настоящий git открывает текстовый редактор. В тренажёре команда <code>git rebase -i HEAD~3</code> открывает такой же список внизу: меняйте действия и порядок, затем «Применить».</p></div>

<h2>Что делать при конфликте</h2>
<pre><code>git status              <span class="c"># покажет, на каком коммите остановились</span>
<span class="c"># правим файл</span>
git add файл
git rebase --continue   <span class="c"># дальше</span>
git rebase --skip       <span class="c"># выбросить этот коммит</span>
git rebase --abort      <span class="c"># вернуть всё как было</span></code></pre>
<p><code>--abort</code> работает на любом шаге и возвращает ветку ровно туда, где она была. Это делает перебазирование гораздо менее страшной операцией, чем кажется.</p>

<h2><code>--onto</code>: перенести на другое основание</h2>
<pre><code><span class="c"># ветку ответвили не от той ветки</span>
git rebase --onto main release feature</code></pre>
<p>Читается как «взять коммиты <code>feature</code>, которых нет в <code>release</code>, и положить на <code>main</code>». Нужен именно там, где ветка отпочковалась от неправильного места, а обычный rebase утащил бы за собой лишние коммиты.</p>
`,
quiz:[
 {q:"Что делает <code>git rebase main</code> с коммитами вашей ветки?",
  opts:["Перемещает их","Создаёт новые коммиты с тем же содержимым и другими хешами","Сливает их в один","Удаляет их"],
  a:1, why:"У новых коммитов другой родитель, поэтому и другой хеш. Старые остаются в базе, пока их держит reflog."},
 {q:"Почему нельзя перебазировать общую ветку?",
  opts:["Git это запрещает","Хеши изменятся, у коллег останутся старые — истории разойдутся и появятся дубли","Это медленно","Потеряются файлы"],
  a:1, why:"После слияния разошедшихся историй в ветке окажутся обе версии каждого коммита."},
 {q:"Чем <code>fixup</code> отличается от <code>squash</code>?",
  opts:["Ничем","<code>fixup</code> выбрасывает сообщение коммита, <code>squash</code> объединяет сообщения","<code>fixup</code> удаляет изменения","<code>squash</code> работает только с двумя"],
  a:1, why:"Для правки «забыл точку с запятой» нужен именно <code>fixup</code> — её сообщение в истории ни к чему."},
 {q:"Как выбросить коммит при интерактивном перебазировании?",
  opts:["<code>git rebase --skip</code>","Написать <code>drop</code> или удалить строку из списка","<code>git revert</code>","Никак"],
  a:1, why:"<code>--skip</code> — для другого: пропустить коммит, который встал конфликтом во время выполнения."},
 {q:"Конфликт посреди перебазирования. Что вернёт всё как было?",
  opts:["<code>git rebase --continue</code>","<code>git rebase --abort</code>","<code>git reset --hard</code>","<code>git merge --abort</code>"],
  a:1, why:"Работает на любом шаге и возвращает ветку ровно туда, где она была."},
 {q:"Сколько раз придётся разрешать конфликты при rebase трёх коммитов?",
  opts:["Один раз","До трёх — по разу на каждый переносимый коммит","Ни разу","Зависит от размера"],
  a:1, why:"Это плата за линейную историю. Merge просит разрешить всё сразу и один раз."}
],
labs:[
 {id:"6a", title:"Линейная история",
  brief:"<p>Ветка <code>feature/фильтры</code> отстала: в <code>main</code> появились новые коммиты. Влить нужно без коммита слияния, чтобы история осталась линейной.</p><ul><li>перебазируйте <code>feature/фильтры</code> на <code>main</code>;</li><li>перейдите на <code>main</code> и влейте ветку — теперь это будет перемотка.</li></ul>",
  hint:"git switch feature/фильтры, git rebase main, git switch main, git merge feature/фильтры",
  setup: R => {
    sc(R, "chore: init", {"app.py": "print('привет')"});
    scNew(R, "feature/фильтры");
    sc(R, "feat: фильтр по дате", {"filters.py": "def by_date(x): return x"});
    scGo(R, "main");
    sc(R, "fix: поправить запуск", {"app.py": "def main():\n    print('привет')"});
    sc(R, "docs: описать запуск", {"README.md": "# webapp\n\nЗапуск: python app.py"});
  },
  checks:[
   {label:"Вы на <code>main</code>, и работа ветки здесь", test:st=>branchName(st.R) === "main" && files(st)["filters.py"] !== undefined},
   {label:"История линейна — коммитов слияния нет", test:st=>linear(st) && files(st)["filters.py"] !== undefined},
   {label:"Работа обеих веток на месте", test:st=>files(st)["filters.py"] && files(st)["README.md"]},
   {label:"Коммит фильтра оказался последним", test:st=>tipMsg(st) === "feat: фильтр по дате"},
   {label:"Использован <code>git rebase</code>", test:st=>okRan(st,/git\s+rebase/)}
  ]},
 {id:"6b", title:"Прибраться перед ревью",
  brief:"<p>В ветке накопилось четыре коммита, показывать их в таком виде стыдно:</p><pre><code>feat: поиск по каталогу\nwip\nопечатка\ndebug: временный вывод</code></pre><p>Приведите ветку в порядок интерактивным перебазированием от <code>HEAD~4</code>:</p><ul><li><code>wip</code> и <code>опечатка</code> — влить в первый коммит <b>без</b> их сообщений;</li><li><code>debug: временный вывод</code> — выбросить совсем;</li><li>должен остаться <b>один</b> коммит <code>feat: поиск по каталогу</code>.</li></ul>",
  hint:"git rebase -i HEAD~4 откроет список внизу: первому оставьте pick, двум следующим поставьте fixup, последнему — drop.",
  setup: R => {
    sc(R, "chore: init", {"app.py": "print('привет')"});
    sc(R, "feat: поиск по каталогу", {"search.py": "def find(q):\n    return []"});
    sc(R, "wip", {"search.py": "def find(q):\n    return [x for x in ITEMS if q in x]"});
    sc(R, "опечатка", {"search.py": "def find(q):\n    return [x for x in ITEMS if q in x]\n\nITEMS = []"});
    sc(R, "debug: временный вывод", {"search.py": "def find(q):\n    print('отладка', q)\n    return [x for x in ITEMS if q in x]\n\nITEMS = []", "debug.log": "шум"});
  },
  checks:[
   {label:"От четырёх коммитов остался один", test:st=>history(st.R, headId(st.R)).length === 2},
   {label:"Его сообщение — <code>feat: поиск по каталогу</code>", test:st=>tipMsg(st) === "feat: поиск по каталогу"},
   {label:"Работа из <code>wip</code> и <code>опечатки</code> сохранилась", test:st=>{
     const f = files(st)["search.py"] || "";
     return history(st.R, headId(st.R)).length === 2 && /ITEMS = \[\]/.test(f) && /q in x/.test(f); }},
   {label:"Отладка выброшена", test:st=>!/отладка/.test(files(st)["search.py"] || "") && !files(st)["debug.log"]}
  ]},
 {id:"6c", title:"Ветка от не той ветки",
  brief:"<p>Ветку <code>feature/подсказки</code> по ошибке ответвили от <code>release</code>, а нужна она поверх <code>main</code>. Обычный <code>git rebase main</code> утащил бы за собой и релизные коммиты.</p><p>Перенесите на <code>main</code> <b>только</b> коммиты самой ветки — тот, что относится к релизу, попасть не должен.</p>",
  hint:"git rebase --onto main release feature/подсказки — «взять коммиты ветки, которых нет в release, и положить на main».",
  setup: R => {
    sc(R, "chore: init", {"app.py": "print('привет')"});
    scBranch(R, "release");
    scGo(R, "release");
    sc(R, "chore: заморозить версию 1.0", {"VERSION": "1.0"});
    scNew(R, "feature/подсказки");
    sc(R, "feat: подсказки при вводе", {"hints.py": "HINTS = ['поиск', 'фильтр']"});
    scGo(R, "main");
    sc(R, "feat: новый разбор запроса", {"query.py": "def parse(q): return q.split()"});
    scGo(R, "feature/подсказки");
  },
  checks:[
   {label:"Работа ветки на месте", test:st=>files(st)["hints.py"] !== undefined &&
                                            isAncestor(st.R, st.R.refs["refs/heads/main"], headId(st.R))},
   {label:"Релизный коммит не приехал", test:st=>files(st)["VERSION"] === undefined},
   {label:"Ветка стоит поверх <code>main</code>", test:st=>isAncestor(st.R, st.R.refs["refs/heads/main"], headId(st.R))},
   {label:"Использован <code>--onto</code>", test:st=>okRan(st,/--onto/)}
  ]}
],
iv:[
 {q:"Чем rebase отличается от merge и что выбираете вы?",
  probe:"Один из самых частых вопросов по git. Ждут не предпочтения, а критерия.",
  a:"Merge создаёт коммит с двумя родителями и сохраняет форму истории: видно, что работа шла параллельно, и хеши не меняются. Rebase берёт коммиты моей ветки и проигрывает их поверх чужой вершины, создавая новые коммиты с тем же содержимым, но другими родителями и хешами; история получается линейной. Отсюда и критерий выбора. Внутри своей ветки, пока её никто не забирал, я перебазируюсь: подтягиваю свежий <code>main</code>, чищу коммиты, привожу к виду, за который не стыдно на ревью. В общую ветку вливаю через merge. Ключевое ограничение — золотое правило: нельзя перебазировать то, что уже видят другие, потому что у них останутся старые хеши, истории разойдутся и после слияния в ветке окажутся дубли каждого коммита.",
  more:["Что произойдёт, если всё-таки перебазировать общую ветку?","Как чинить, если уже случилось?"]},
 {q:"Как привести ветку из десяти хаотичных коммитов в вид, пригодный для ревью?",
  probe:"Практический вопрос. Ждут интерактивный rebase и понимание его инструментов.",
  a:"Интерактивным перебазированием: <code>git rebase -i main</code> или <code>-i HEAD~10</code>. Открывается список коммитов от старых к новым, и каждому назначается действие. Мелкие правки вида «опечатка» и «забыл файл» я вливаю в тот коммит, к которому они относятся, через <code>fixup</code> — он выбрасывает бессмысленное сообщение; если сообщение стоит сохранить, беру <code>squash</code>. Отладочные и экспериментальные коммиты помечаю <code>drop</code>. Сообщения правлю через <code>reword</code>. Порядок строк — это порядок коммитов, так что связанные вещи можно поставить рядом. Отдельно удобен приём с <code>git commit --fixup=&lt;хеш&gt;</code> прямо во время работы и потом <code>git rebase -i --autosquash</code>: git сам расставит правки к нужным коммитам. Если посреди что-то пошло не так, <code>git rebase --abort</code> возвращает ветку ровно как было.",
  more:["Что такое --autosquash?","Как быть с конфликтами на каждом шаге?"]}
]
},

/* ------------------------------------------------ 7 */
{
n:7, id:"undo", title:"Отмена", sub:"reset, revert, restore",
lede:"Три команды с похожими названиями и разными последствиями. Выбор между ними — это ответ на один вопрос: видел ли эти коммиты кто-то ещё.",
theory:`
<h2>Кто на что действует</h2>
<div class="tw"><table>
<tr><th>Команда</th><th>Двигает ветку</th><th>Меняет индекс</th><th>Меняет файлы</th></tr>
<tr><td><code>git reset --soft</code></td><td>да</td><td>нет</td><td>нет</td></tr>
<tr><td><code>git reset</code> (mixed)</td><td>да</td><td>да</td><td>нет</td></tr>
<tr><td><code>git reset --hard</code></td><td>да</td><td>да</td><td><b>да</b></td></tr>
<tr><td><code>git restore file</code></td><td>нет</td><td>нет</td><td>да</td></tr>
<tr><td><code>git revert</code></td><td>да — <b>вперёд</b></td><td>да</td><td>да</td></tr>
</table></div>

<h2>reset: переставить указатель назад</h2>
<pre><code>git reset --soft HEAD~1   <span class="c"># коммит убрать, изменения оставить в индексе</span>
git reset HEAD~1          <span class="c"># коммит убрать, изменения оставить в файлах</span>
git reset --hard HEAD~1   <span class="c"># коммит убрать вместе с изменениями</span></code></pre>
<p>Первый вариант — рабочая лошадка: «закоммитил рано, хочу пересобрать коммит». Второй пригодится, если хочется разложить изменения по нескольким коммитам заново.</p>
<div class="note ok"><b class="hd"><code>--hard</code> не так страшен, как кажется</b><p>Коммиты никуда не деваются — на них просто больше не указывает ветка. Возврат: <code>git reset --hard ORIG_HEAD</code> сразу после, или через reflog. По-настоящему теряются только незакоммиченные изменения, которых git не видел.</p></div>

<h2>revert: отменить, ничего не переписывая</h2>
<pre><code>git revert a1b2c3d</code></pre>
<p><code>revert</code> не убирает коммит, а добавляет новый — с обратными изменениями. История не переписывается, хеши не меняются, старые клоны не ломаются. Это <b>единственный правильный способ</b> отменить то, что уже ушло к другим.</p>
<div class="tw"><table>
<tr><th>Ситуация</th><th>Инструмент</th></tr>
<tr><td>Коммит только у меня</td><td><code>reset</code> или <code>rebase -i</code> — история чище</td></tr>
<tr><td>Коммит уже на сервере</td><td><b>только</b> <code>revert</code></td></tr>
<tr><td>Нужно отменить несколько подряд</td><td><code>git revert HEAD~3..HEAD</code></td></tr>
</table></div>

<h2>Отмена коммита слияния</h2>
<pre><code>git revert -m 1 &lt;хеш слияния&gt;</code></pre>
<div class="note warn"><b class="hd">Почему нужен <code>-m</code></b><p>У коммита слияния два родителя, и git не знает, относительно какого из них отменять. <code>-m 1</code> означает «вернуть состояние первого родителя» — то есть той ветки, в которую вливали. Обычно нужен именно он.</p><p>Есть неочевидное последствие: после такой отмены повторное слияние той же ветки ничего не принесёт — с точки зрения git она уже влита. Чтобы вернуть работу, отменяют сам revert.</p></div>

<h2>Восстановить отдельный файл из прошлого</h2>
<pre><code>git restore --source=HEAD~3 config.py   <span class="c"># версия трёхкоммитной давности</span>
git restore --source=main forms.py      <span class="c"># версия из другой ветки</span></code></pre>
<p>Это не путешествие во времени для всего проекта, а точечная операция над одним файлом. Ветка и остальные файлы не трогаются.</p>
`,
quiz:[
 {q:"Чем <code>git reset --soft HEAD~1</code> отличается от <code>--mixed</code>?",
  opts:["Ничем","<code>--soft</code> оставляет изменения в индексе, <code>--mixed</code> — только в файлах","<code>--soft</code> удаляет файлы","<code>--mixed</code> двигает ветку, <code>--soft</code> нет"],
  a:1, why:"Оба двигают ветку назад. Разница только в том, докуда откатывается состояние."},
 {q:"Коммит уже на сервере и его нужно отменить. Что применить?",
  opts:["<code>git reset --hard</code> и force push","<code>git revert</code> — он добавляет обратный коммит, не переписывая историю","<code>git rebase -i</code> с drop","<code>git commit --amend</code>"],
  a:1, why:"Всё остальное переписывает историю, которую уже забрали другие."},
 {q:"Что происходит с коммитами после <code>git reset --hard HEAD~3</code>?",
  opts:["Удаляются немедленно","Остаются в базе объектов — ветка на них просто больше не указывает","Уходят в stash","Переносятся в другую ветку"],
  a:1, why:"Возврат: <code>git reset --hard ORIG_HEAD</code> сразу после, либо через reflog."},
 {q:"Зачем <code>git revert</code> требует <code>-m</code> для коммита слияния?",
  opts:["Для сообщения","У слияния два родителя — git не знает, относительно какого отменять","Для указания ветки","Это необязательный ключ"],
  a:1, why:"<code>-m 1</code> означает «вернуть состояние первого родителя», то есть ветки, в которую вливали."},
 {q:"Отменили слияние через revert. Что будет при повторном слиянии той же ветки?",
  opts:["Работа вернётся","Ничего не принесёт — с точки зрения git ветка уже влита","Будет конфликт","Git откажется"],
  a:1, why:"Чтобы вернуть работу, отменяют сам revert. Неочевидное, но частое затруднение."},
 {q:"Как вернуть один файл к версии трёхкоммитной давности, не трогая остальное?",
  opts:["<code>git reset --hard HEAD~3</code>","<code>git restore --source=HEAD~3 файл</code>","<code>git revert HEAD~3</code>","<code>git checkout HEAD~3</code>"],
  a:1, why:"Точечная операция: ветка и остальные файлы не трогаются."}
],
labs:[
 {id:"7a", title:"Пересобрать последний коммит",
  brief:"<p>Последний коммит <code>всё вместе</code> свалил в кучу две несвязанные вещи: правку разбора и новый файл документации. Ветка ещё никуда не отправлялась.</p><ul><li>отмените коммит, оставив изменения <b>в файлах</b>;</li><li>соберите два коммита: сначала <code>fix: разбор пустого запроса</code> с одним <code>parse.py</code>, затем <code>docs: описание формата</code> с одним <code>FORMAT.md</code>.</li></ul>",
  hint:"git reset HEAD~1 вернёт изменения в рабочую копию, дальше добавляйте файлы по одному.",
  setup: R => {
    sc(R, "chore: init", {"parse.py": "def parse(q): return q.split()"});
    sc(R, "всё вместе", {"parse.py": "def parse(q):\n    if not q: return []\n    return q.split()",
                         "FORMAT.md": "# Формат запроса"});
  },
  checks:[
   {label:"Коммитов стало три", test:st=>history(st.R, headId(st.R)).length === 3},
   {label:"Первым идёт правка разбора", test:st=>msgs(st)[1] === "fix: разбор пустого запроса"},
   {label:"Вторым — документация", test:st=>tipMsg(st) === "docs: описание формата"},
   {label:"В коммите с документацией только один файл", test:st=>{
     const h = headId(st.R), o = obj(st.R, h);
     const now = commitFiles(st.R, h), was = commitFiles(st.R, o.parents[0]);
     return Object.keys(now).filter(p => now[p] !== was[p]).length === 1; }},
   {label:"Работа не потеряна", test:st=>history(st.R, headId(st.R)).length === 3 && /if not q/.test(files(st)["parse.py"] || "")}
  ]},
 {id:"7b", title:"Отменить то, что уже у всех",
  brief:"<p>Коммит <code>feat: новый алгоритм скидок</code> уже на сервере, его забрала вся команда — и он оказался неверным. Переписывать историю нельзя.</p><p>Отмените его так, чтобы история осталась целой, а изменения — откатились.</p>",
  hint:"git revert <хеш> добавит коммит с обратными изменениями. Хеш найдёте через git log --oneline.",
  setup: R => {
    sc(R, "chore: init", {"pricing.py": "def price(x): return x"});
    sc(R, "feat: новый алгоритм скидок", {"pricing.py": "def price(x): return x * 0.5"});
    sc(R, "docs: описать цены", {"PRICING.md": "# Цены"});
    scRemote(R);
  },
  checks:[
   {label:"Ничего не удалено — история выросла", test:st=>history(st.R, headId(st.R)).length === 4},
   {label:"Оба прежних коммита на месте", test:st=>history(st.R, headId(st.R)).length === 4 &&
                                                  msgs(st).indexOf("feat: новый алгоритм скидок") >= 0 &&
                                                  msgs(st).indexOf("docs: описать цены") >= 0},
   {label:"Изменение отката", test:st=>files(st)["pricing.py"] === "def price(x): return x"},
   {label:"Документация не пострадала", test:st=>files(st)["PRICING.md"] !== undefined &&
                                                files(st)["pricing.py"] === "def price(x): return x"},
   {label:"Использован <code>git revert</code>", test:st=>okRan(st,/git\s+revert/)}
  ]}
],
iv:[
 {q:"В чём разница между reset и revert?",
  probe:"Классический вопрос. Важен не механизм, а критерий выбора.",
  a:"<code>reset</code> переставляет указатель ветки назад — коммиты остаются в базе, но ветка на них больше не показывает, то есть история переписывается. <code>revert</code> ничего не переписывает: он создаёт новый коммит, содержащий обратные изменения, и история растёт вперёд. Критерий выбора один: видел ли эти коммиты кто-то ещё. Если ветка только у меня, я беру <code>reset</code> или интерактивный rebase — история получается чище. Если коммит уже на сервере и его могли забрать, единственный правильный путь — <code>revert</code>, потому что <code>reset</code> с последующим форсом сломает работу всем, кто успел синхронизироваться. У <code>reset</code> есть три режима: <code>--soft</code> оставляет изменения в индексе, обычный <code>--mixed</code> — в рабочей копии, <code>--hard</code> убирает и их.",
  more:["Что делает reset --hard с незакоммиченным?","Как отменить merge-коммит?"]},
 {q:"Как отменить влитую ветку, если слияние уже на сервере?",
  probe:"Вопрос с подвохом про повторное слияние — на нём спотыкаются даже опытные.",
  a:"Через <code>git revert -m 1 &lt;хеш коммита слияния&gt;</code>. Ключ обязателен, потому что у коммита слияния два родителя и git не знает, относительно какого считать «обратные» изменения; единица означает первого родителя, то есть ветку, в которую вливали, — обычно это то, что нужно. Дальше идёт неочевидная часть, о которой стоит знать заранее: после такой отмены ветка с точки зрения git остаётся влитой, потому что коммит слияния в истории есть. Если потом доработать ту же ветку и слить снова, придут только новые коммиты, а исходные изменения не вернутся — они ведь были отменены. Правильный способ вернуть их — отменить сам revert, то есть сделать revert на revert, и уже поверх этого продолжать.",
  more:["Почему повторный merge не помогает?","Что если веток слияния несколько?"]}
]
},

/* ------------------------------------------------ 8 */
{
n:8, id:"rescue", title:"Спасение", sub:"reflog и всё, что кажется потерянным",
lede:"«Я всё сломал, коммиты пропали» почти всегда неправда. Git очень неохотно расстаётся с данными — надо знать, где смотреть.",
theory:`
<h2>reflog — журнал перемещений</h2>
<p>Каждый раз, когда <code>HEAD</code> или ветка куда-то переезжают, git записывает это в reflog. Коммит, сделанный и «потерянный», в этом журнале есть.</p>
<pre><code>$ <span class="k">git reflog</span>
a1b2c3d HEAD@{0}: reset: moving to HEAD~3
7f8e9d0 HEAD@{1}: commit: feat: важная работа
3c4d5e6 HEAD@{2}: commit: feat: начало</code></pre>
<pre><code>git reset --hard HEAD@{1}     <span class="c"># вернуться туда, где были</span>
git branch спасение 7f8e9d0   <span class="c"># или завести ветку на коммите</span></code></pre>
<div class="note warn"><b class="hd">Reflog — ваш личный журнал</b><p>Он живёт в <code>.git/logs/</code>, не клонируется, не отправляется на сервер, у коллеги свой. Поэтому он спасает от собственных ошибок — и совершенно бесполезен, если репозиторий склонировали заново. По умолчанию записи хранятся 90 дней, а для недостижимых коммитов — 30.</p></div>

<h2>ORIG_HEAD</h2>
<p>Перед «крупными» операциями — <code>merge</code>, <code>rebase</code>, <code>reset</code>, <code>pull</code> — git запоминает прежнюю позицию в <code>ORIG_HEAD</code>. Это быстрый способ откатить последнюю такую операцию:</p>
<pre><code>git reset --hard ORIG_HEAD</code></pre>

<h2>Что где искать</h2>
<div class="tw"><table>
<tr><th>Что случилось</th><th>Чем вернуть</th></tr>
<tr><td>Неудачный <code>reset --hard</code></td><td><code>git reset --hard ORIG_HEAD</code> или <code>HEAD@{1}</code></td></tr>
<tr><td>Удалили ветку</td><td><code>git reflog</code>, затем <code>git branch имя &lt;хеш&gt;</code></td></tr>
<tr><td>Затёрли коммит через <code>--amend</code></td><td><code>git reflog</code> — прежний коммит там</td></tr>
<tr><td>Неудачный rebase</td><td><code>git rebase --abort</code>, а если завершился — <code>reflog</code></td></tr>
<tr><td>Потеряли stash</td><td><code>git fsck --unreachable</code> покажет висячие объекты</td></tr>
<tr><td>Reflog уже вычищен</td><td><code>git fsck</code> — последняя линия обороны</td></tr>
</table></div>
<div class="note ok"><b class="hd">Почему <code>git fsck</code> обычно молчит</b><p>Пока на коммит ссылается хоть одна запись reflog, git не считает его висячим. Поэтому сразу после удаления ветки <code>fsck</code> ничего не покажет — коммит держит reflog, и искать надо там. <code>fsck</code> нужен, когда журнал уже вычищен или репозиторий склонирован заново.</p></div>

<h2>stash — отложить, не коммитя</h2>
<div class="tw"><table>
<tr><th>Команда</th><th>Что делает</th></tr>
<tr><td><code>git stash</code></td><td>спрятать изменения, вернуть чистую копию</td></tr>
<tr><td><code>git stash list</code></td><td>что спрятано</td></tr>
<tr><td><code>git stash pop</code></td><td>достать последнее и убрать из списка</td></tr>
<tr><td><code>git stash apply</code></td><td>достать, но оставить в списке</td></tr>
<tr><td><code>git stash drop</code></td><td>выбросить</td></tr>
</table></div>
<div class="note trap"><b class="hd">Про stash стоит помнить одно</b><p>Это стопка временных заметок, а не место для хранения. Спрятанное легко забыть, оно не имеет сообщения, не участвует в поиске и не уезжает на сервер. Если работа заслуживает того, чтобы её не потерять, ей место в коммите — пусть даже во временной ветке с сообщением «wip». Коммит всегда лучше stash.</p></div>

<h2>Когда всё-таки поздно</h2>
<p>Единственное, что действительно не восстанавливается, — то, чего git не видел: правки в рабочей копии, затёртые через <code>git restore</code>, и неотслеживаемые файлы, удалённые через <code>git clean -f</code>. Отсюда практический вывод: коммитьте чаще. Коммит — это не публикация, а точка сохранения, и его всегда можно потом переписать.</p>
`,
quiz:[
 {q:"Что такое reflog?",
  opts:["Часть истории, которая уходит на сервер","Локальный журнал перемещений HEAD и веток","Лог сетевых операций","Кеш диффов"],
  a:1, why:"Он не клонируется и не пушится: у коллеги свой. Спасает от локальных ошибок, но не поможет после нового клона."},
 {q:"После неудачного <code>git reset --hard HEAD~3</code> как вернуть ветку?",
  opts:["<code>git reset --hard ORIG_HEAD</code>","<code>git revert HEAD~3</code>","Никак","<code>git pull</code>"],
  a:0, why:"Эквивалент через журнал — <code>git reset --hard HEAD@{1}</code>. Коммиты никуда не пропадали."},
 {q:"Ветку удалили через <code>git branch -D</code>. Работа потеряна?",
  opts:["Да, безвозвратно","Нет: коммиты в базе, хеш есть в reflog — заводим ветку заново","Только если был push","Восстановит только сервер"],
  a:1, why:"<code>git reflog</code>, находим хеш, <code>git branch имя &lt;хеш&gt;</code>."},
 {q:"Почему <code>git fsck</code> обычно ничего не показывает после удаления ветки?",
  opts:["Он сломан","Пока на коммит ссылается запись reflog, он не считается висячим","Нужны права","Ветки не удаляются"],
  a:1, why:"Искать надо в reflog. <code>fsck</code> нужен, когда журнал вычищен или репозиторий склонирован заново."},
 {q:"Что действительно нельзя восстановить?",
  opts:["Коммиты после reset --hard","Правки в рабочей копии, затёртые через <code>git restore</code>","Удалённую ветку","Изменённый через amend коммит"],
  a:1, why:"Git их никогда не видел — сохранять было нечего. Отсюда вывод: коммитьте чаще."},
 {q:"Почему временную работу лучше коммитить, чем прятать в stash?",
  opts:["Stash медленнее","Коммит имеет сообщение, находится поиском и не забывается; stash — стопка безымянных заметок","Stash ограничен по размеру","Разницы нет"],
  a:1, why:"Коммит — это точка сохранения, а не публикация. Его всегда можно потом переписать."}
],
labs:[
 {id:"8a", title:"Вернуть после reset --hard",
  brief:"<p>Кто-то (вы) выполнил <code>git reset --hard HEAD~2</code> и снёс два коммита с работой за день. Ветка <code>main</code> стоит на старом коммите.</p><p>Верните её туда, где она была: последним коммитом снова должен стать <code>feat: отчёт по продажам</code>.</p>",
  hint:"Посмотрите git reflog — прежняя позиция там. Вернуть: git reset --hard ORIG_HEAD или git reset --hard HEAD@{1}.",
  setup: R => {
    sc(R, "chore: init", {"app.py": "print('привет')"});
    sc(R, "feat: выгрузка данных", {"dump.py": "def dump(): pass"});
    sc(R, "feat: отчёт по продажам", {"report.py": "def sales(): return []"});
    runGit(R, "git reset --hard HEAD~2");
  },
  checks:[
   {label:"Оба коммита вернулись", test:st=>history(st.R, headId(st.R)).length === 3},
   {label:"Последний — отчёт по продажам", test:st=>tipMsg(st) === "feat: отчёт по продажам"},
   {label:"Файлы на месте", test:st=>work(st)["report.py"] && work(st)["dump.py"]},
   {label:"Использован reflog или ORIG_HEAD", test:st=>okRan(st,/reflog|ORIG_HEAD|@\{/)}
  ]},
 {id:"8b", title:"Восстановить удалённую ветку",
  brief:"<p>Ветку <code>feature/импорт</code> удалили через <code>git branch -D</code>, решив, что она влита. Она не влита — работы там на два коммита.</p><p>Восстановите ветку под тем же именем, чтобы она снова указывала на <code>feat: импорт из CSV</code>.</p>",
  hint:"git reflog покажет хеш; git branch feature/импорт <хеш> создаст ветку заново.",
  setup: R => {
    sc(R, "chore: init", {"app.py": "print('привет')"});
    scNew(R, "feature/импорт");
    sc(R, "feat: чтение файла", {"io.py": "def read(p): pass"});
    sc(R, "feat: импорт из CSV", {"import_csv.py": "def load(p): return []"});
    scGo(R, "main");
    runGit(R, "git branch -D feature/импорт");
  },
  checks:[
   {label:"Ветка снова существует", test:st=>hasBranch(st, "feature/импорт")},
   {label:"Она указывает на нужный коммит", test:st=>{
     const id = st.R.refs["refs/heads/feature/импорт"];
     return !!id && obj(st.R, id).msg === "feat: импорт из CSV"; }},
   {label:"Оба коммита достижимы", test:st=>{
     const id = st.R.refs["refs/heads/feature/импорт"];
     return !!id && history(st.R, id).length === 3; }},
   {label:"Использован reflog", test:st=>okRan(st,/reflog|@\{/)}
  ]}
],
iv:[
 {q:"Вы случайно сделали git reset --hard и потеряли коммиты. Что делать?",
  probe:"Проверяют, знаете ли вы про reflog. Ответ «ничего не сделать» закрывает вопрос не в вашу пользу.",
  a:"Коммиты никуда не пропали: <code>reset</code> лишь переставил указатель ветки, а сами объекты остались в базе. Первым делом смотрю <code>git reflog</code> — это локальный журнал всех перемещений <code>HEAD</code> и веток, и прежняя позиция там будет записью <code>HEAD@{1}</code>. Возврат — <code>git reset --hard HEAD@{1}</code> или, что то же самое сразу после операции, <code>git reset --hard ORIG_HEAD</code>: git запоминает предыдущую позицию перед каждой крупной операцией. Если ветка уже другая, можно не двигать её, а завести новую на найденном хеше: <code>git branch спасение &lt;хеш&gt;</code>, и разобраться спокойно. Важная оговорка: reflog локальный, он не клонируется и не отправляется на сервер, поэтому спасает от собственных ошибок, но не поможет, если репозиторий склонировали заново.",
  more:["Сколько живут записи reflog?","А если reflog уже вычищен?"]},
 {q:"Когда вы используете stash, а когда нет?",
  probe:"Смотрят на здравый смысл. Ответ «всегда, это удобно» — тревожный.",
  a:"Stash беру для коротких переключений: правлю одно, срочно нужно посмотреть другое, работа не в том состоянии, чтобы её коммитить. Спрятал, переключился, вернулся, достал. За пределами этого сценария предпочитаю коммит — пусть даже временный, во временной ветке, с сообщением вроде «wip: черновик». Причины простые: у stash нет внятного имени, он не участвует в поиске по истории, его легко забыть и потерять при переключении веток, он не уезжает на сервер и не переживёт потерю машины. Коммит же — это просто точка сохранения, а не публикация: его всегда можно переписать через <code>rebase -i</code> или влить с <code>--squash</code>. Отдельно стоит помнить, что <code>git stash</code> по умолчанию не прячет неотслеживаемые файлы — для них нужен ключ <code>-u</code>, и на этом регулярно спотыкаются.",
  more:["Что делает git stash -u?","Как вернуть потерянный stash?"]}
]
},
