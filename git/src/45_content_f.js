
/* ------------------------------------------------ 11 */
{
n:11, id:"search", title:"Поиск", sub:"Найти, когда и почему сломалось",
lede:"История ценна не тем, что хранится, а тем, что в ней можно найти. Четыре инструмента отвечают на четыре разных вопроса — и bisect среди них недооценён сильнее всех.",
theory:`
<h2>Четыре вопроса, четыре инструмента</h2>
<div class="tw"><table>
<tr><th>Вопрос</th><th>Инструмент</th></tr>
<tr><td>Что происходило в этом файле?</td><td><code>git log -- путь</code></td></tr>
<tr><td>Кто и когда написал эту строку?</td><td><code>git blame</code></td></tr>
<tr><td>В каком коммите появилась или исчезла эта строка?</td><td><code>git log -S</code></td></tr>
<tr><td>Какой коммит сломал поведение?</td><td><code>git bisect</code></td></tr>
</table></div>

<h2>Фильтры лога</h2>
<pre><code>git log --oneline -10                 <span class="c"># последние десять, кратко</span>
git log --oneline -- src/parse.py     <span class="c"># только по файлу</span>
git log --author="Ким"                <span class="c"># по автору</span>
git log --since="2 weeks ago"         <span class="c"># по времени</span>
git log --grep="таймаут"              <span class="c"># по сообщению</span>
git log -S "TIMEOUT"                  <span class="c"># по появлению строки в коде</span>
git log --oneline --graph --all       <span class="c"># форма истории целиком</span></code></pre>
<div class="note ok"><b class="hd"><code>-S</code> — самый недооценённый ключ</b><p><code>--grep</code> ищет по сообщениям коммитов, то есть по тому, что люди <b>написали</b>. <code>-S</code> ищет по коду: находит коммиты, где число вхождений строки изменилось. Вопрос «когда вообще появилась эта константа» решается им за секунду, а перебором логов — за час.</p></div>

<h2>blame</h2>
<pre><code>$ <span class="k">git blame config.py</span>
a1b2c3d (Ким  12) timeout = 30
7f8e9d0 (Ли   13) retries = 3</code></pre>
<p>Показывает, каким коммитом добавлена каждая строка. Дальше обычно смотрят <code>git show &lt;хеш&gt;</code>, чтобы прочитать сообщение и увидеть остальные изменения того же коммита, — часто именно там и находится ответ «почему так сделано».</p>
<div class="note warn"><b class="hd">Массовое форматирование ломает blame</b><p>Коммит, который переформатировал весь проект, становится автором всех строк, и <code>blame</code> перестаёт быть полезным. Лечится файлом <code>.git-blame-ignore-revs</code> со списком таких коммитов — git и хостинги умеют его учитывать.</p></div>

<h2>bisect: двоичный поиск по истории</h2>
<p>Есть поведение, которое раньше работало, а теперь нет, и двести коммитов между. Перебирать вручную — часы. Bisect находит виновника за <code>log₂</code> шагов: из двухсот коммитов — примерно за восемь проверок.</p>
<pre><code>git bisect start
git bisect bad                 <span class="c"># сейчас сломано</span>
git bisect good v1.0           <span class="c"># здесь работало</span>
<span class="c"># git переключается на середину — проверяем</span>
git bisect good                <span class="c"># или bad</span>
<span class="c"># ...повторяем, пока git не назовёт коммит</span>
git bisect reset               <span class="c"># вернуться, где были</span></code></pre>
<div class="note trap"><b class="hd">Что делает bisect бесполезным</b><p>Коммиты, на которых проект не собирается. Если половина истории не запускается, проверять нечего, и двоичный поиск превращается в ручной. Это самый практичный аргумент за правило «каждый коммит должен быть рабочим» — не эстетика, а способность найти регрессию за десять минут.</p></div>

<h2>Диапазоны</h2>
<div class="tw"><table>
<tr><th>Запись</th><th>Что показывает</th></tr>
<tr><td><code>A..B</code></td><td>коммиты, достижимые из B, но не из A — «что нового в B»</td></tr>
<tr><td><code>A...B</code></td><td>коммиты, которые есть только у одного из них</td></tr>
<tr><td><code>HEAD..origin/main</code></td><td>что приехало с сервера и ещё не у меня</td></tr>
<tr><td><code>origin/main..HEAD</code></td><td>что я ещё не отправил</td></tr>
</table></div>
<p>Две точки читаются как «из второго вычесть первое». Именно эта пара запросов отвечает на вопрос «в каком я состоянии относительно сервера» точнее, чем строчка в <code>git status</code>.</p>
`,
quiz:[
 {q:"Чем <code>git log -S \"TIMEOUT\"</code> отличается от <code>--grep=\"TIMEOUT\"</code>?",
  opts:["Ничем","<code>-S</code> ищет по коду, <code>--grep</code> — по сообщениям коммитов","<code>-S</code> ищет по автору","<code>--grep</code> ищет по файлам"],
  a:1, why:"Вопрос «когда появилась эта константа» решается через <code>-S</code> за секунду."},
 {q:"Что показывает <code>git blame</code>?",
  opts:["Кто виноват в ошибке","Каким коммитом добавлена каждая строка файла","Список авторов","Историю файла"],
  a:1, why:"Дальше обычно смотрят <code>git show &lt;хеш&gt;</code> — там сообщение и остальные изменения того же коммита."},
 {q:"Сколько проверок нужно bisect на 200 коммитов?",
  opts:["200","Около восьми — это двоичный поиск","100","Одна"],
  a:1, why:"Каждый шаг делит диапазон пополам. Отсюда и ценность на длинной истории."},
 {q:"Что делает <code>git bisect</code> бесполезным?",
  opts:["Слишком длинная история","Коммиты, на которых проект не собирается","Много веток","Слияния"],
  a:1, why:"Это самый практичный аргумент за правило «каждый коммит должен быть рабочим»."},
 {q:"Что покажет <code>git log HEAD..origin/main</code>?",
  opts:["Все коммиты обеих","Что есть на сервере и ещё не у меня","Что я не отправил","Разницу файлов"],
  a:1, why:"Обратная запись <code>origin/main..HEAD</code> покажет неотправленное. Две точки — «из второго вычесть первое»."},
 {q:"Коммит с массовым форматированием испортил <code>git blame</code>. Что делать?",
  opts:["Ничего","Завести <code>.git-blame-ignore-revs</code> со списком таких коммитов","Переписать историю","Отменить форматирование"],
  a:1, why:"Файл понимают и git, и хостинги — такие коммиты просто пропускаются при разметке."}
],
labs:[
 {id:"11a", title:"Когда появился секрет",
  brief:"<p>В коде обнаружилась строка <code>API_KEY</code>, которой там быть не должно. Нужно понять, каким коммитом она появилась, — по сообщениям это не видно, автор назвал коммит нейтрально.</p><p>Найдите коммит через поиск <b>по содержимому</b> и посмотрите его целиком.</p>",
  hint:"git log -S API_KEY найдёт коммит, git show <хеш> покажет его содержимое.",
  setup: R => {
    sc(R, "chore: init", {"app.py": "print('привет')"});
    sc(R, "feat: клиент API", {"client.py": "import requests\n\ndef get(url): return requests.get(url)"});
    sc(R, "chore: мелкие правки", {"client.py": "import requests\n\nAPI_KEY = 'sk-live-9f2b'\n\ndef get(url): return requests.get(url)"});
    sc(R, "refactor: вынести таймаут", {"client.py": "import requests\n\nAPI_KEY = 'sk-live-9f2b'\nTIMEOUT = 10\n\ndef get(url): return requests.get(url, timeout=TIMEOUT)"});
  },
  checks:[
   {label:"Использован поиск по содержимому", test:st=>okRan(st,/log\s+.*-S/)},
   {label:"Найденный коммит просмотрен", test:st=>okRan(st,/git\s+show/)},
   {label:"Смотрели именно тот коммит", test:st=>st.log.some(e => {
     if(e.err || !/git\s+show/.test(e.cmd)) return false;
     return e.out.some(l => /chore: мелкие правки/.test(l)); })},
   {label:"История не тронута", test:st=>history(st.R, headId(st.R)).length === 4 && okRan(st,/log\s+.*-S/)}
  ]},
 {id:"11b", title:"Найти виновника двоичным поиском",
  brief:"<p>В файле <code>calc.py</code> функция должна возвращать <code>42</code>, а возвращает <code>-1</code>. Восемь коммитов назад всё работало.</p><ul><li>запустите <code>git bisect start</code>;</li><li>пометьте текущее состояние как плохое, а <code>HEAD~8</code> как хорошее;</li><li>на каждом шаге смотрите <code>cat calc.py</code> и отвечайте <code>git bisect good</code> или <code>git bisect bad</code>;</li><li>когда git назовёт коммит, выйдите через <code>git bisect reset</code>.</li></ul><p>Виновник — коммит, где <code>RESULT</code> стал отрицательным.</p>",
  hint:"git bisect start, git bisect bad, git bisect good HEAD~8. Дальше cat calc.py и good/bad.",
  setup: R => {
    sc(R, "chore: init", {"calc.py": "RESULT = 42"});
    sc(R, "feat: шаг 1", {"a.py": "x = 1"});
    sc(R, "feat: шаг 2", {"b.py": "x = 2"});
    sc(R, "feat: шаг 3", {"c.py": "x = 3"});
    sc(R, "refactor: пересчитать константу", {"calc.py": "RESULT = -1"});
    sc(R, "feat: шаг 5", {"d.py": "x = 5"});
    sc(R, "feat: шаг 6", {"e.py": "x = 6"});
    sc(R, "feat: шаг 7", {"f.py": "x = 7"});
    sc(R, "feat: шаг 8", {"g.py": "x = 8"});
  },
  checks:[
   {label:"Двоичный поиск запускался", test:st=>okRan(st,/bisect\s+start/)},
   {label:"Границы заданы", test:st=>okRan(st,/bisect\s+bad/) && okRan(st,/bisect\s+good/)},
   {label:"Git назвал виновника", test:st=>st.log.some(e => e.out.some(l => /is the first bad commit/.test(l)) &&
                                                            e.out.some(l => /пересчитать константу/.test(l)))},
   {label:"Поиск завершён через <code>reset</code>", test:st=>okRan(st,/bisect\s+reset/) && !st.R.bisect},
   {label:"Вы вернулись на <code>main</code>", test:st=>branchName(st.R) === "main" && okRan(st,/bisect\s+start/)}
  ]}
],
iv:[
 {q:"Как найти коммит, который сломал поведение?",
  probe:"Ждут bisect. Ответ «пересматриваю коммиты вручную» показывает, что инструмент незнаком.",
  a:"Через <code>git bisect</code> — двоичный поиск по истории. Говорю <code>git bisect start</code>, помечаю текущее состояние как <code>bad</code>, а известный рабочий коммит или тег как <code>good</code>. Git переключает меня на середину диапазона, я проверяю поведение и отвечаю <code>good</code> или <code>bad</code>, и так далее: каждый шаг вдвое сужает область. На двухстах коммитах это примерно восемь проверок вместо двухсот. Если проверка автоматизируется, есть <code>git bisect run &lt;скрипт&gt;</code> — тогда git находит виновника вообще без участия человека, по коду возврата. Главное условие, чтобы это работало: каждый коммит в истории должен собираться и запускаться. Именно поэтому правило «не коммить сломанное» — не про эстетику, а про способность найти регрессию за десять минут.",
  more:["Что такое git bisect run?","Что делать, если часть коммитов не собирается?"]}
]
},

/* ------------------------------------------------ 12 */
{
n:12, id:"flow", title:"Практики", sub:"Как это устроено в командах",
lede:"Технику мы разобрали. Осталось то, о чём договариваются: как выглядит ветка задачи, что делает кнопка слияния в интерфейсе и почему выбор между тремя кнопками — это выбор формы истории.",
theory:`
<h2>Pull request — это не git</h2>
<p>Pull request (он же merge request) — понятие хостинга, а не git. В самом git его нет: есть ветка, есть коммиты, есть слияние. PR добавляет вокруг этого обсуждение, ревью и проверки CI, а в конце всё равно выполняет одну из трёх операций.</p>

<h2>Три кнопки слияния</h2>
<div class="tw"><table>
<tr><th>Кнопка</th><th>Что делает</th><th>История</th></tr>
<tr><td><b>Create a merge commit</b></td><td>обычный <code>merge --no-ff</code></td><td>все коммиты ветки плюс коммит слияния</td></tr>
<tr><td><b>Squash and merge</b></td><td>склеивает ветку в <b>один</b> коммит</td><td>один коммит на задачу, линейная</td></tr>
<tr><td><b>Rebase and merge</b></td><td>перекладывает коммиты поверх, без слияния</td><td>все коммиты ветки, линейная</td></tr>
</table></div>
<div class="note warn"><b class="hd">После squash и rebase локальная ветка «расходится»</b><p>Обе кнопки создают <b>новые</b> коммиты с другими хешами. Ваша локальная ветка после этого указывает на старые, и git считает истории разошедшимися. Не пытайтесь мержить или перебазировать её обратно — работа уже в <code>main</code>. Правильно: переключиться на <code>main</code>, сделать <code>pull</code> и удалить локальную ветку задачи.</p></div>

<h2>Как обычно выглядит поток задачи</h2>
<pre><code>git switch main &amp;&amp; git pull          <span class="c"># от свежего состояния</span>
git switch -c feature/TC-1042        <span class="c"># ветка на задачу</span>
<span class="c"># работа, коммиты</span>
git rebase main                      <span class="c"># подтянуть свежее перед ревью</span>
git push -u origin feature/TC-1042
<span class="c"># открываем PR, проходим ревью, правим</span>
git push --force-with-lease          <span class="c"># после правок с amend или rebase</span>
<span class="c"># PR влит кнопкой</span>
git switch main &amp;&amp; git pull
git branch -d feature/TC-1042</code></pre>

<h2>Стратегии ветвления</h2>
<div class="tw"><table>
<tr><th>Стратегия</th><th>Суть</th><th>Кому подходит</th></tr>
<tr><td><b>trunk-based</b></td><td>одна ветка <code>main</code>, короткоживущие ветки задач на день-два</td><td>частые релизы, сильный CI</td></tr>
<tr><td><b>GitHub flow</b></td><td><code>main</code> плюс ветки задач, релиз — из <code>main</code></td><td>веб-сервисы, непрерывная поставка</td></tr>
<tr><td><b>git flow</b></td><td><code>main</code>, <code>develop</code>, ветки задач, релизов и исправлений</td><td>версионируемые продукты с длинным циклом</td></tr>
</table></div>
<p>Общая тенденция последних лет — в сторону коротких веток. Причина не мода, а арифметика: чем дольше ветка живёт, тем дальше она расходится с основной и тем дороже слияние. Ветка на две недели почти всегда означает болезненный merge; ветка на день — не означает почти ничего.</p>

<h2>Что стоит настроить один раз</h2>
<div class="tw"><table>
<tr><th>Настройка</th><th>Зачем</th></tr>
<tr><td>Защита <code>main</code></td><td>запрет прямого push и force — самая дешёвая страховка от катастрофы</td></tr>
<tr><td>Обязательный PR и зелёный CI</td><td>в основную ветку не попадает несобирающийся код, работает bisect</td></tr>
<tr><td><code>.gitignore</code> с самого начала</td><td>дешевле не пустить, чем потом вычищать историю</td></tr>
<tr><td>Договорённость о сообщениях</td><td>история становится читаемой, автоматический список изменений — возможным</td></tr>
</table></div>

<h2>Что делает историю полезной</h2>
<p>Ни одно из правил выше не про красоту. Осмысленные сообщения нужны, чтобы <code>blame</code> отвечал на вопрос «почему»; атомарные коммиты — чтобы работал <code>revert</code>; рабочие коммиты — чтобы работал <code>bisect</code>. История, за которой не следят, отвечает на вопрос «когда» и не отвечает ни на один другой.</p>
`,
quiz:[
 {q:"Что такое pull request с точки зрения git?",
  opts:["Команда git","Понятие хостинга: в git есть только ветки, коммиты и слияние","Тип коммита","Ветка особого вида"],
  a:1, why:"PR добавляет обсуждение, ревью и CI, а в конце всё равно выполняет одну из трёх операций слияния."},
 {q:"Что делает кнопка «Squash and merge»?",
  opts:["Сливает с коммитом слияния","Склеивает всю ветку в один новый коммит","Перебазирует ветку","Удаляет ветку"],
  a:1, why:"История остаётся линейной, одна задача — один коммит. Локальная ветка после этого расходится с main."},
 {q:"PR влили через «Squash and merge». Что делать с локальной веткой задачи?",
  opts:["Перебазировать на main","Переключиться на main, сделать pull и удалить ветку","Слить с main","Оставить как есть"],
  a:1, why:"Работа уже в <code>main</code>, но под другим хешем. Мержить или перебазировать старую ветку — плодить дубли."},
 {q:"Почему короткие ветки лучше длинных?",
  opts:["Так принято","Чем дольше ветка живёт, тем дальше расходится и тем дороже слияние","Экономия места","Требование CI"],
  a:1, why:"Это арифметика, а не мода: ветка на две недели почти всегда означает болезненное слияние."},
 {q:"Зачем требовать, чтобы каждый коммит собирался?",
  opts:["Для красоты","Иначе не работают bisect и откат отдельного коммита","Так требует git","Для ускорения CI"],
  a:1, why:"Ни одно правило из этого раздела не про эстетику — все они про то, чтобы инструменты поиска и отката работали."},
 {q:"Какая настройка репозитория даёт больше всего страховки за меньшую цену?",
  opts:["Обязательные подписи коммитов","Защита основной ветки от прямого push и force","Ограничение размера файлов","Запрет на merge-коммиты"],
  a:1, why:"Ровно та ситуация, где чужая работа теряется безвозвратнее всего, закрывается одним переключателем."}
],
labs:[
 {id:"12a", title:"Полный цикл задачи",
  brief:"<p>Пройдите путь задачи целиком, как это делается в команде.</p><ul><li>от свежего <code>main</code> создайте ветку <code>feature/TC-1042</code>;</li><li>сделайте в ней коммит <code>feat: пагинация списка</code>, добавляющий файл <code>paging.py</code> со строкой <code>PAGE_SIZE = 50</code>;</li><li>отправьте ветку на сервер со связыванием;</li><li>вернитесь на <code>main</code>.</li></ul>",
  hint:"git switch -c, echo, git add, git commit, git push -u origin <ветка>, git switch main",
  setup: R => {
    sc(R, "chore: init", {"app.py": "print('привет')"});
    scRemote(R);
  },
  checks:[
   {label:"Ветка создана", test:st=>hasBranch(st, "feature/TC-1042")},
   {label:"Коммит с нужным файлом в ней", test:st=>{
     const id = st.R.refs["refs/heads/feature/TC-1042"];
     return !!id && obj(st.R, id).msg === "feat: пагинация списка" &&
            /PAGE_SIZE\s*=\s*50/.test(commitFiles(st.R, id)["paging.py"] || ""); }},
   {label:"Ветка отправлена и связана", test:st=>!!st.R.remotes.origin.repo.refs["refs/heads/feature/TC-1042"] &&
                                                st.R.upstream["feature/TC-1042"] === "origin/feature/TC-1042"},
   {label:"Вы вернулись на <code>main</code>", test:st=>branchName(st.R) === "main" && hasBranch(st, "feature/TC-1042")},
   {label:"<code>main</code> не тронут", test:st=>tipMsg(st) === "chore: init" && hasBranch(st, "feature/TC-1042")}
  ]},
 {id:"12b", title:"После кнопки «Squash and merge»",
  brief:"<p>Ваш PR влили кнопкой «Squash and merge»: три коммита ветки склеились в один новый коммит в <code>main</code>, с <b>другим</b> хешем. Локально у вас всё ещё старая ветка и старый <code>main</code>.</p><p>Приведите репозиторий в порядок:</p><ul><li>обновите <code>main</code> с сервера;</li><li>удалите локальную ветку задачи — сливать или перебазировать её не нужно, работа уже в <code>main</code>.</li></ul><p>Обратите внимание: <code>git branch -d</code> откажется, потому что формально коммиты ветки никуда не влиты. Это тот случай, когда <code>-D</code> уместен.</p>",
  hint:"git switch main, git pull, затем git branch -D feature/фильтры.",
  setup: R => {
    sc(R, "chore: init", {"app.py": "print('привет')"});
    scRemote(R);
    scNew(R, "feature/фильтры");
    sc(R, "feat: фильтр", {"filters.py": "def by_date(x): return x"});
    sc(R, "wip", {"filters.py": "def by_date(x): return x\ndef by_type(x): return x"});
    sc(R, "fix: опечатка", {"filters.py": "def by_date(x): return x\ndef by_type(t): return t"});
    runGit(R, "git push -u origin feature/фильтры");
    scGo(R, "main");
    /* хостинг склеил ветку в один коммит прямо в main */
    scTheirs(R, "feat: фильтры списка (#42)", {"filters.py": "def by_date(x): return x\ndef by_type(t): return t"});
  },
  checks:[
   {label:"Вы на <code>main</code>, и он обновлён", test:st=>branchName(st.R) === "main" && tipMsg(st) === "feat: фильтры списка (#42)"},
   {label:"Склеенный коммит приехал", test:st=>tipMsg(st) === "feat: фильтры списка (#42)"},
   {label:"Работа на месте", test:st=>/by_type/.test(files(st)["filters.py"] || "")},
   {label:"Локальная ветка задачи удалена", test:st=>!hasBranch(st, "feature/фильтры")},
   {label:"История линейна — лишнего слияния нет", test:st=>linear(st) && !hasBranch(st, "feature/фильтры")}
  ]}
],
iv:[
 {q:"Какую стратегию ветвления вы предпочитаете и почему?",
  probe:"Проверяют, есть ли у вас обоснование, или вы называете то, что видели последним.",
  a:"Я исхожу из цикла поставки, а не из названия. Для сервиса, который выкатывается часто, беру простую схему: одна основная ветка, короткоживущие ветки задач на день-два, слияние через pull request с зелёным CI. Причина в арифметике: чем дольше ветка живёт, тем дальше она расходится с основной и тем дороже слияние; ветка на две недели почти всегда означает болезненный merge, ветка на день не означает почти ничего. Более тяжёлые схемы вроде git flow с отдельным <code>develop</code>, ветками релизов и исправлений оправданы там, где есть версии, которые нужно поддерживать параллельно, — коробочный продукт, мобильное приложение с длинной модерацией. Что важнее самой схемы: защита основной ветки от прямого push и требование зелёной сборки. Это одна настройка, а закрывает она самый дорогой класс аварий.",
  more:["Чем trunk-based отличается от GitHub flow?","Как быть с долгими фичами?"]},
 {q:"Чем отличаются три кнопки слияния pull request?",
  probe:"Практический вопрос для тех, кто работал с хостингами. Ловушка — в том, что происходит с локальной веткой.",
  a:"«Create a merge commit» делает обычное слияние без перемотки: в основную ветку попадают все коммиты ветки задачи плюс коммит слияния — видно, что было отдельной задачей, и всю группу можно откатить одним <code>revert -m 1</code>. «Squash and merge» склеивает всю ветку в один новый коммит: история линейная, одна задача — один коммит, но внутренние шаги теряются. «Rebase and merge» перекладывает коммиты ветки поверх основной по одному, без коммита слияния. Важная практическая деталь, на которой спотыкаются: две последние кнопки создают <b>новые</b> коммиты с другими хешами, поэтому локальная ветка задачи после них считается разошедшейся с основной. Мержить или перебазировать её обратно не надо — работа уже влита; правильно переключиться на основную, сделать <code>pull</code> и удалить локальную ветку, причём удалять придётся через <code>-D</code>, потому что формально её коммиты никуда не влиты.",
  more:["Почему branch -d отказывается удалять?","Что выбрать для команды из пяти человек?"]}
]
}

];
