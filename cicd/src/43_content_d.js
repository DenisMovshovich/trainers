
/* ------------------------------------------------ 9 */
{
n:9, id:"secrets", title:"Секреты", sub:"И безопасность конвейера",
lede:"Конвейер имеет доступ к тому, к чему не имеет доступа разработчик: ключам выкладки, реестрам, боевым средам. Поэтому он — привлекательная цель, а ошибки здесь стоят дороже обычных.",
theory:`
<h2>Как подавать</h2>
<pre><code>- <span class="k">run</span>: dotnet nuget push --api-key \${{ secrets.NUGET_KEY }}
- <span class="k">env</span>:
    <span class="k">TOKEN</span>: \${{ secrets.TOKEN }}
  <span class="k">run</span>: ./deploy.sh</code></pre>
<p>Значение подставляется, но в журнале заменяется на <code>***</code>. Маскирование работает по точному совпадению строки — и это важно: если секрет попадёт в журнал в изменённом виде (в base64, по частям, внутри JSON с экранированием), маскирование не сработает.</p>
<div class="note ok"><b class="hd">Проверьте маскирование</b><p>В сценарии внизу есть секрет. Напечатайте его через <code>echo</code> и посмотрите журнал — вместо значения будут звёздочки. Это не защита, а страховка от случайной утечки.</p></div>

<h2>Запросы слияния из форков</h2>
<div class="note trap"><b class="hd">Секретов там нет — и это правильно</b><p>Любой может открыть запрос слияния, изменив в нём файл конвейера. Если бы такому запуску выдавались секреты, чужой код получил бы ключи от боевой среды одним pull request. Поэтому для форков секреты не передаются, а права ограничены чтением.</p><p>Практическое следствие: конвейер, которому для проверки нужен секрет, на форках падать не должен — либо шаг пропускают по условию, либо проверку выносят в отдельный запуск с доверенным контекстом.</p></div>

<h2>Чего нельзя делать</h2>
<div class="tw"><table>
<tr><th>Приём</th><th>Чем плохо</th></tr>
<tr><td>Секрет в переменной, которую печатают</td><td>маскирование не всегда спасает: преобразованное значение утечёт</td></tr>
<tr><td>Подстановка <code>\${{ github.event.* }}</code> в <code>run</code></td><td>заголовок запроса слияния становится частью команды — это выполнение чужого кода</td></tr>
<tr><td>Права «на всё» по умолчанию</td><td>любой шаг может писать в репозиторий; нужно выдавать минимум</td></tr>
<tr><td>Плавающие версии действий</td><td>чужое действие может измениться и получить доступ к секретам</td></tr>
<tr><td>Секреты в артефактах и логах сборки</td><td>артефакт скачивают все, у кого есть доступ к сборке</td></tr>
</table></div>
<pre><code><span class="c"># опасно: значение из события попадает прямо в команду</span>
- <span class="k">run</span>: echo <span class="s">"Заголовок: \${{ github.event.pull_request.title }}"</span>

<span class="c"># безопасно: значение приходит через переменную окружения</span>
- <span class="k">env</span>:
    <span class="k">TITLE</span>: \${{ github.event.pull_request.title }}
  <span class="k">run</span>: echo <span class="s">"Заголовок: $TITLE"</span></code></pre>
<p>Разница в том, что в первом случае текст подставляется <b>в саму команду</b> до её запуска, и специально составленный заголовок выполнится как код. Во втором он остаётся данными.</p>

<h2>Права</h2>
<pre><code><span class="k">permissions</span>:
  <span class="k">contents</span>: read        <span class="c"># по умолчанию — только чтение</span>

<span class="k">jobs</span>:
  <span class="k">release</span>:
    <span class="k">permissions</span>:
      <span class="k">contents</span>: write   <span class="c"># расширяем только там, где нужно</span></code></pre>

<h2>Окружения и подтверждение</h2>
<p>Для боевой выкладки заводят отдельное окружение с собственным набором секретов и, при необходимости, ручным подтверждением: конвейер доходит до задачи и ждёт, пока человек нажмёт кнопку. Это простейший и самый надёжный гейт перед необратимым действием.</p>
`,
quiz:[
 {q:"Что происходит с секретом в журнале?",
  opts:["Он не подставляется","Подставляется, но маскируется по точному совпадению строки","Шифруется","Печатается как есть"],
  a:1, why:"Преобразованное значение — в base64, по частям, внутри JSON — маскирование не поймает."},
 {q:"Почему запросам слияния из форков не выдают секреты?",
  opts:["Экономия","Иначе чужой код получил бы ключи от боевой среды одним pull request","Технические ограничения","Выдают"],
  a:1, why:"Любой может открыть запрос, изменив в нём файл конвейера."},
 {q:"Чем опасна подстановка <code>\${{ github.event.pull_request.title }}</code> прямо в <code>run</code>?",
  opts:["Ничем","Заголовок становится частью команды — специально составленный выполнится как код","Слишком длинно","Может быть пустым"],
  a:1, why:"Безопасно передавать такие значения через <code>env</code>: тогда они остаются данными, а не кодом."},
 {q:"Какие права стоит давать конвейеру по умолчанию?",
  opts:["Полные — так проще","Минимальные: чтение, а запись — только там, где нужно","Права владельца","Никаких"],
  a:1, why:"Иначе любой шаг, включая чужое действие, может писать в репозиторий."},
 {q:"Зачем указывать точную версию стороннего действия?",
  opts:["Для читаемости","Чужое действие может измениться и получить доступ к секретам","Для скорости","Так требует система"],
  a:1, why:"В строгих случаях указывают не метку, а отпечаток коммита — его нельзя переписать."},
 {q:"Что даёт отдельное окружение для боевой выкладки?",
  opts:["Скорость","Свой набор секретов и возможность ручного подтверждения перед необратимым действием","Отдельные раннеры","Больше памяти"],
  a:1, why:"Простейший и самый надёжный гейт: конвейер ждёт, пока человек нажмёт кнопку."}
],
labs:[
 {id:"9a", title:"Секрет не должен утечь",
  brief:"<p>Шаг печатает токен в журнал. Убедитесь, что маскирование работает, и приведите конвейер в порядок:</p><ul><li>запустите и посмотрите журнал — вместо значения должны быть звёздочки;</li><li>уберите печать секрета совсем: замените шаг на печать строки <code>токен получен</code>, а сам секрет передайте через <code>env</code> в переменную <code>TOKEN</code>.</li></ul>",
  hint:"У шага есть блок env: TOKEN: \${{ secrets.TOKEN }}, а run печатает только сообщение.",
  setup: () => newScenario({files:{
    "src/App.cs": "class App { }",
    ".github/workflows/ci.yml":
`name: CI
on: [push]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "токен \${{ secrets.TOKEN }}"`}}),
  checks:[
   {label:"Маскирование проверено на запуске", test:st=>st.C.runs.length >= 1},
   {label:"Секрет больше не печатается", test:st=>{
     const w = wf(st); if(!w) return false;
     return ![].concat((w.jobs.deploy || {}).steps || []).some(s => /secrets\./.test(String(s.run || ""))); }},
   {label:"Он передаётся через <code>env</code>", test:st=>{
     const w = wf(st); if(!w) return false;
     return [].concat((w.jobs.deploy || {}).steps || []).some(s =>
       /secrets\.TOKEN/.test(JSON.stringify(s.env || {}))); }},
   {label:"Журнал сообщает «токен получен»", test:st=>logHas(st, /токен получен/)},
   {label:"Значения секрета в журнале нет", test:st=>
     logHas(st, /токен получен/) && !logHas(st, /s3cr3t/)}
  ]},
 {id:"9b", title:"Конвейер, который не падает на форках",
  brief:"<p>Запрос слияния пришёл из форка — секретов в таком запуске нет, и шаг выкладки в реестр падает. Это ожидаемое поведение системы, а не поломка.</p><p>Сделайте так, чтобы шаг публикации выполнялся <b>только на событии <code>push</code></b>, а на <code>pull_request</code> конвейер проходил целиком.</p><p>Проверьте оба случая: <code>ci run</code> и <code>ci run --event pull_request</code>.</p>",
  hint:"if: github.event_name == 'push' у шага публикации.",
  setup: () => newScenario({fork: true, files:{
    "src/App.cs": "class App { }",
    ".github/workflows/ci.yml":
`name: CI
on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: dotnet build
      - name: публикация
        run: echo "ключ \${{ secrets.NUGET_KEY }}" > pushed.txt`}}),
  checks:[
   {label:"У шага публикации появилось условие", test:st=>{
     const w = wf(st); if(!w) return false;
     return [].concat((w.jobs.build || {}).steps || []).some(s =>
       String(s.name) === "публикация" && s["if"] !== undefined); }},
   {label:"Условие смотрит на событие", test:st=>{
     const w = wf(st); if(!w) return false;
     return [].concat((w.jobs.build || {}).steps || []).some(s =>
       String(s.name) === "публикация" && /event_name/.test(String(s["if"]))); }},
   {label:"Запуск на <code>pull_request</code> проходит", test:st=>
     st.C.runs.some(r => r.event === "pull_request" && r.status === "success")},
   {label:"Публикация там пропущена", test:st=>{
     const r = st.C.runs.filter(x => x.event === "pull_request").pop();
     if(!r) return false;
     const s = r.jobs[0].instances[0].steps.filter(x => x.name === "публикация")[0];
     return !!s && s.status === "skipped"; }},
   {label:"На <code>push</code> конвейер тоже зелёный", test:st=>
     st.C.runs.some(r => r.event === "push" && r.status === "success")}
  ]}
],
iv:[
 {q:"Как в конвейере работают с секретами?",
  probe:"Ждут не только «через secrets», но и понимание границ этого механизма.",
  a:"Секреты хранятся на стороне системы и подставляются в шаг через контекст <code>secrets</code> — обычно в блок <code>env</code>, а не прямо в команду. В журнале значение маскируется, но полагаться на это как на защиту нельзя: маскирование работает по точному совпадению строки, и если секрет попадёт в вывод преобразованным — по частям, в base64, внутри JSON — он утечёт как есть. Отдельно важно, что для запросов слияния из форков секреты не выдаются вовсе, и это правильно: иначе любой желающий получил бы ключи, просто открыв pull request с изменённым конвейером. Поэтому шаги, которым нужен секрет, ограничивают условием по событию. И общий принцип, который я стараюсь соблюдать: минимальные права по умолчанию, расширение только в той задаче, где оно нужно, точные версии сторонних действий и отдельное окружение с ручным подтверждением для боевой выкладки.",
  more:["Почему нельзя подставлять данные события в run?","Что такое окружение с подтверждением?"]}
]
},

/* ------------------------------------------------ 10 */
{
n:10, id:"tests", title:"Тесты", sub:"Нестабильность и доверие",
lede:"Самая частая причина, по которой конвейер перестают уважать, — тесты, которые падают через раз. Разбираемся, что с этим делать, и чего делать нельзя.",
theory:`
<h2>Что такое нестабильный тест</h2>
<p>Тест, который на одном и том же коде иногда проходит, иногда нет. Причин немного, и они повторяются:</p>
<div class="tw"><table>
<tr><th>Причина</th><th>Как выглядит</th></tr>
<tr><td>Ожидание по времени вместо условия</td><td><code>sleep 2</code> вместо ожидания элемента</td></tr>
<tr><td>Зависимость от порядка</td><td>тест падает только когда идёт вторым</td></tr>
<tr><td>Общие данные</td><td>два теста правят одну запись</td></tr>
<tr><td>Внешняя система</td><td>сторонний сервис отвечает медленно</td></tr>
<tr><td>Гонка в самом приложении</td><td>тест нашёл настоящую ошибку — просто редкую</td></tr>
</table></div>
<div class="note warn"><b class="hd">Последняя строка — про честность</b><p>Прежде чем объявлять тест нестабильным, стоит убедиться, что он не ловит настоящую редкую гонку. Ошибка, воспроизводящаяся в одном прогоне из двадцати, у пользователей случится куда чаще, чем кажется.</p></div>

<h2>Перезапуски</h2>
<pre><code>- <span class="k">run</span>: dotnet test --filter ui --retries 3</code></pre>
<div class="note trap"><b class="hd">Перезапуск — обезболивающее, а не лечение</b><p>Он делает сборку зелёной и прячет проблему. Пользоваться им можно, но только вместе с двумя вещами: перезапуски должны быть <b>видны</b> в отчёте, и по ним должна вестись статистика. Тест, который стабильно проходит со второй попытки, — не исправленный тест, а скрытый долг.</p><p>Разумная схема: перезапуск разрешён, отчёт помечает такие тесты как нестабильные, раз в неделю список разбирают. Тест, который не удаётся починить, отключают явно — с задачей и сроком, а не молчаливо.</p></div>

<h2>Где искать причину</h2>
<div class="tw"><table>
<tr><th>Приём</th><th>Что показывает</th></tr>
<tr><td>Прогнать тест сто раз подряд</td><td>частоту падений — с этого начинают</td></tr>
<tr><td>Прогнать в другом порядке</td><td>зависимость от соседей</td></tr>
<tr><td>Прогнать в одиночку</td><td>зависимость от параллельности</td></tr>
<tr><td>Трассировка и видео из конвейера</td><td>что было на экране в момент падения</td></tr>
<tr><td>Сравнить локально и в CI</td><td>разницу окружения: часовой пояс, язык, разрешение, скорость</td></tr>
</table></div>
<p>Последний пункт для тестов интерфейса особенно важен: раннер медленнее ноутбука и работает без экрана, поэтому тайминги там другие, и тесты, привязанные ко времени, разваливаются именно в CI.</p>

<h2>Разделение по скорости</h2>
<div class="tw"><table>
<tr><th>Когда</th><th>Что гонять</th></tr>
<tr><td>На каждый запрос слияния</td><td>модульные и часть интеграционных: минуты</td></tr>
<tr><td>На вливание в основную ветку</td><td>полный набор, включая интерфейс</td></tr>
<tr><td>Ночью по расписанию</td><td>самое долгое: длинные сценарии, совместимость, нагрузка</td></tr>
</table></div>
<p>Смысл в том, чтобы обратная связь по обычной правке приходила за минуты. Конвейер, который идёт сорок минут на каждую запятую, люди начинают обходить.</p>

<h2>Что делает конвейер полезным для тестировщика</h2>
<p>Три вещи: отчёт, по которому видно, что упало, без открывания журнала; артефакты, по которым можно разобраться, не воспроизводя локально; и стабильность, при которой красный цвет означает настоящую поломку. Всё остальное — детали.</p>
`,
quiz:[
 {q:"Что такое нестабильный тест?",
  opts:["Медленный тест","Тест, который на одном и том же коде иногда проходит, иногда нет","Тест без проверок","Тест, зависящий от данных"],
  a:1, why:"Причин немного и они повторяются: ожидание по времени, порядок, общие данные, внешние системы, настоящая гонка."},
 {q:"Что проверить, прежде чем объявлять тест нестабильным?",
  opts:["Скорость","Не ловит ли он настоящую редкую гонку в приложении","Покрытие","Автора"],
  a:1, why:"Ошибка, воспроизводящаяся раз в двадцать прогонов, у пользователей случится куда чаще."},
 {q:"Чем плохи перезапуски как единственная мера?",
  opts:["Медленно","Делают сборку зелёной и прячут проблему — это скрытый долг","Требуют настройки","Ничем"],
  a:1, why:"Пользоваться можно, но перезапуски должны быть видны в отчёте и по ним должна вестись статистика."},
 {q:"С чего начинают разбор нестабильного теста?",
  opts:["С переписывания","С прогона сто раз подряд — узнать частоту падений","С отключения","С правки таймаутов"],
  a:1, why:"Дальше — прогон в другом порядке и в одиночку, чтобы отделить зависимость от соседей и от параллельности."},
 {q:"Почему тесты интерфейса чаще падают в CI, чем локально?",
  opts:["Хуже сеть","Раннер медленнее и работает без экрана — тайминги другие","Другая версия браузера","Меньше памяти"],
  a:1, why:"Поэтому тесты, привязанные ко времени, а не к условию, разваливаются именно там."},
 {q:"Что гонять на каждый запрос слияния?",
  opts:["Всё","Быстрое: модульные и часть интеграционных — обратная связь за минуты","Только сборку","Ничего"],
  a:1, why:"Конвейер, который идёт сорок минут на каждую запятую, люди начинают обходить."}
],
labs:[
 {id:"10a", title:"Нестабильный тест",
  brief:"<p>Задача с тестами интерфейса падает через раз. Сегодня она упала.</p><ul><li>запустите и посмотрите журнал — какие именно тесты не прошли;</li><li>как временную меру включите перезапуски: добавьте к команде <code>--retries 3</code>;</li><li>убедитесь, что задача проходит и в журнале видно, сколько тестов прошло со второй попытки.</li></ul><p>Это именно временная мера — в разборе ниже написано, чем она плоха.</p>",
  hint:"dotnet test --filter ui --retries 3",
  setup: () => newScenario({seed: 11, files:{
    "src/App.cs": "class App { }",
    ".github/workflows/ci.yml":
`name: CI
on: [push]

jobs:
  ui:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: dotnet test --filter ui`}}),
  checks:[
   {label:"Падение было видно", test:st=>st.C.runs.some(r => r.status === "failure")},
   {label:"Перезапуски включены", test:st=>{
     const w = wf(st); if(!w) return false;
     return [].concat((w.jobs.ui || {}).steps || []).some(s => /--retries\s+\d/.test(String(s.run || ""))); }},
   {label:"Задача стала проходить", test:st=>{ const r = last(st); return !!r && r.status === "success"; }},
   {label:"Нестабильные видны в отчёте", test:st=>logHas(st, /со второй попытки/)},
   {label:"Журнал открывался", test:st=>okRan(st,/ci\s+logs/)}
  ]},
 {id:"10b", title:"Быстрые отдельно, медленные отдельно",
  brief:"<p>Сейчас на каждый запуск гоняется всё сразу, и конвейер идёт долго. Разделите:</p><ul><li>задача <code>fast</code> — модульные тесты (<code>--filter unit</code>), выполняется на любом событии;</li><li>задача <code>slow</code> — тесты интерфейса (<code>--filter ui --retries 3</code>), выполняется <b>только на событии <code>push</code></b>.</li></ul><p>Сценарий запускается на <code>pull_request</code>, поэтому <code>slow</code> должна быть пропущена, а <code>fast</code> — пройти.</p>",
  hint:"Две задачи вместо одной, у slow — if: github.event_name == 'push'. Не забудьте checkout в каждой.",
  setup: () => newScenario({event: "pull_request", seed: 11, files:{
    "src/App.cs": "class App { }",
    ".github/workflows/ci.yml":
`name: CI
on: [push, pull_request]

jobs:
  all:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: dotnet test --filter unit
      - run: dotnet test --filter ui --retries 3`}}),
  checks:[
   {label:"Задачи разделены", test:st=>{
     const w = wf(st); return !!w && !!w.jobs.fast && !!w.jobs.slow; }},
   {label:"У медленной есть условие по событию", test:st=>{
     const w = wf(st); if(!w || !w.jobs.slow) return false;
     return /event_name/.test(String(w.jobs.slow["if"] || "")); }},
   {label:"Быстрая прошла", test:st=>{ const j = jobOf(st, "fast"); return !!j && j.status === "success"; }},
   {label:"Медленная пропущена на запросе слияния", test:st=>{
     const j = jobOf(st, "slow"); return !!j && j.status === "skipped"; }},
   {label:"Конвейер зелёный и быстрый", test:st=>{ const r = last(st); return !!r && r.status === "success" && r.wall < 120; }}
  ]}
],
iv:[
 {q:"В конвейере есть тест, который падает через раз. Ваши действия?",
  probe:"Ключевой вопрос для автоматизатора. Ответ «поставлю перезапуск» без продолжения — плохой знак.",
  a:"Сначала я выясняю, действительно ли он нестабилен, а не ловит редкую гонку в приложении: прогоняю его сто раз подряд и смотрю частоту падений. Если падает — пробую отделить причину: прогон в одиночку против прогона в общем наборе покажет зависимость от соседей и от параллельности, прогон в другом порядке — зависимость от порядка, а трассировка и видео из CI покажут, что было на экране. Для тестов интерфейса чаще всего оказывается ожидание по времени вместо ожидания условия: раннер медленнее ноутбука, поэтому в CI такой тест разваливается, а локально нет. Перезапуски я использую, но как временную меру и обязательно с двумя условиями: они видны в отчёте, и по ним ведётся статистика, иначе это просто способ спрятать долг. Тест, который не удаётся починить в разумный срок, лучше явно отключить с задачей и сроком, чем оставить мигать: конвейер, которому не верят, перестаёт работать как проверка.",
  more:["Как отличить нестабильный тест от настоящей гонки?","Что делать, если таких тестов тридцать?"]}
]
},

/* ------------------------------------------------ 11 */
{
n:11, id:"speed", title:"Скорость", sub:"Где уходит время",
lede:"Быстрый конвейер — не роскошь, а условие того, что им будут пользоваться. Разбираемся, из чего складывается время и какие рычаги на что влияют.",
theory:`
<h2>Из чего складывается время</h2>
<div class="tw"><table>
<tr><th>Часть</th><th>Обычно</th><th>Чем сокращать</th></tr>
<tr><td>Ожидание свободного раннера</td><td>секунды–минуты</td><td>свои раннеры, меньше задач</td></tr>
<tr><td>Выгрузка кода</td><td>секунды</td><td>ограничить глубину истории</td></tr>
<tr><td>Установка среды и зависимостей</td><td><b>часто больше всего</b></td><td>кеш</td></tr>
<tr><td>Сборка</td><td>минуты</td><td>кеш сборки, инкрементальность</td></tr>
<tr><td>Тесты</td><td>минуты–десятки минут</td><td>параллельность, шардирование, разделение по скорости</td></tr>
</table></div>
<div class="note ok"><b class="hd">Сначала измерить</b><p>Журнал показывает время каждого шага. Прежде чем что-то оптимизировать, стоит посмотреть, где на самом деле уходит время: интуиция здесь ошибается регулярно, и «медленные тесты» часто оказываются пятиминутной установкой браузеров.</p></div>

<h2>Три рычага и что они дают</h2>
<div class="tw"><table>
<tr><th>Рычаг</th><th>Время конвейера</th><th>Машинное время</th><th>Стоимость</th></tr>
<tr><td>Кеш</td><td>меньше</td><td>меньше</td><td>почти ноль</td></tr>
<tr><td>Параллельность и матрица</td><td>меньше</td><td><b>то же или больше</b></td><td>дороже</td></tr>
<tr><td>Убрать лишнее</td><td>меньше</td><td>меньше</td><td>ноль</td></tr>
</table></div>
<p>Порядок применения обычно такой: сначала убрать лишнее, потом кеш, потом параллельность. Начинать с параллельности — распространённая ошибка: она самая заметная и самая дорогая.</p>

<h2>Что бывает лишним</h2>
<div class="tw"><table>
<tr><th>Что</th><th>Почему лишнее</th></tr>
<tr><td>Полная история репозитория</td><td>для сборки хватает последнего коммита</td></tr>
<tr><td>Установка того, что уже есть на образе раннера</td><td>стандартные образы содержат многое из коробки</td></tr>
<tr><td>Сборка в каждой задаче заново</td><td>иногда дешевле передать артефактом</td></tr>
<tr><td>Полный прогон на каждую правку документации</td><td>фильтры по путям решают это одной строкой</td></tr>
<tr><td>Медленные тесты на каждом запросе слияния</td><td>их место — на вливании или ночью</td></tr>
</table></div>

<h2>Отмена устаревших запусков</h2>
<pre><code><span class="k">concurrency</span>:
  <span class="k">group</span>: \${{ github.workflow }}-\${{ github.ref }}
  <span class="k">cancel-in-progress</span>: true</code></pre>
<p>Если в ветку прилетело три коммита подряд, гонять полный конвейер на первые два бессмысленно. Отмена устаревших запусков освобождает раннеры и сокращает очередь — иногда это даёт больше, чем любая оптимизация внутри.</p>

<h2>Разумный ориентир</h2>
<div class="tw"><table>
<tr><th>Проверка</th><th>Сколько должна идти</th></tr>
<tr><td>Сборка и быстрые тесты на запрос слияния</td><td>до 10 минут</td></tr>
<tr><td>Полный набор на вливание</td><td>до 30 минут</td></tr>
<tr><td>Ночной прогон</td><td>сколько угодно</td></tr>
</table></div>
<p>Границы условны, но принцип общий: чем ближе проверка к моменту написания кода, тем быстрее она обязана быть.</p>
`,
quiz:[
 {q:"Что чаще всего занимает больше всего времени в конвейере?",
  opts:["Сборка","Установка среды и зависимостей","Выгрузка кода","Ожидание раннера"],
  a:1, why:"Именно поэтому кеш — первый по эффективности рычаг. Но сначала стоит измерить."},
 {q:"Что делает параллельность с машинным временем?",
  opts:["Уменьшает","Оставляет тем же или увеличивает — уменьшается только время ожидания","Обнуляет","Не влияет на время конвейера"],
  a:1, why:"Кеш уменьшает оба. Поэтому начинать оптимизацию с параллельности — распространённая ошибка."},
 {q:"В каком порядке разумно оптимизировать конвейер?",
  opts:["Параллельность, кеш, лишнее","Убрать лишнее, потом кеш, потом параллельность","Только кеш","Сначала купить раннеры"],
  a:1, why:"Параллельность самая заметная и самая дорогая, поэтому она последняя."},
 {q:"Зачем нужна отмена устаревших запусков?",
  opts:["Экономия места","Три коммита подряд — незачем гонять полный конвейер на первые два","Стабильность","Безопасность"],
  a:1, why:"Освобождает раннеры и сокращает очередь; иногда даёт больше, чем оптимизация внутри."},
 {q:"Сколько разумно идти проверке на запросе слияния?",
  opts:["Сколько нужно","До десяти минут — она ближе всего к моменту написания кода","До часа","Меньше минуты"],
  a:1, why:"Чем ближе проверка к написанию кода, тем быстрее она обязана быть."},
 {q:"С чего начинать оптимизацию?",
  opts:["С добавления раннеров","С измерения: журнал показывает время каждого шага","С кеша","С матрицы"],
  a:1, why:"Интуиция ошибается: «медленные тесты» часто оказываются пятиминутной установкой браузеров."}
],
labs:[
 {id:"11a", title:"Ускорить конвейер",
  brief:"<p>Конвейер идёт долго. Сделайте его быстрее, не убирая ни одной проверки:</p><ul><li>добавьте кеш пакетов с ключом на основе <code>hashFiles</code>;</li><li>разнесите модульные и интерфейсные тесты по двум задачам, чтобы они шли одновременно;</li><li>интерфейсные разбейте матрицей на два шарда.</li></ul><p>Ориентир: время конвейера должно стать заметно меньше исходного, а машинное — остаться сопоставимым.</p>",
  hint:"Сначала посмотрите ci logs build — где уходит время. Кеш ставится после checkout и до restore.",
  setup: () => newScenario({flakyFixed: true, files:{
    "src/App.cs": "class App { }",
    "Directory.Packages.props": "<Project/>",
    ".github/workflows/ci.yml":
`name: CI
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: dotnet restore
      - run: dotnet build
      - run: dotnet test --filter unit
      - run: dotnet test --filter ui`}}),
  checks:[
   {label:"Кеш добавлен с ключом на <code>hashFiles</code>", test:st=>{
     const w = wf(st); if(!w) return false;
     for(const n in w.jobs)
       if([].concat((w.jobs[n] || {}).steps || []).some(s =>
         /actions\/cache/.test(String(s.uses || "")) && /hashFiles/.test(String((s["with"] || {}).key || ""))))
         return true;
     return false; }},
   {label:"Задач стало больше одной", test:st=>{ const w = wf(st); return !!w && Object.keys(w.jobs).length >= 2; }},
   {label:"Где-то применена матрица", test:st=>{
     const w = wf(st); if(!w) return false;
     return Object.keys(w.jobs).some(n => !!((w.jobs[n].strategy || {}).matrix)); }},
   {label:"Все проверки на месте", test:st=>logHas(st, /Всего тестов: 40/) && logHas(st, /Всего тестов: 9/)},
   {label:"Конвейер стал быстрее 130 секунд", test:st=>{ const r = last(st); return !!r && r.status === "success" && r.wall < 130; }}
  ]}
],
iv:[
 {q:"Конвейер идёт сорок минут. Что будете делать?",
  probe:"Ждут порядок действий и понимание, какой рычаг что даёт.",
  a:"Сначала измерю: журнал показывает время каждого шага, и почти всегда выясняется что-то неожиданное — например, что половина времени уходит не на тесты, а на установку зависимостей и браузеров. Дальше по порядку возрастания стоимости. Убрать лишнее: полную историю репозитория при выгрузке, установку того, что уже есть в образе раннера, полный прогон на правки документации — это решается фильтром по путям. Потом кеш: он уменьшает и время ожидания, и машинное время, и стоит почти ничего, главное — правильный ключ на основе отпечатка файла зависимостей. Только потом параллельность: разложить независимые проверки на задачи, разбить тесты матрицей на шарды. Важно понимать, что параллельность уменьшает время ожидания, но не машинное время, то есть делает быстрее и дороже одновременно. И отдельно — отмена устаревших запусков: если в ветку летит несколько коммитов подряд, гонять полный конвейер на каждый бессмысленно.",
  more:["Что даёт кеш по сравнению с матрицей?","Как отменять устаревшие запуски?"]}
]
},

/* ------------------------------------------------ 12 */
{
n:12, id:"design", title:"Проектирование", sub:"Стадии, гейты, выкладка",
lede:"Собираем всё вместе: как выглядит конвейер, которым пользуются, а не обходят, и какие решения в нём принимают осознанно.",
theory:`
<h2>Типичные стадии</h2>
<div class="tw"><table>
<tr><th>Стадия</th><th>Что делает</th><th>Когда</th></tr>
<tr><td>Проверка</td><td>стиль, статический анализ, сборка</td><td>всегда, первой — она быстрая</td></tr>
<tr><td>Быстрые тесты</td><td>модульные, часть интеграционных</td><td>всегда</td></tr>
<tr><td>Медленные тесты</td><td>интерфейс, длинные сценарии</td><td>на вливание и ночью</td></tr>
<tr><td>Сборка образа</td><td>то, что поедет в среду</td><td>после зелёных проверок</td></tr>
<tr><td>Выкладка на стенд</td><td>автоматически</td><td>на вливание в основную ветку</td></tr>
<tr><td>Выкладка в бой</td><td>по кнопке или по метке</td><td>осознанно</td></tr>
</table></div>

<h2>Непрерывная поставка и непрерывное развёртывание</h2>
<div class="tw"><table>
<tr><th></th><th>Continuous Delivery</th><th>Continuous Deployment</th></tr>
<tr><td>Что автоматизировано</td><td>всё до боевой выкладки</td><td>включая боевую выкладку</td></tr>
<tr><td>Кто нажимает кнопку</td><td>человек</td><td>никто</td></tr>
<tr><td>Что требуется</td><td>зелёный конвейер</td><td>плюс наблюдаемость и быстрый откат</td></tr>
</table></div>
<p>Второе без первого не бывает, и переход между ними — вопрос доверия к тестам, а не настройки конвейера. Идти в непрерывное развёртывание, имея нестабильные тесты, значит автоматически выкладывать в бой всё подряд.</p>

<h2>Гейты: что блокирует вливание</h2>
<div class="tw"><table>
<tr><th>Гейт</th><th>Разумно</th></tr>
<tr><td>Сборка проходит</td><td>обязательно</td></tr>
<tr><td>Тесты зелёные</td><td>обязательно</td></tr>
<tr><td>Ревью одобрено</td><td>обязательно</td></tr>
<tr><td>Покрытие не упало</td><td>осторожно: превращается в игру с числами</td></tr>
<tr><td>Ноль замечаний анализатора</td><td>только для новых замечаний, иначе никто не начнёт</td></tr>
</table></div>
<div class="note trap"><b class="hd">Гейт, который обходят, хуже отсутствующего</b><p>Любая обязательная проверка, которую регулярно приходится отключать «в порядке исключения», обучает команду, что проверки не обязательны. Лучше меньше гейтов, но настоящих.</p></div>

<h2>Откат</h2>
<p>Про выкладку думают, про откат — редко, а он важнее: возможность быстро вернуться означает, что выкладываться можно чаще и спокойнее. Минимум — уметь выложить предыдущую версию той же кнопкой. Признак того, что откат не продуман: он требует ручных шагов, которых нет в конвейере.</p>

<h2>Чек-лист зрелого конвейера</h2>
<div class="tw"><table>
<tr><th>Признак</th><th>Почему важен</th></tr>
<tr><td>Красный означает поломку</td><td>иначе цвет перестают читать</td></tr>
<tr><td>Обратная связь за минуты</td><td>иначе конвейер обходят</td></tr>
<tr><td>Есть что забрать из упавшего прогона</td><td>иначе разбор превращается в гадание</td></tr>
<tr><td>Описан в репозитории и проходит ревью</td><td>иначе меняется бесконтрольно</td></tr>
<tr><td>Работает без секретов на форках</td><td>иначе внешние вклады невозможны</td></tr>
<tr><td>Откат — такая же кнопка, как выкладка</td><td>иначе выкладку боятся</td></tr>
</table></div>
`,
quiz:[
 {q:"Чем Continuous Delivery отличается от Continuous Deployment?",
  opts:["Ничем","В первом боевую выкладку запускает человек, во втором она автоматическая","Второе быстрее","Первое только для библиотек"],
  a:1, why:"Переход между ними — вопрос доверия к тестам, а не настройки конвейера."},
 {q:"Почему опасно делать гейтом «покрытие не должно падать»?",
  opts:["Он медленный","Превращается в игру с числами вместо улучшения тестов","Его нельзя измерить","Он не работает"],
  a:1, why:"Правило про анализатор похожее: требовать ноль замечаний имеет смысл только для новых."},
 {q:"Чем плох гейт, который регулярно отключают «в порядке исключения»?",
  opts:["Ничем","Он обучает команду, что проверки не обязательны","Он замедляет","Он ломает конвейер"],
  a:1, why:"Любая обязательная проверка, которую регулярно отключают «в порядке исключения», обучает команду, что проверки не обязательны. Лучше меньше гейтов, но настоящих."},
 {q:"Что важнее продумать — выкладку или откат?",
  opts:["Выкладку","Откат: возможность быстро вернуться позволяет выкладываться чаще и спокойнее","Одинаково","Ни то, ни другое"],
  a:1, why:"Признак того, что откат не продуман: он требует ручных шагов, которых нет в конвейере."},
 {q:"Какая стадия должна идти первой?",
  opts:["Тесты интерфейса","Быстрые проверки: стиль, анализ, сборка","Выкладка","Сборка образа"],
  a:1, why:"Они дешёвые и дают ответ раньше всех остальных."},
 {q:"Главный признак зрелого конвейера?",
  opts:["Много стадий","Красный цвет означает настоящую поломку","Быстрые раннеры","Красивый отчёт"],
  a:1, why:"Всё остальное теряет смысл, если цвету перестают верить."}
],
labs:[
 {id:"12a", title:"Собрать конвейер целиком",
  brief:"<p>Итоговое задание. Напишите конвейер с нуля — файл <code>.github/workflows/ci.yml</code> заготовлен пустым.</p><p>Требуется:</p><ul><li>запуск на <code>push</code> и <code>pull_request</code>;</li><li>задача <code>build</code>: выгрузка кода, кеш пакетов с ключом на <code>hashFiles('Directory.Packages.props')</code>, <code>dotnet restore</code>, <code>dotnet build</code>;</li><li>задача <code>unit</code> после сборки: модульные тесты;</li><li>задача <code>ui</code> после сборки, параллельно с <code>unit</code>: интерфейсные тесты с <code>--retries 3</code> и выгрузкой артефакта <code>ui-results</code> из <code>test-results/**</code> с условием <code>always()</code>;</li><li>задача <code>deploy</code> после обеих тестовых: печатает <code>выложено</code>, но <b>только</b> на событии <code>push</code>.</li></ul><p>Не забудьте: у каждой задачи своя машина.</p>",
  hint:"Четыре задачи. У unit и ui — needs: [build]; у deploy — needs: [unit, ui] и if по event_name. В каждой задаче свой checkout.",
  setup: () => newScenario({event: "pull_request", seed: 11, files:{
    "src/App.cs": "class App { }",
    "Directory.Packages.props": "<Project/>",
    ".github/workflows/ci.yml": "# соберите конвейер здесь\n"}}),
  checks:[
   {label:"Четыре задачи описаны", test:st=>{
     const w = wf(st); return !!w && ["build","unit","ui","deploy"].every(n => !!w.jobs[n]); }},
   {label:"Кеш с правильным ключом", test:st=>{
     const w = wf(st); if(!w || !w.jobs.build) return false;
     return [].concat(w.jobs.build.steps || []).some(s =>
       /actions\/cache/.test(String(s.uses || "")) && /hashFiles/.test(String((s["with"] || {}).key || ""))); }},
   {label:"Тестовые задачи идут одновременно после сборки", test:st=>{
     const u = jobOf(st, "unit"), i = jobOf(st, "ui");
     return !!u && !!i && u.start === i.start && u.start > 0; }},
   {label:"Интерфейсные тесты прошли с перезапусками", test:st=>{
     const j = jobOf(st, "ui"); return !!j && j.status === "success" && logHas(st, /со второй попытки/); }},
   {label:"Артефакт сохранён", test:st=>{ const r = last(st); return !!r && !!r.artifacts["ui-results"]; }},
   {label:"Выкладка пропущена на запросе слияния", test:st=>{
     const j = jobOf(st, "deploy"); return !!j && j.status === "skipped"; }},
   {label:"Конвейер зелёный", test:st=>{ const r = last(st); return !!r && r.status === "success"; }}
  ]},
 {id:"12b", title:"Проверить чужой конвейер",
  brief:"<p>Вам на ревью пришёл конвейер с несколькими проблемами. Найдите их встроенной проверкой (<code>ci lint</code>) и исправьте все замечания.</p><p>Правки не должны ломать работу: после починки конвейер обязан запускаться и проходить.</p>",
  hint:"ci lint перечислит всё разом. Их три: отсутствие выгрузки кода, ключ кеша без hashFiles и артефакт без if.",
  setup: () => newScenario({flakyFixed: true, files:{
    "src/App.cs": "class App { }",
    "Directory.Packages.props": "<Project/>",
    ".github/workflows/ci.yml":
`name: CI
on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/cache@v4
        with:
          path: ~/.nuget/packages
          key: nuget-packages
      - run: dotnet test --filter unit
      - uses: actions/upload-artifact@v4
        with:
          name: trx
          path: TestResults/**`}}),
  checks:[
   {label:"Проверка запускалась", test:st=>okRan(st,/ci\s+lint/)},
   {label:"Замечаний не осталось", test:st=>{
     const r = runCi(st.C, "ci lint");
     return !r.err && (r.out || []).some(l => /замечаний нет/.test(l)); }},
   {label:"Выгрузка кода добавлена", test:st=>{
     const w = wf(st); if(!w) return false;
     return [].concat((w.jobs.test || {}).steps || []).some(s => /checkout/.test(String(s.uses || ""))); }},
   {label:"Конвейер проходит", test:st=>{ const r = last(st); return !!r && r.status === "success"; }},
   {label:"Артефакт сохраняется", test:st=>{ const r = last(st); return !!r && !!r.artifacts["trx"]; }}
  ]}
],
iv:[
 {q:"Как бы вы выстроили конвейер для проекта с автотестами?",
  probe:"Итоговый вопрос. Ждут стадии, а не перечисление возможностей системы.",
  a:"Я разделяю его по скорости обратной связи. Первыми идут самые дешёвые проверки — сборка, статический анализ, стиль: они отвечают за минуту и отсекают очевидное. Дальше быстрые тесты, модульные и часть интеграционных, тоже на каждый запрос слияния; цель — уложиться минут в десять, потому что более медленный конвейер люди начинают обходить. Медленное — тесты интерфейса, длинные сценарии — я вешаю на вливание в основную ветку и на ночной прогон по расписанию, обязательно с выгрузкой отчёта, трассировок и видео артефактами с условием <code>always()</code>, иначе разбирать падения будет нечем. Зависимости кеширую по отпечатку файла блокировки, тесты интерфейса разбиваю матрицей на шарды. Выкладка на стенд автоматическая после зелёных проверок, боевая — отдельной задачей по кнопке и с ограничением по ветке. И два условия, без которых всё остальное не работает: красный цвет должен означать настоящую поломку, а откат должен быть такой же кнопкой, как выкладка.",
  more:["Какие гейты сделаете обязательными?","Когда переходить к автоматической выкладке в бой?"]}
]
}

];
