/* ============================================================
   Карточки «На собеседовании» для разделов 1–12
   ============================================================ */
(function(){
const ADD = {

"sdk":[
 {q:"На какой версии .NET у вас проект и почему именно на ней?",
  probe:"Проверяют, различаете ли вы SDK, рантайм и целевую платформу — их путают постоянно.",
  a:"Проекты собраны под <code>net8.0</code> — это целевая платформа, то есть набор API, на который компилируется код. SDK на машине при этом может быть новее: у меня стоит десятый, и восьмая платформа собирается им нормально, потому что пакет таргетинга приезжает из NuGet. Третья величина — рантайм, на котором всё запускается. Различать их важно практически: ошибка <code>NETSDK1045</code> означает, что SDK старше целевой платформы, и лечится обновлением SDK, а не правкой проекта. Восьмая версия выбрана как LTS: долгая поддержка важнее свежести для набора, который живёт годами.",
  more:["Что делает global.json?","Что будет, если на агенте CI другой SDK?"]},
 {q:"Зачем нужен global.json?",
  probe:"Вопрос про воспроизводимость сборки. Многие о файле не знают.",
  a:"Он фиксирует версию SDK для дерева проектов, чтобы на машине разработчика и на агенте CI собирало одно и то же. Без него берётся самый свежий установленный SDK, и поведение может разъехаться — вплоть до разных предупреждений и разного поведения анализаторов. Ключевая настройка внутри — <code>rollForward</code>: она определяет, что делать, если указанной версии нет. Значение <code>latestFeature</code> или <code>latestMinor</code> — разумный компромисс между воспроизводимостью и тем, чтобы сборка не падала на любом новом агенте.",
  more:["Что произойдёт, если указанной версии нет?","Где ещё фиксируют версии в проекте?"]}
],

"nuget":[
 {q:"Как у вас управляются версии пакетов?",
  probe:"Смотрят, знаете ли вы про транзитивные зависимости и централизованное управление.",
  a:"В каждом проекте свои <code>PackageReference</code>, и сейчас версии совпадают: во всех трёх проектах NUnit 3.14.0, Playwright.NUnit 1.58.0, NUnit3TestAdapter 4.5.0. Но держится это на дисциплине, а не на механизме: обновит кто-то один проект — и в общем решении появится разное поведение одинакового кода. Правильное лечение — централизованное управление версиями — файл <code>Directory.Packages.props</code>, где версия задаётся один раз на всё решение, а в проектах остаётся только имя пакета. Отдельно стоит помнить, что прямых пакетов у нас шесть, а разрешается граф из двадцати трёх: конфликт версий чаще всего приходит именно из транзитивных, и разбирается он через <code>obj/project.assets.json</code>.",
  more:["Что такое транзитивная зависимость?","Как разрешается конфликт версий?"]}
],

"test":[
 {q:"Как вы запускаете подмножество тестов?",
  probe:"Практический вопрос: пользуетесь ли вы фильтрами или гоняете всё подряд.",
  a:"Через <code>--filter</code>. Основные выражения: <code>TestCategory=Smoke</code> по атрибуту <code>[Category]</code>, <code>FullyQualifiedName~ContactForm</code> по вхождению в полное имя, <code>Name=…</code> по имени метода. Их можно соединять через <code>&amp;</code> и <code>|</code>. В пайплайне это основа разделения: дымовой набор по категории на каждый коммит, полный — по расписанию. Одна практическая деталь, на которой я сам обжигался: <code>--list-tests</code> игнорирует <code>--filter</code> и печатает полный список независимо от фильтра, поэтому проверять выборку надо самим прогоном или по отчёту.",
  more:["Как соединять несколько условий?","Что если категорий нет вовсе?"]},
 {q:"Что происходит между dotnet test и запуском вашего теста?",
  probe:"Проверяют понимание слоёв: многие считают, что тесты запускает сам dotnet.",
  a:"Три слоя. <code>dotnet test</code> — это обёртка, она делает restore и build, а затем передаёт управление VSTest. VSTest — платформа запуска, она сама про NUnit ничего не знает. Знание приносит адаптер — <code>NUnit3TestAdapter</code>: он находит тесты по атрибутам, запускает их и отдаёт результаты обратно. И только внутри работает сам NUnit. Практический смысл в диагностике: если тесты «не обнаружены», причина почти всегда в этой цепочке — не подключён адаптер, не подключён <code>Microsoft.NET.Test.Sdk</code> или проект не помечен как тестовый. А параметры в runsettings адресованы разным слоям, и каждый читает только свою секцию.",
  more:["Почему тесты могут не обнаружиться?","Кто читает секцию NUnit в runsettings?"]}
],

"errors":[
 {q:"Сборка проходит локально и падает в CI. С чего начнёте?",
  probe:"Проверяют наличие метода, а не знание конкретной ошибки.",
  a:"Сначала смотрю, отличается ли окружение: версия SDK на агенте, доступность внутреннего NuGet-фида, наличие кеша пакетов. Самые частые причины именно там. Дальше читаю текст ошибки целиком, а не первую строку: коды вида <code>NETSDK…</code> и <code>NU…</code> однозначно указывают на слой — SDK или восстановление пакетов. Если сообщение невнятное, повышаю подробность: <code>dotnet build -v n</code> или <code>-v d</code>. Отдельно проверяю, не различается ли конфигурация: локально обычно Debug, в CI — Release, и часть проблем видна только в одной из них. И, если речь про тесты, а не про сборку, — установлены ли браузеры Playwright на агенте: это отдельный шаг, и его пропуск даёт ошибку, которая на сборку не похожа.",
  more:["Что означает код NU1101?","Как посмотреть вычисленное значение свойства?"]}
]
};
SECTIONS.forEach(s=>{ if(ADD[s.id]) s.iv = ADD[s.id]; });
})();
