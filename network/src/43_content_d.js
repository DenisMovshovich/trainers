
/* ------------------------------------------------ 7 */
{
n:7, id:"redirect", title:"Перенаправления", sub:"И кеширование ответов",
lede:"Два механизма, из-за которых запрос ведёт себя «не так, как написано»: сервер отправляет в другое место либо отвечает, что ничего не изменилось.",
theory:`
<h2>Коды перенаправления</h2>
<div class="tw"><table>
<tr><th>Код</th><th>Смысл</th><th>Метод при переходе</th></tr>
<tr><td>301</td><td>переехало навсегда</td><td>может смениться на GET</td></tr>
<tr><td>302</td><td>временно</td><td>может смениться на GET</td></tr>
<tr><td>307</td><td>временно, строго</td><td><b>сохраняется</b></td></tr>
<tr><td>308</td><td>навсегда, строго</td><td><b>сохраняется</b></td></tr>
</table></div>
<div class="note trap"><b class="hd">Отсюда пропадающие POST</b><p>Исторически клиенты при 301 и 302 меняли метод на GET и теряли тело. Поэтому <code>POST</code> на адрес, который перенаправляет, может превратиться в <code>GET</code> без тела — и сервер ответит непонятной ошибкой. Коды 307 и 308 введены именно для того, чтобы метод сохранялся. Практический вывод: если API перенаправляет, это должно быть 307 или 308.</p></div>

<h2>Куда ведёт перенаправление</h2>
<pre><code>$ <span class="k">curl -I http://api.example.com/old</span>
&lt; HTTP/1.1 301 Moved Permanently
&lt; location: /new</code></pre>
<p>По умолчанию curl <b>не переходит</b> — он показывает ответ как есть. Переход включается ключом <code>-L</code>. Это удобно: сначала посмотреть цепочку, потом пройти её.</p>
<div class="note ok"><b class="hd">Цепочки и петли</b><p>Перенаправлений может быть несколько подряд. Если адреса начинают повторяться, получается петля, и curl останавливается на двадцатом переходе с сообщением <code>Maximum (20) redirects followed</code>. Обычная причина — правило «добавить слеш» и правило «убрать слеш», настроенные одновременно.</p></div>

<h2>Кеширование</h2>
<div class="tw"><table>
<tr><th>Заголовок</th><th>Что делает</th></tr>
<tr><td><code>Cache-Control: max-age=60</code></td><td>ответ можно хранить 60 секунд</td></tr>
<tr><td><code>Cache-Control: no-store</code></td><td>не хранить вовсе</td></tr>
<tr><td><code>ETag: "v1"</code></td><td>метка версии содержимого</td></tr>
<tr><td><code>If-None-Match: "v1"</code></td><td>запрос клиента: «пришли, только если изменилось»</td></tr>
<tr><td><code>304 Not Modified</code></td><td>ответ сервера: «не изменилось, бери своё»</td></tr>
</table></div>
<pre><code>$ <span class="k">curl -I -H 'If-None-Match: "v1"' https://api.example.com/cached</span>
&lt; HTTP/1.1 304 Not Modified</code></pre>
<p>Ответ 304 приходит без тела — в этом и смысл: экономится трафик, а не время на запрос. Для тестировщика это означает, что проверять надо оба случая: с меткой и без.</p>

<h2>Кеш как источник ложных выводов</h2>
<div class="note warn"><b class="hd">«Исправили, а всё по-старому»</b><p>Между вами и сервисом может стоять несколько кешей: браузера, промежуточного прокси, сети доставки содержимого. Правка доехала до сервера, а вы видите прежний ответ. Признаки: <code>curl</code> показывает новое, а браузер старое; или помогает добавление случайного параметра к адресу. Проверять надо с отключённым кешем и смотреть на заголовки <code>Cache-Control</code> и <code>Age</code> в ответе.</p></div>
`,
quiz:[
 {q:"Чем 307 отличается от 302?",
  opts:["Ничем","При 307 метод и тело сохраняются, при 302 могут смениться на GET","307 постоянный","302 только для сайтов"],
  a:1, why:"Поэтому POST на перенаправляющий адрес может превратиться в GET без тела."},
 {q:"Переходит ли curl по перенаправлению по умолчанию?",
  opts:["Да","Нет — нужен ключ <code>-L</code>","Только для 301","Только для https"],
  a:1, why:"Удобно: сначала посмотреть цепочку, потом пройти её."},
 {q:"Что означает <code>Maximum (20) redirects followed</code>?",
  opts:["Сервер перегружен","Перенаправления зациклились","Слишком длинный адрес","Нет доступа"],
  a:1, why:"Обычная причина — правила «добавить слеш» и «убрать слеш», настроенные одновременно."},
 {q:"Что делает заголовок <code>If-None-Match</code>?",
  opts:["Отключает кеш","Просит прислать ответ, только если содержимое изменилось","Задаёт срок хранения","Проверяет подпись"],
  a:1, why:"Если не изменилось, сервер отвечает 304 без тела."},
 {q:"Приходит ли тело вместе с 304?",
  opts:["Да","Нет — в этом и смысл: экономится трафик","Только заголовки Content-Type","Зависит от сервера"],
  a:1, why:"Тестировать поэтому нужно оба случая: с меткой версии и без."},
 {q:"Исправление выкатили, а ответ прежний. Что заподозрить?",
  opts:["DNS","Кеш: браузера, прокси или сети доставки","Сертификат","Порт"],
  a:1, why:"Признак: curl показывает новое, а браузер старое. Смотреть на <code>Cache-Control</code> и <code>Age</code>."}
],
labs:[
 {id:"7a", title:"Пройти цепочку",
  brief:"<p>Адрес <code>/old</code> куда-то ведёт, но неизвестно куда.</p><ul><li>посмотрите ответ <b>без</b> перехода и найдите заголовок <code>location</code>;</li><li>пройдите цепочку целиком ключом <code>-L</code> и получите содержимое;</li><li>отдельно попробуйте <code>/loop</code> — там перенаправления зациклены.</li></ul>",
  hint:"curl -I покажет 301 и location. curl -L пройдёт до конца.",
  setup: () => newScenario({
    dns: {"api.example.com": [{type:"A", value:"203.0.113.10"}]},
    nodes: {"203.0.113.10": {ports: {443: {server:"api", tls:true, cert:"c"}}}},
    certs: {c: {cn:"api.example.com", san:["api.example.com"], notAfter: 999999}},
    servers: {api: {routes: [
      {path:"/old", status:301, location:"/v2/old"},
      {path:"/v2/old", status:302, location:"/v2/new"},
      {path:"/v2/new", body:"новое место"},
      {path:"/loop", status:302, location:"/loop2"},
      {path:"/loop2", status:302, location:"/loop"}
    ]}}
  }),
  checks:[
   {label:"Ответ без перехода посмотрели", test:st=>logHas(st, /location: \/v2\/old/)},
   {label:"Цепочка пройдена с <code>-L</code>", test:st=>okRan(st,/curl[^|]*-L\b/)},
   {label:"Содержимое получено", test:st=>logHas(st, /новое место/)},
   {label:"Петля обнаружена", test:st=>logHas(st, /Maximum \(20\) redirects/)}
  ]},
 {id:"7b", title:"Условный запрос",
  brief:"<p>Проверьте кеширование адреса <code>/catalog</code>:</p><ul><li>запросите его и посмотрите заголовки — найдите <code>etag</code> и <code>cache-control</code>;</li><li>повторите запрос, передав ту же метку в <code>If-None-Match</code>;</li><li>убедитесь, что во второй раз пришёл 304 и тела нет.</li></ul>",
  hint:"Заголовок с кавычками задают в одинарных: -H 'If-None-Match: \"v7\"'",
  setup: () => newScenario({
    dns: {"api.example.com": [{type:"A", value:"203.0.113.10"}]},
    nodes: {"203.0.113.10": {ports: {443: {server:"api", tls:true, cert:"c"}}}},
    certs: {c: {cn:"api.example.com", san:["api.example.com"], notAfter: 999999}},
    servers: {api: {routes: [
      {path:"/catalog", json:[{id:1, name:"кружка"}], etag:'"v7"', cache:"max-age=300"}
    ]}}
  }),
  checks:[
   {label:"Заголовки первого ответа осмотрены", test:st=>logHas(st, /etag: "v7"/)},
   {label:"Правило кеширования увидено", test:st=>logHas(st, /cache-control: max-age=300/)},
   {label:"Условный запрос отправлен", test:st=>ran(st,/If-None-Match/i)},
   {label:"Получен 304", test:st=>logHas(st, /304 Not Modified/)},
   {label:"Тела в нём нет", test:st=>st.log.some(e =>
     /If-None-Match/i.test(e.cmd) && !(e.out || []).some(l => /кружка/.test(l)))}
  ]}
],
iv:[
 {q:"Почему POST на адрес с перенаправлением может «потерять» тело?",
  probe:"Тонкий вопрос, который отличает читавшего спецификацию от знающего по верхам.",
  a:"Исторически клиенты при кодах 301 и 302 меняли метод на GET и отбрасывали тело — так сложилось из практики браузеров, и спецификация это допускает. Поэтому <code>POST</code> на адрес, который перенаправляет, приходит на конечный адрес уже как <code>GET</code> без данных, и сервер отвечает чем-то вроде 400 или 405, что выглядит совершенно необъяснимо. Именно для этого случая ввели коды 307 и 308: они означают то же самое — временно и постоянно, — но обязывают сохранить метод и тело. Практический вывод простой: если API перенаправляет, он должен отвечать 307 или 308, а не 302. А при отладке стоит смотреть на цепочку без ключа <code>-L</code>, чтобы увидеть каждый шаг и код на каждом из них.",
  more:["Как посмотреть всю цепочку целиком?","Что делать, если перенаправления зациклились?"]}
]
},

/* ------------------------------------------------ 8 */
{
n:8, id:"proxy", title:"Промежуточные узлы", sub:"Прокси, балансировщики, шлюзы",
lede:"Между клиентом и приложением почти никогда нет пустоты. Понимание того, кто именно ответил, сокращает разбор вдвое — особенно когда логи приложения пусты.",
theory:`
<h2>Кто стоит на пути</h2>
<div class="tw"><table>
<tr><th>Узел</th><th>Что делает</th></tr>
<tr><td>Обратный прокси</td><td>принимает запросы снаружи и передаёт приложению; часто он же завершает шифрование</td></tr>
<tr><td>Балансировщик</td><td>распределяет запросы между несколькими экземплярами</td></tr>
<tr><td>Корпоративный прокси</td><td>стоит между вашей машиной и интернетом; задаётся переменными окружения</td></tr>
<tr><td>Сеть доставки содержимого</td><td>кеширует и отдаёт ближе к пользователю</td></tr>
</table></div>

<h2>Что видит приложение</h2>
<p>Если запрос прошёл через прокси, приложение видит адрес прокси, а не клиента. Настоящий адрес передаётся заголовками:</p>
<div class="tw"><table>
<tr><th>Заголовок</th><th>Что несёт</th></tr>
<tr><td><code>X-Forwarded-For</code></td><td>исходный адрес клиента</td></tr>
<tr><td><code>X-Forwarded-Proto</code></td><td>исходная схема: <code>http</code> или <code>https</code></td></tr>
<tr><td><code>X-Forwarded-Host</code></td><td>исходное имя из адреса</td></tr>
</table></div>
<div class="note trap"><b class="hd">Отсюда два частых дефекта</b><p>Первый: приложение строит ссылки по своей схеме и отдаёт <code>http://</code> вместо <code>https://</code>, потому что шифрование завершилось на прокси и до приложения дошёл обычный HTTP. Лечится учётом <code>X-Forwarded-Proto</code>.</p><p>Второй: ограничение по адресу клиента считает всех одним и тем же, потому что видит адрес прокси. Лечится учётом <code>X-Forwarded-For</code> — но только для доверенных прокси, иначе заголовок легко подделать.</p></div>

<h2>502 и 504</h2>
<div class="tw"><table>
<tr><th>Код</th><th>Что произошло</th><th>Где смотреть</th></tr>
<tr><td><b>502</b></td><td>прокси не смог получить ответ: приложение не отвечает или закрыло соединение</td><td>жив ли процесс, слушает ли порт</td></tr>
<tr><td><b>504</b></td><td>ответ не пришёл вовремя: приложение думает дольше, чем ждёт прокси</td><td>долгие запросы, таймаут прокси</td></tr>
</table></div>
<div class="note ok"><b class="hd">Как понять, кто ответил</b><p>Смотрите заголовок <code>Server</code> и тело ошибки. Страница вида «502 Bad Gateway» с подписью nginx — это ответ прокси, а не приложения; в логах приложения такого запроса может не быть вовсе. Это самая частая причина фразы «в логах пусто, а пользователи видят ошибку».</p></div>

<h2>Прокси на вашей машине</h2>
<pre><code>$ <span class="k">env</span>
https_proxy=http://proxy.corp:3128

$ <span class="k">curl -x http://proxy.corp:3128 http://api.example.com/health</span></code></pre>
<p>Корпоративный прокси задаётся переменными окружения, и клиенты подхватывают их автоматически. Отсюда классическое расхождение: в терминале работает, в приложении нет — потому что приложение переменные не читает. Или наоборот: локально всё ходит через прокси, а в контейнере переменных нет, и запросы уходят напрямую в никуда.</p>

<h2>Что проверять</h2>
<div class="tw"><table>
<tr><th>Вопрос</th><th>Как ответить</th></tr>
<tr><td>Есть ли прокси вообще?</td><td><code>env</code> — переменные <code>http_proxy</code>, <code>https_proxy</code>, <code>no_proxy</code></td></tr>
<tr><td>Кто ответил?</td><td>заголовок <code>Server</code> и тело ошибки</td></tr>
<tr><td>Дошёл ли запрос до приложения?</td><td>логи приложения по сквозному идентификатору</td></tr>
<tr><td>Не режет ли прокси заголовки?</td><td>сравнить отправленное и полученное приложением</td></tr>
</table></div>
`,
quiz:[
 {q:"Что означает 502?",
  opts:["Приложение вернуло ошибку","Промежуточный узел не смог получить ответ от приложения","Слишком много запросов","Не найдено"],
  a:1, why:"В логах приложения такого запроса может не быть вовсе — он мог до него не дойти."},
 {q:"Чем 504 отличается от 502?",
  opts:["Ничем","504 — ответ не пришёл вовремя; 502 — не пришёл вовсе","504 от приложения","502 временный"],
  a:1, why:"При 504 смотреть надо на долгие запросы и таймаут прокси."},
 {q:"Приложение отдаёт ссылки с <code>http://</code>, хотя сайт по <code>https</code>. Причина?",
  opts:["Ошибка в коде ссылок","Шифрование завершилось на прокси, приложение не учитывает <code>X-Forwarded-Proto</code>","Неверный сертификат","Кеш"],
  a:1, why:"До приложения дошёл обычный HTTP, и оно строит ссылки по своей схеме."},
 {q:"Зачем нужен <code>X-Forwarded-For</code>?",
  opts:["Для кеширования","Он несёт исходный адрес клиента, скрытый прокси","Для маршрутизации","Для авторизации"],
  a:1, why:"Учитывать его можно только для доверенных прокси — иначе заголовок легко подделать."},
 {q:"Как быстро понять, что ответил прокси, а не приложение?",
  opts:["По времени ответа","По заголовку <code>Server</code> и виду страницы ошибки","По коду","Никак"],
  a:1, why:"Это самая частая причина фразы «в логах пусто, а пользователи видят ошибку»."},
 {q:"В терминале запрос проходит, а из приложения нет. Что заподозрить?",
  opts:["DNS","Прокси из переменных окружения, который читает терминал и не читает приложение","Сертификат","Кеш"],
  a:1, why:"Бывает и наоборот: локально через прокси, а в контейнере переменных нет."}
],
labs:[
 {id:"8a", title:"Кто ответил",
  brief:"<p>Запрос возвращает ошибку. Выясните, кто её сформировал — приложение или промежуточный узел.</p><ul><li>выполните запрос и посмотрите заголовки ответа;</li><li>обратите внимание на <code>Server</code> и на вид страницы;</li><li>проверьте отдельно, доступен ли сам сервис приложения на его порту;</li><li>зафиксируйте вывод.</li></ul>",
  hint:"curl -I через прокси покажет, кто ответил. Затем nc -zv на порт приложения.",
  setup: () => newScenario({
    dns: {
      "shop.example.com": [{type:"A", value:"203.0.113.40"}],
      "app-1.internal": [{type:"A", value:"10.0.1.5"}]
    },
    nodes: {
      "203.0.113.40": {ports: {443: {server:"gw", tls:true, cert:"c"}}},
      "10.0.1.5": {down: true, ports: {}}
    },
    certs: {c: {cn:"shop.example.com", san:["shop.example.com"], notAfter: 999999}},
    servers: {gw: {serverName:"nginx/1.25", upstream:{ip:"10.0.1.5", port:8080}}}
  }),
  checks:[
   {label:"Ошибка получена", test:st=>logHas(st, /502/)},
   {label:"Видно, что ответил прокси", test:st=>logHas(st, /nginx/)},
   {label:"Доступность приложения проверена", test:st=>ran(st,/(nc|curl)[^|]*(10\.0\.1\.5|app-1\.internal)/)},
   {label:"Приложение действительно не отвечает", test:st=>logHas(st, /timed out|No route/)},
   {label:"Вывод записан как <code>proxy</code>", test:st=>verdict(st) === "proxy"}
  ]},
 {id:"8b", title:"Забытый прокси",
  brief:"<p>На машине задан корпоративный прокси, и все запросы идут через него. Прокси при этом сломан.</p><ul><li>посмотрите переменные окружения — <code>env</code>;</li><li>убедитесь, что запрос идёт через прокси: <code>curl -v</code> покажет это первой строкой;</li><li>проверьте, что сам сервис доступен напрямую, — обойдите прокси ключом <code>--noproxy &quot;*&quot;</code>;</li><li>зафиксируйте вывод.</li></ul>",
  hint:"curl --noproxy \"*\" http://api.example.com/health — запрос пойдёт напрямую, минуя переменную окружения.",
  setup: () => newScenario({
    proxyEnv: "http://proxy.corp:3128",
    dns: {
      "api.example.com": [{type:"A", value:"203.0.113.10"}],
      "proxy.corp": [{type:"A", value:"10.0.0.8"}]
    },
    nodes: {
      "203.0.113.10": {ports: {80: {server:"api"}}},
      "10.0.0.8": {ports: {}}
    },
    servers: {api: {routes: [{path:"/health", body:"ok"}]}}
  }),
  checks:[
   {label:"Переменные окружения проверены", test:st=>ran(st,/^\s*env\b/)},
   {label:"Видно, что запрос идёт через прокси", test:st=>logHas(st, /Uses proxy/)},
   {label:"Прокси не отвечает", test:st=>logHas(st, /Connection refused|timed out/)},
   {label:"Сервис доступен напрямую, минуя прокси", test:st=>st.log.some(e =>
     /--noproxy/.test(e.cmd) && (e.out || []).some(l => /^ok$/.test(l)))},
   {label:"Вывод записан как <code>proxy</code>", test:st=>verdict(st) === "proxy"}
  ]}
],
iv:[
 {q:"Пользователи видят 502, а в логах приложения пусто. Что происходит?",
  probe:"Практический вопрос эксплуатации. Ждут понимания, кто формирует ответ.",
  a:"502 почти всегда формирует не приложение, а то, что стоит перед ним, — обратный прокси или балансировщик. Он означает, что промежуточный узел не смог получить ответ: приложение не запущено, упало, слушает не тот порт или закрыло соединение. Поэтому в логах приложения запроса и нет — он до него не дошёл. Проверяю я так: сначала смотрю заголовок <code>Server</code> и вид страницы ошибки, чтобы подтвердить, что отвечает прокси; потом проверяю доступность самого приложения на его внутреннем адресе и порту; потом смотрю логи прокси — там будет причина, вплоть до «connection refused к такому-то адресу». Родственный код 504 означает другое: приложение живо, но отвечает дольше, чем прокси готов ждать; там разговор про долгие запросы и настройку таймаутов, и вот тогда в логах приложения запрос как раз будет — просто незавершённый.",
  more:["Чем 504 отличается от 502?","Зачем нужен X-Forwarded-For?"]}
]
},

/* ------------------------------------------------ 9 */
{
n:9, id:"cors", title:"CORS", sub:"Почему curl работает, а браузер нет",
lede:"Самая непонятая тема в вебе. Ключ к ней — одно утверждение: правила соблюдает браузер, а не сервер. Ответ приходит, браузер просто не отдаёт его коду страницы.",
theory:`
<h2>Что вообще происходит</h2>
<p>Страница, загруженная с одного адреса, по умолчанию не может читать ответы с другого — это защита от того, чтобы чужой сайт делал запросы от вашего имени и читал результат. Разрешение выдаёт <b>сервер</b> заголовком, а проверяет его <b>браузер</b>.</p>
<div class="note ok"><b class="hd">Отсюда главный признак</b><p>Если <code>curl</code> тот же запрос выполняет, а страница получает ошибку — это CORS. Никакой сетевой проблемы нет: запрос ушёл, ответ пришёл, браузер его не отдал. Проверить можно за десять секунд: внизу есть команда <code>browser</code>, которая ходит по тем же адресам, но с правилами страницы.</p></div>

<h2>Простые запросы и предварительные</h2>
<div class="tw"><table>
<tr><th></th><th>Простой</th><th>С предварительным запросом</th></tr>
<tr><td>Когда</td><td>GET, HEAD, POST с обычным типом содержимого</td><td>другой метод, свой заголовок, JSON в теле</td></tr>
<tr><td>Что делает браузер</td><td>шлёт запрос, потом смотрит заголовок в ответе</td><td>сначала шлёт <code>OPTIONS</code>, и только при разрешении — сам запрос</td></tr>
</table></div>
<p>Предварительный запрос — источник недоумения «в журнале сервера два запроса вместо одного» и «запрос не дошёл до обработчика»: если <code>OPTIONS</code> не обработан, до основного запроса дело не дойдёт вовсе.</p>

<h2>Заголовки, которыми разрешают</h2>
<div class="tw"><table>
<tr><th>Заголовок ответа</th><th>Что разрешает</th></tr>
<tr><td><code>Access-Control-Allow-Origin</code></td><td>какому источнику читать ответ</td></tr>
<tr><td><code>Access-Control-Allow-Methods</code></td><td>какие методы (в ответе на <code>OPTIONS</code>)</td></tr>
<tr><td><code>Access-Control-Allow-Headers</code></td><td>какие свои заголовки</td></tr>
<tr><td><code>Access-Control-Allow-Credentials</code></td><td>можно ли слать куки и учётные данные</td></tr>
<tr><td><code>Access-Control-Max-Age</code></td><td>сколько хранить результат предварительного запроса</td></tr>
</table></div>
<div class="note trap"><b class="hd">Звёздочка и учётные данные несовместимы</b><p><code>Access-Control-Allow-Origin: *</code> запрещено использовать вместе с куками. Если запрос идёт с учётными данными, сервер обязан вернуть точное имя источника и вдобавок <code>Access-Control-Allow-Credentials: true</code>. Настроить «звёздочку и куки» нельзя — и это не ограничение реализации, а осознанное решение.</p></div>

<h2>Как читать ошибку</h2>
<pre><code>Access to fetch at 'https://api.example.com/orders' from origin
'https://app.example.com' has been blocked by CORS policy:
No 'Access-Control-Allow-Origin' header is present on the requested resource.</code></pre>
<p>В сообщении всегда есть три вещи: куда шли, откуда пришла страница и чего именно не хватило. Последняя часть — это и есть задача разработчику сервиса, и формулируется она однозначно.</p>

<h2>Чего делать не надо</h2>
<div class="tw"><table>
<tr><th>Приём</th><th>Почему плохо</th></tr>
<tr><td>Поставить <code>*</code> в бою</td><td>читать ответы сможет любой сайт; для публичного API допустимо, для внутреннего — нет</td></tr>
<tr><td>Пытаться «обойти» из кода страницы</td><td>невозможно: проверку делает браузер, а не ваш код</td></tr>
<tr><td>Отключить проверку в браузере ключом запуска</td><td>работает только у вас и маскирует настоящую проблему</td></tr>
<tr><td>Ставить прокси только ради обхода</td><td>иногда оправдано, но чаще проще договориться о заголовке</td></tr>
</table></div>
`,
quiz:[
 {q:"Кто соблюдает правила CORS?",
  opts:["Сервер","Браузер: он не отдаёт ответ коду страницы","Прокси","Операционная система"],
  a:1, why:"Поэтому curl тот же запрос выполняет без возражений — сетевой проблемы нет."},
 {q:"Что означает, что curl работает, а страница получает ошибку?",
  opts:["Сеть сломана","Это CORS: ответ пришёл, но браузер его не отдал","Сертификат неверен","Сервер лежит"],
  a:1, why:"Признак настолько надёжный, что проверку стоит делать первой."},
 {q:"Когда браузер шлёт предварительный запрос <code>OPTIONS</code>?",
  opts:["Всегда","Когда метод не простой, есть свой заголовок или JSON в теле","Только для POST","Только с куками"],
  a:1, why:"Отсюда «два запроса вместо одного» в журнале сервера."},
 {q:"Можно ли сочетать <code>Access-Control-Allow-Origin: *</code> с куками?",
  opts:["Да","Нет: с учётными данными нужен точный источник и <code>Allow-Credentials: true</code>","Только для GET","Зависит от браузера"],
  a:1, why:"Это осознанное решение спецификации, а не ограничение реализации."},
 {q:"Что нельзя починить со стороны кода страницы?",
  opts:["Заголовки запроса","Саму проверку CORS — её делает браузер","Метод","Тело"],
  a:1, why:"Разрешение выдаёт сервер; из кода страницы его не обойти."},
 {q:"Обработчик <code>OPTIONS</code> не настроен. Что произойдёт?",
  opts:["Ничего","До основного запроса дело не дойдёт вовсе","Запрос пойдёт как обычно","Ответ будет 500"],
  a:1, why:"Это объясняет «запрос не доходит до обработчика», хотя в curl всё работает."}
],
labs:[
 {id:"9a", title:"Доказать, что дело в браузере",
  brief:"<p>Разработчик утверждает, что сервис <code>api.example.com</code> недоступен: страница получает ошибку.</p><ul><li>выполните тот же запрос через <code>curl</code> — он должен пройти;</li><li>выполните его через <code>browser</code> — увидите блокировку;</li><li>прочитайте, чего именно не хватило в ответе;</li><li>зафиксируйте вывод.</li></ul>",
  hint:"browser https://api.example.com/orders — команда ходит по тому же адресу, но с правилами страницы.",
  setup: () => newScenario({
    origin: "https://app.example.com",
    dns: {"api.example.com": [{type:"A", value:"203.0.113.10"}],
          "app.example.com": [{type:"A", value:"203.0.113.20"}]},
    nodes: {"203.0.113.10": {ports: {443: {server:"api", tls:true, cert:"c1"}}},
            "203.0.113.20": {ports: {443: {server:"web", tls:true, cert:"c2"}}}},
    certs: {c1: {cn:"api.example.com", san:["api.example.com"], notAfter: 999999},
            c2: {cn:"app.example.com", san:["app.example.com"], notAfter: 999999}},
    servers: {
      api: {routes: [{path:"/orders", json:[{id:1}]}]},
      web: {routes: [{path:"/", body:"страница"}]}
    }
  }),
  checks:[
   {label:"Через <code>curl</code> запрос проходит", test:st=>st.log.some(e =>
     /^\s*curl\b/.test(e.cmd) && (e.out || []).some(l => /\{"id":1\}/.test(l)))},
   {label:"Через <code>browser</code> блокируется", test:st=>logHas(st, /ЗАБЛОКИРОВАНО/)},
   {label:"Причина прочитана", test:st=>logHas(st, /Access-Control-Allow-Origin/)},
   {label:"Вывод записан как <code>cors</code>", test:st=>verdict(st) === "cors"}
  ]},
 {id:"9b", title:"Предварительный запрос",
  brief:"<p>Страница шлёт запрос со своим заголовком <code>X-Request-Id</code>, и он не проходит, хотя обычный запрос к тому же адресу работает.</p><ul><li>убедитесь, что <code>browser</code> без своего заголовка проходит;</li><li>повторите с заголовком <code>X-Request-Id: 42</code> и посмотрите, что изменилось;</li><li>найдите в выводе предварительный запрос и то, чего он не разрешил;</li><li>зафиксируйте вывод.</li></ul>",
  hint:'browser -H "X-Request-Id: 42" https://api.example.com/orders',
  setup: () => newScenario({
    origin: "https://app.example.com",
    dns: {"api.example.com": [{type:"A", value:"203.0.113.10"}]},
    nodes: {"203.0.113.10": {ports: {443: {server:"api", tls:true, cert:"c1"}}}},
    certs: {c1: {cn:"api.example.com", san:["api.example.com"], notAfter: 999999}},
    servers: {
      api: {routes: [
        {path:"/orders", json:[{id:1}],
         headers:{"access-control-allow-origin":"https://app.example.com",
                  "access-control-allow-methods":"GET, POST",
                  "access-control-allow-headers":"content-type"}}
      ]}
    }
  }),
  checks:[
   {label:"Обычный запрос через браузер проходит", test:st=>st.log.some(e =>
     /^\s*browser\b/.test(e.cmd) && !/-H/.test(e.cmd) && !(e.out || []).some(l => /ЗАБЛОКИРОВАНО/.test(l)))},
   {label:"Со своим заголовком отправлялся", test:st=>ran(st,/browser[^|]*X-Request-Id/i)},
   {label:"Предварительный запрос виден", test:st=>logHas(st, /Предварительный запрос/)},
   {label:"Видно, какой заголовок не разрешён", test:st=>logHas(st, /не разрешил заголовок/)},
   {label:"Вывод записан как <code>cors</code>", test:st=>verdict(st) === "cors"}
  ]}
],
iv:[
 {q:"Что такое CORS и почему в curl работает, а в браузере нет?",
  probe:"Классический вопрос. Ответ «надо добавить заголовок» без объяснения механизма считается поверхностным.",
  a:"Это правило браузера, а не сервера: страница, загруженная с одного источника, по умолчанию не может читать ответы с другого. Ключевое здесь в том, что запрос всё равно уходит и ответ всё равно приходит — браузер просто не отдаёт его коду страницы, если сервер не разрешил это заголовком <code>Access-Control-Allow-Origin</code>. Поэтому curl, который никаких правил источника не соблюдает, выполняет тот же запрос без возражений, и это отличный способ за десять секунд отделить CORS от настоящей сетевой проблемы. Отдельно есть предварительный запрос: если метод не из простых, есть свой заголовок или в теле JSON, браузер сначала шлёт <code>OPTIONS</code> и идёт дальше только при разрешении — отсюда «два запроса вместо одного» и «запрос не доходит до обработчика». Чинится это на сервере; из кода страницы обойти проверку нельзя, а отключение её ключом запуска браузера маскирует проблему, а не решает.",
  more:["Когда шлётся предварительный запрос?","Почему нельзя сочетать звёздочку и куки?"]}
]
},
