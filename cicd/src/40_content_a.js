
/* ============================================================
   Содержание
   ============================================================ */

/* ── помощники ─────────────────────────────────────────── */
const ran   = (st, re) => st.log.some(e => re.test(e.cmd));
const okRan = (st, re) => st.log.some(e => !e.err && re.test(e.cmd));
const last  = st => st.C.runs.length ? st.C.runs[st.C.runs.length - 1] : null;
const jobOf = (st, n) => { const r = last(st); return r ? r.jobs.filter(j => j.name === n)[0] : null; };
const insts = (st, n) => { const j = jobOf(st, n); return j ? j.instances : []; };
const wf    = st => { try{ return parseWorkflow(st.C, workflowFile(st.C)); }catch(e){ return null; } };
const stepsOf = (st, n) => { const i = insts(st, n)[0]; return i ? i.steps : []; };
const logHas = (st, re) => st.log.some(e => (e.out || []).some(l => re.test(l)));

const MODULES = [

/* ------------------------------------------------ 1 */
{
n:1, id:"why", title:"Конвейер", sub:"Зачем и что в нём",
lede:"Непрерывная интеграция — не про инструмент, а про короткую обратную связь: изменение проверяется автоматически, одинаково у всех и до того, как о нём забыли.",
theory:`
<h2>Что решает конвейер</h2>
<div class="tw"><table>
<tr><th>Без него</th><th>С ним</th></tr>
<tr><td>«у меня работает»</td><td>сборка на чистой машине по описанию из репозитория</td></tr>
<tr><td>тесты гоняет тот, кто помнит</td><td>гоняются на каждое изменение</td></tr>
<tr><td>про поломку узнают через неделю</td><td>через десять минут, пока автор помнит контекст</td></tr>
<tr><td>шаги релиза в голове у одного человека</td><td>шаги в файле, который можно прочитать</td></tr>
</table></div>
<p>Ключевое слово — <b>воспроизводимость</b>. Конвейер описан в том же репозитории, что и код, версионируется вместе с ним и выполняется на чистой машине. Поэтому «работает на моей машине» перестаёт быть аргументом: есть третья сторона, у которой ничего нет заранее.</p>

<h2>Из чего он состоит</h2>
<div class="tw"><table>
<tr><th>Понятие</th><th>Что это</th></tr>
<tr><td><b>Событие</b> (<code>on</code>)</td><td>что запускает конвейер: отправка в ветку, pull request, расписание, ручной запуск</td></tr>
<tr><td><b>Задача</b> (<code>job</code>)</td><td>набор шагов на одной машине; задачи умеют идти параллельно</td></tr>
<tr><td><b>Шаг</b> (<code>step</code>)</td><td>одна команда (<code>run</code>) или готовое действие (<code>uses</code>)</td></tr>
<tr><td><b>Раннер</b></td><td>машина, на которой всё это выполняется</td></tr>
</table></div>
<div class="note ok"><b class="hd">Проверьте внизу</b><p>В сценарии лежит настоящий репозиторий с файлом конвейера. Наберите <code>cat .github/workflows/ci.yml</code>, чтобы прочитать его, затем <code>ci run</code> — конвейер выполнится, а <code>ci logs &lt;задача&gt;</code> покажет, что происходило внутри.</p></div>

<h2>Два времени</h2>
<pre><code>Итог: успех · время конвейера 3м 20с · машинного времени 7м 10с</code></pre>
<div class="tw"><table>
<tr><th>Величина</th><th>Что означает</th><th>Кого волнует</th></tr>
<tr><td><b>Время конвейера</b></td><td>сколько ждал человек</td><td>разработчика</td></tr>
<tr><td><b>Машинное время</b></td><td>сумма по всем задачам</td><td>того, кто платит за раннеры</td></tr>
</table></div>
<p>Параллельность уменьшает первое и не уменьшает второе. Кеш уменьшает оба. Это разные рычаги, и путать их не стоит: «ускорили конвейер вдвое» может означать и «стало дешевле», и «стало дороже».</p>

<h2>Что автоматизируют в первую очередь</h2>
<div class="tw"><table>
<tr><th>Шаг</th><th>Почему рано</th></tr>
<tr><td>Сборка</td><td>ловит то, что не компилируется у всех, кроме автора</td></tr>
<tr><td>Быстрые тесты</td><td>дают обратную связь за минуты</td></tr>
<tr><td>Проверка стиля и статический анализ</td><td>дёшево и снимает споры на ревью</td></tr>
<tr><td>Медленные тесты</td><td>позже и отдельной задачей — чтобы не блокировать быстрые</td></tr>
</table></div>
<div class="note trap"><b class="hd">Главный враг конвейера — недоверие</b><p>Конвейер, который «иногда падает сам по себе», перестают читать. Дальше по нарастающей: перезапустить не глядя, потом отключить обязательность проверки, потом вливать поверх красного. Поэтому нестабильные тесты — не косметическая проблема, а угроза самой идее. Об этом будет отдельный раздел.</p></div>
`,
quiz:[
 {q:"В чём главная ценность конвейера?",
  opts:["Экономия времени разработчика","Воспроизводимость и короткая обратная связь: проверка на чистой машине по описанию из репозитория","Красивые отчёты","Замена ревью"],
  a:1, why:"Поэтому «работает на моей машине» перестаёт быть аргументом: есть третья сторона, у которой ничего нет заранее."},
 {q:"Чем время конвейера отличается от машинного времени?",
  opts:["Ничем","Первое — сколько ждал человек, второе — сумма по всем задачам","Первое считается в минутах","Второе включает ожидание раннера"],
  a:1, why:"Параллельность уменьшает первое и не уменьшает второе. Кеш уменьшает оба."},
 {q:"Что такое задача (job) в конвейере?",
  opts:["Одна команда","Набор шагов на одной машине; задачи умеют идти параллельно","Файл конвейера","Событие запуска"],
  a:1, why:"Шаги внутри задачи выполняются по очереди на одной и той же машине, поэтому делят файловую систему."},
 {q:"Что стоит вынести в отдельную задачу, а не гнать вместе с быстрыми тестами?",
  opts:["Проверку стиля","Медленные тесты интерфейса","Сборку","Статический анализ"],
  a:1, why:"Иначе медленные тесты блокируют обратную связь по быстрым, ради которой всё и затевалось."},
 {q:"Почему нестабильные тесты опасны для самой идеи конвейера?",
  opts:["Они долго идут","Конвейеру перестают верить: перезапускают не глядя, потом отключают обязательность","Они занимают место","Они ломают кеш"],
  a:1, why:"Дальше вливают поверх красного, и проверка окончательно превращается в декорацию."},
 {q:"Где живёт описание конвейера?",
  opts:["В настройках сервера сборки","В репозитории вместе с кодом — версионируется тем же способом","В базе данных","У администратора"],
  a:1, why:"Поэтому изменение конвейера проходит ревью и откатывается так же, как изменение кода."}
],
labs:[
 {id:"1a", title:"Первый запуск",
  brief:"<p>В репозитории уже лежит конвейер. Разберитесь, что он делает, не меняя его:</p><ul><li>посмотрите список файлов — <code>ls</code>;</li><li>прочитайте описание конвейера — <code>cat .github/workflows/ci.yml</code>;</li><li>запустите — <code>ci run</code>;</li><li>откройте журнал задачи <code>build</code> — <code>ci logs build</code>.</li></ul>",
  hint:"Четыре команды подряд: ls, cat .github/workflows/ci.yml, ci run, ci logs build",
  setup: () => newScenario({files:{
    "src/App.cs": "class App { }",
    "shop.sln": "решение",
    "Directory.Packages.props": "<Project/>",
    ".github/workflows/ci.yml":
`name: CI
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: dotnet restore
      - run: dotnet build`}}),
  checks:[
   {label:"Файлы репозитория просмотрены", test:st=>okRan(st,/^ls\b/)},
   {label:"Описание конвейера прочитано", test:st=>okRan(st,/^cat\s+\.github/)},
   {label:"Конвейер запущен успешно", test:st=>{ const r = last(st); return !!r && r.status === "success"; }},
   {label:"Журнал задачи открыт", test:st=>okRan(st,/ci\s+logs\s+build/)}
  ]},
 {id:"1b", title:"Забытый checkout",
  brief:"<p>Конвейер падает на первом же шаге: команда не находит файлы проекта. Причина в том, что раннер получает <b>чистую машину</b> — кода на ней нет, пока его не выгрузят.</p><ul><li>запустите и посмотрите журнал, чтобы увидеть ошибку;</li><li>добавьте в начало задачи шаг <code>- uses: actions/checkout@v4</code>;</li><li>добейтесь успешного запуска.</li></ul>",
  hint:"Правьте .github/workflows/ci.yml во вкладке «Конвейер» внизу. Шаг checkout идёт первым в списке steps.",
  setup: () => newScenario({files:{
    "src/App.cs": "class App { }",
    "shop.sln": "решение",
    ".github/workflows/ci.yml":
`name: CI
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: cat shop.sln
      - run: dotnet build`}}),
  checks:[
   {label:"Падение было замечено", test:st=>st.C.runs.some(r => r.status === "failure")},
   {label:"Шаг выгрузки кода добавлен", test:st=>{
     const w = wf(st);
     return !!w && [].concat(w.jobs.build.steps || []).some(s => /actions\/checkout/.test(String(s.uses || ""))); }},
   {label:"Он стоит первым", test:st=>{
     const w = wf(st);
     return !!w && /actions\/checkout/.test(String(([].concat(w.jobs.build.steps || [])[0] || {}).uses || "")); }},
   {label:"Конвейер проходит", test:st=>{ const r = last(st); return !!r && r.status === "success"; }}
  ]}
],
iv:[
 {q:"Зачем нужна непрерывная интеграция, если есть ревью и тесты локально?",
  probe:"Открывающий вопрос. Ждут не пересказ определения, а понимание, что именно она даёт.",
  a:"Она даёт две вещи, которых нет ни у ревью, ни у локального прогона. Первая — воспроизводимость: сборка и тесты выполняются на чистой машине по описанию, которое лежит в том же репозитории, поэтому «у меня работает» перестаёт быть аргументом, а забытая зависимость или локальная настройка всплывают сразу. Вторая — короткая обратная связь: проверка идёт на каждое изменение, и автор узнаёт о поломке через минуты, пока ещё помнит, что делал, а не через неделю на чужой ветке. Ревью при этом никуда не девается, но перестаёт тратиться на то, что машина проверяет лучше — стиль, компиляцию, упавшие тесты. Для меня как автоматизатора есть и третий смысл: конвейер — единственное место, где автотесты гоняются регулярно и одинаково, а значит только там видно, какие из них на самом деле нестабильны.",
  more:["Что кладёте в конвейер в первую очередь?","Как понять, что конвейер работает плохо?"]}
]
},
